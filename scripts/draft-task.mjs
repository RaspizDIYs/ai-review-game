/**
 * Черновик задачи через ЛЛМ — офлайн, на этапе авторинга.
 *
 * Печатает промпт со всем контекстом: формат, живой пример из пака и список уже
 * занятых тем, чтобы модель не выдала двадцатую вариацию `except: pass`.
 * Ответ кладётся в content/tasks/ и дальше идёт обычным путём — шаги 4–6
 * из content/README.md, включая проверку вторым человеком.
 *
 * Ключей и сетевых вызовов тут нет намеренно: модель не должна становиться
 * зависимостью сборки, см. заметку «ЛЛМ на авторинге, а не в рантайме».
 *
 *   node scripts/draft-task.mjs <js|py|sql> <тема-кебабом> <сложность 1-5>
 *   node scripts/draft-task.mjs js race-condition 4 | pbcopy
 */
import { readdirSync, readFileSync } from 'node:fs'

const [stack, topic, difficulty] = process.argv.slice(2)

if (!['js', 'py', 'sql'].includes(stack) || !topic || !/^[1-5]$/.test(difficulty ?? '')) {
  console.error('node scripts/draft-task.mjs <js|py|sql> <тема-кебабом> <сложность 1-5>')
  process.exit(1)
}

const SRC = new URL('../content/tasks/', import.meta.url)
const files = readdirSync(SRC).filter((f) => f.endsWith('.json'))
const tasks = files.map((f) => JSON.parse(readFileSync(new URL(f, SRC), 'utf8')))

const id = `${stack}-${topic}-001`
if (files.includes(`${id}.json`)) {
  console.error(`${id} уже есть — возьми другую тему или номер`)
  process.exit(1)
}

const taken = [...new Set(tasks.flatMap((t) => t.bugs.map((b) => b.tag)))].sort()
const example = tasks.find((t) => t.stack === stack && !t.clean) ?? tasks[0]

console.log(`Ты помогаешь собрать контент для игры «Ревью за ИИ». Игроку показывают
запрос к нейросети, зелёный прогон тестов и диф. Он ищет строку, где код выглядит
рабочим, но им не является. Тесты в задаче ВСЕГДА проходят — подлянку не видно
на CI и видно только глазами.

Напиши одну задачу: стек ${stack}, тема «${topic}», сложность ${difficulty} из 5.

Требования, они же критерии приёмки:
1. Код собирается, тесты зелёные. Если подлянка валит CI — задача не годится.
2. Диф выглядит нормальной работой: подлянка не единственное изменение в нём.
3. Ты обязан одной фразой объяснить, ПОЧЕМУ зелёный тест врёт: «проверяет запись,
   а не чтение», «гоняет happy path», «фикстуры не задевают границу».
   Не выходит сформулировать — переделай задачу, она сырая.
4. consequence описывает, что сломается, у кого и когда. Конкретно.
   «Будут проблемы» — это не ответ.
5. Одна-две обманки: строки, к которым справедливо придраться, но которые
   подлянкой не являются. Обманка не должна стоять вплотную к подлянке kind=missing —
   у неё допуск ±1 строка.
6. match — кусок кода, уникальный среди строк новой версии файла.
   Не «}» и не «  return», бери фрагмент подлиннее. Пустую строку матчить нельзя.
7. kind: wrong — код на строке неверен. missing — кода не хватает,
   якорь ставится на строку, рядом с которой он должен был быть.
8. Домен — ветеринарная клиника: визиты, пациенты, назначения, склад, чеки, ОФД.
   Русские тексты, английский код.

Уже занятые темы, не повторяйся: ${taken.join(', ')}.

Ответь ОДНИМ JSON-объектом без markdown-обёртки, id = "${id}",
verified_by и verified_at — пустые строки.

Вот живой пример из пака, держи такой же уровень детализации:

${JSON.stringify(example, null, 2)}`)
