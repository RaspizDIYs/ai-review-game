[
  {
    "id": "cpp-dangling-reference-001",
    "stack": "cpp",
    "difficulty": 1,
    "title": "Имя кабинета по номеру",
    "prompt": "Отдавай подпись кабинета по номеру, для неизвестных — «кабинет N»",
    "tests": "[==========] Running 4 tests from 1 test suite.\n[  PASSED  ] 4 tests.",
    "diff": "--- a/src/rooms.cpp\n+++ b/src/rooms.cpp\n@@ -8,15 +8,28 @@ namespace clinic {\n \n static const std::map<int, std::string> kNames = {\n     {1, \"Приёмная\"}, {2, \"Операционная\"}, {3, \"Рентген\"},\n };\n \n-const std::string& RoomName(int number) {\n-    return kNames.at(number);\n+const std::string& RoomName(int number) {\n+    auto it = kNames.find(number);\n+    if (it != kNames.end()) {\n+        return it->second;\n+    }\n+\n+    std::string fallback = \"кабинет \" + std::to_string(number);\n+\n+    return fallback;\n+}\n+\n+bool RoomExists(int number) {\n+    return kNames.count(number) > 0;\n }\n \n }  // namespace clinic\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/rooms.cpp",
        "kind": "wrong",
        "tag": "dangling-reference",
        "explain": "Возвращается ссылка на локальную строку, которая умирает вместе с вызовом. Для известных кабинетов ссылка живая, для остальных — висячая.",
        "consequence": "Подпись неизвестного кабинета печатается то верно, то мусором, то роняет процесс — зависит от того, что легло на освободившийся стек. Тесты гоняют только три кабинета из справочника, поэтому зелёные.",
        "line": 21
      }
    ],
    "decoys": [
      {
        "why": "count вместо contains — старый стиль, но работает верно",
        "line": 25
      },
      {
        "why": "Лишняя аллокация на каждый неизвестный кабинет — расточительно, но само по себе безопасно",
        "line": 19
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "cpp-iterator-invalidation-001",
    "stack": "cpp",
    "difficulty": 2,
    "title": "Разбивка длинных приёмов",
    "prompt": "Разбей приёмы длиннее часа на две записи по половине, порядок не важен",
    "tests": "[==========] Running 6 tests from 1 test suite.\n[  PASSED  ] 6 tests.",
    "diff": "--- a/src/schedule.cpp\n+++ b/src/schedule.cpp\n@@ -10,16 +10,29 @@ namespace clinic {\n \n constexpr int kLongVisitMinutes = 60;\n \n-void SplitLongVisits(std::vector<Visit>& visits) {\n-    (void)visits;\n+void SplitLongVisits(std::vector<Visit>& visits) {\n+    for (auto& visit : visits) {\n+        if (visit.minutes <= kLongVisitMinutes) {\n+            continue;\n+        }\n+\n+        const int half = visit.minutes / 2;\n+        visit.minutes = half;\n+\n+        Visit tail = visit;\n+        tail.at = visit.at + std::chrono::minutes(half);\n+\n+        visits.push_back(tail);\n+    }\n }\n \n int TotalMinutes(const std::vector<Visit>& visits) {\n     int total = 0;\n     for (const auto& visit : visits) {\n         total += visit.minutes;\n     }\n     return total;\n }\n \n }  // namespace clinic\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/schedule.cpp",
        "kind": "wrong",
        "tag": "mutating-while-iterating",
        "explain": "push_back внутри range-for по тому же вектору: при перевыделении памяти итераторы и ссылка visit становятся недействительными.",
        "consequence": "На маленьком расписании ёмкости хватает и всё «работает». На загруженном дне вектор переезжает, цикл продолжает читать освобождённую память — расписание приезжает с мусорными приёмами или процесс падает. Воспроизводится только начиная с определённого числа записей.",
        "line": 25
      }
    ],
    "decoys": [
      {
        "why": "Нечётная длительность теряет минуту при делении — неточность, но не порча памяти",
        "line": 19
      },
      {
        "why": "Вторая половина начинается ровно там, где кончилась первая, без перерыва — вопрос к продукту",
        "line": 23
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "cpp-shared-ptr-cycle-001",
    "stack": "cpp",
    "difficulty": 3,
    "title": "Питомец знает своего владельца",
    "prompt": "Пусть из карточки питомца можно дойти до владельца и обратно",
    "tests": "[==========] Running 5 tests from 1 test suite.\n[  PASSED  ] 5 tests.",
    "diff": "--- a/src/owner.h\n+++ b/src/owner.h\n@@ -6,17 +6,30 @@ namespace clinic {\n \n struct Owner;\n \n struct Pet {\n     std::string name;\n-    std::shared_ptr<Owner> owner;\n+    std::shared_ptr<Owner> owner;\n+    int visits = 0;\n };\n \n struct Owner {\n     std::string last_name;\n-    std::vector<std::string> pet_names;\n+    std::vector<std::shared_ptr<Pet>> pets;\n };\n \n+inline void Link(const std::shared_ptr<Owner>& owner, const std::shared_ptr<Pet>& pet) {\n+    pet->owner = owner;\n+    owner->pets.push_back(pet);\n+}\n+\n+inline int TotalVisits(const Owner& owner) {\n+    int total = 0;\n+    for (const auto& pet : owner.pets) {\n+        total += pet->visits;\n+    }\n+    return total;\n+}\n+\n }  // namespace clinic\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/owner.h",
        "kind": "wrong",
        "tag": "shared-ptr-cycle",
        "explain": "Владелец держит питомцев, питомец держит владельца — оба по shared_ptr. Счётчики ссылок никогда не дойдут до нуля; обратная ссылка должна быть weak_ptr.",
        "consequence": "Каждая загруженная карточка остаётся в памяти навсегда. Сервис регистратуры на длинной смене распухает и уходит по OOM ближе к вечеру, а на коротком прогоне тестов утечка не видна вовсе.",
        "line": 11
      }
    ],
    "decoys": [
      {
        "why": "Повторный вызов Link добавит питомца дважды — дубликат в списке, но не утечка",
        "line": 22
      },
      {
        "why": "Разыменование без проверки на пустой указатель — упадёт, если в списке окажется nullptr",
        "line": 28
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "cpp-signed-unsigned-001",
    "stack": "cpp",
    "difficulty": 4,
    "title": "Последние приёмы за смену",
    "prompt": "Верни последние N приёмов из списка, если их меньше N — верни все",
    "tests": "[==========] Running 7 tests from 1 test suite.\n[  PASSED  ] 7 tests.",
    "diff": "--- a/src/visits.cpp\n+++ b/src/visits.cpp\n@@ -9,16 +9,29 @@ namespace clinic {\n \n-std::vector<Visit> LastVisits(const std::vector<Visit>& visits, int n) {\n-    return visits;\n+std::vector<Visit> LastVisits(const std::vector<Visit>& visits, int n) {\n+    if (n <= 0) {\n+        return {};\n+    }\n+\n+    std::vector<Visit> tail;\n+    tail.reserve(n);\n+\n+    for (size_t i = visits.size() - n; i < visits.size(); ++i) {\n+        tail.push_back(visits[i]);\n+    }\n+\n+    return tail;\n }\n \n bool ByTime(const Visit& a, const Visit& b) {\n     return a.at < b.at;\n }\n \n }  // namespace clinic\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/visits.cpp",
        "kind": "wrong",
        "tag": "signed-unsigned-compare",
        "explain": "size() беззнаковый: если приёмов меньше n, разность не станет отрицательной, а завернётся в огромное число. Условие цикла сразу ложно — но индекс уже за пределами вектора.",
        "consequence": "Случай «приёмов меньше, чем просили» — ровно тот, что просили обработать, — молча отдаёт пустой список вместо всех записей. На новой клинике с тремя визитами за день сводка пустая, на старой всё в порядке.",
        "line": 18
      }
    ],
    "decoys": [
      {
        "why": "reserve со знаковым n рядом с беззнаковым размером выглядит подозрительно, но само по себе безопасно",
        "line": 16
      },
      {
        "why": "Сравнение без учёта одинакового времени даёт нестабильный порядок — к выборке последних отношения не имеет",
        "line": 26
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "cpp-uninitialized-value-001",
    "stack": "cpp",
    "difficulty": 1,
    "title": "Средний вес по приёмам",
    "prompt": "Посчитай средний вес питомца по приёмам, на пустом списке верни ноль",
    "tests": "[==========] Running 4 tests from 1 test suite.\n[  PASSED  ] 4 tests.",
    "diff": "--- a/src/weight_stats.cpp\n+++ b/src/weight_stats.cpp\n@@ -6,15 +6,28 @@ namespace clinic {\n \n-double AverageWeight(const std::vector<Visit>& visits) {\n-    return 0.0;\n+double AverageWeight(const std::vector<Visit>& visits) {\n+    if (visits.empty()) {\n+        return 0.0;\n+    }\n+\n+    double sum;\n+\n+    for (const auto& visit : visits) {\n+        sum += visit.weight_kg;\n+    }\n+\n+    return sum / static_cast<double>(visits.size());\n }\n \n double MaxWeight(const std::vector<Visit>& visits) {\n     double max = 0.0;\n     for (const auto& visit : visits) {\n         max = std::max(max, visit.weight_kg);\n     }\n     return max;\n }\n \n }  // namespace clinic\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/weight_stats.cpp",
        "kind": "wrong",
        "tag": "uninitialized-value",
        "explain": "Локальная переменная не инициализирована, а к ней сразу прибавляют. Соседняя функция начинает с нуля явно — здесь про это забыли.",
        "consequence": "Средний вес приезжает то верным, то астрономическим — зависит от того, что осталось на стеке. В отладочной сборке обычно ноль, поэтому тесты зелёные, а жалобы приходят только из продовой.",
        "line": 12
      }
    ],
    "decoys": [
      {
        "why": "Приведение размера к double выглядит избыточно, но оно как раз правильное",
        "line": 18
      },
      {
        "why": "Максимум с нуля не сработает для отрицательных значений — для веса это не случай",
        "line": 22
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "cs-async-void-001",
    "stack": "cs",
    "difficulty": 1,
    "title": "Уведомление после сохранения",
    "prompt": "После сохранения записи отправляй уведомление, только не тормози на этом ответ",
    "tests": "Passed!  - Failed: 0, Passed: 7, Skipped: 0, Total: 7, Duration: 63 ms\nBuild succeeded.",
    "diff": "--- a/src/Appointments/AppointmentService.cs\n+++ b/src/Appointments/AppointmentService.cs\n@@ -21,12 +21,24 @@ public class AppointmentService\n     public async Task<int> SaveAsync(Appointment appointment)\n     {\n         _db.Appointments.Add(appointment);\n         await _db.SaveChangesAsync();\n \n+        NotifyAsync(appointment);\n+\n         return appointment.Id;\n     }\n \n+    private async void NotifyAsync(Appointment appointment)\n+    {\n+        var owner = await _db.Owners.FindAsync(appointment.OwnerId);\n+        if (owner is null)\n+        {\n+            return;\n+        }\n+\n+        await _sms.SendAsync(owner.Phone, Templates.Reminder(appointment));\n+    }\n+\n     public Task<Appointment?> FindAsync(int id) =>\n         _db.Appointments.FindAsync(id).AsTask();\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/Appointments/AppointmentService.cs",
        "kind": "wrong",
        "tag": "async-void",
        "explain": "async void вместо async Task: вызывающий код ничего не ждёт и, главное, не может поймать исключение — оно летит прямо в процесс.",
        "consequence": "Отвалившаяся смс-шлюзом ручка роняет весь воркер, а не один вызов. В логах при этом остаётся успешное сохранение приёма: до падения оно уже прошло, и связь между двумя событиями никто не увидит.",
        "line": 31
      }
    ],
    "decoys": [
      {
        "why": "Вызов без await выглядит подозрительно, но он тут намеренный — просили не тормозить ответ",
        "line": 26
      },
      {
        "why": "Ещё один поход в базу там, где владельца можно было подтянуть заранее — вопрос к производительности",
        "line": 33
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "cs-error-swallowed-001",
    "stack": "cs",
    "difficulty": 1,
    "title": "Импорт справочника пород",
    "prompt": "Импортируй породы из файла поставщика, кривые строки просто пропускай",
    "tests": "Passed!  - Failed: 0, Passed: 4, Skipped: 0, Total: 4, Duration: 19 ms",
    "diff": "--- a/src/Breeds/BreedImporter.cs\n+++ b/src/Breeds/BreedImporter.cs\n@@ -12,14 +12,28 @@ public class BreedImporter\n     private readonly ClinicContext _db;\n \n-    public int Import(IEnumerable<string> lines) => 0;\n+    public int Import(IEnumerable<string> lines)\n+    {\n+        var imported = 0;\n+\n+        foreach (var line in lines)\n+        {\n+            try\n+            {\n+                _db.Breeds.Add(Parse(line));\n+                imported++;\n+            }\n+            catch\n+            {\n+            }\n+        }\n+\n+        _db.SaveChanges();\n+\n+        return imported;\n+    }\n \n     private static Breed Parse(string line)\n     {\n         var cells = line.Split(';');\n \n         return new Breed { Code = cells[0], Name = cells[1] };\n     }\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/Breeds/BreedImporter.cs",
        "kind": "wrong",
        "tag": "error-swallowed",
        "explain": "Пустой catch без типа глотает вообще всё: и кривую строку, и обрыв соединения с базой, и опечатку в коде. Ни счётчика пропусков, ни записи в лог.",
        "consequence": "Поставщик сменил разделитель — импорт отчитывается «0 пород» и считается успешным. Справочник пустеет, а в мониторинге ровно ничего: единственный след останется в цифре, на которую никто не смотрит.",
        "line": 25
      }
    ],
    "decoys": [
      {
        "why": "Обращение к ячейкам без проверки длины — но именно на этом и держится «просто пропускай»",
        "line": 39
      },
      {
        "why": "Сохранение одним разом в конце — правильный выбор, хотя при падении пропадёт вся пачка",
        "line": 30
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "cs-httpclient-per-call-001",
    "stack": "cs",
    "difficulty": 3,
    "title": "Проверка полиса в страховой",
    "prompt": "Сходи в страховую за статусом полиса, обязательно с таймаутом",
    "tests": "Passed!  - Failed: 0, Passed: 9, Skipped: 0, Total: 9, Duration: 148 ms",
    "diff": "--- a/src/Insurance/PolicyChecker.cs\n+++ b/src/Insurance/PolicyChecker.cs\n@@ -14,13 +14,29 @@ public class PolicyChecker\n     private readonly IOptions<InsuranceOptions> _options;\n \n     public PolicyChecker(IOptions<InsuranceOptions> options) => _options = options;\n \n-    public Task<PolicyStatus> CheckAsync(string number) =>\n-        Task.FromResult(PolicyStatus.Unknown);\n+    public async Task<PolicyStatus> CheckAsync(string number, CancellationToken ct)\n+    {\n+        using var http = new HttpClient\n+        {\n+            Timeout = TimeSpan.FromSeconds(3),\n+        };\n+\n+        var url = $\"{_options.Value.BaseUrl}/policies/{number}\";\n+        using var response = await http.GetAsync(url, ct);\n+\n+        if (!response.IsSuccessStatusCode)\n+        {\n+            return PolicyStatus.Unknown;\n+        }\n+\n+        var body = await response.Content.ReadFromJsonAsync<PolicyDto>(ct);\n+        return body?.Active == true ? PolicyStatus.Active : PolicyStatus.Expired;\n+    }\n \n     public bool IsSupported(string number) => number.Length == 16;\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/Insurance/PolicyChecker.cs",
        "kind": "wrong",
        "tag": "httpclient-per-call",
        "explain": "HttpClient создаётся и уничтожается на каждый вызов. Сокет после Dispose остаётся в TIME_WAIT, и пул не переиспользуется — клиента берут из IHttpClientFactory один раз.",
        "consequence": "На пиковой нагрузке порты кончаются, и сервис начинает отвечать «нет соединения» на все исходящие запросы сразу — включая те, что к страховой отношения не имеют. Проявляется только под нагрузкой, на стенде никогда.",
        "line": 20
      }
    ],
    "decoys": [
      {
        "why": "Таймаут задан прямо в коде, а не в настройках — просили с таймаутом, здесь он есть",
        "line": 22
      },
      {
        "why": "Неуспешный ответ превращается в Unknown без записи в лог — разбирать инцидент будет тяжело, но полис при этом не портится",
        "line": 30
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "cs-local-time-001",
    "stack": "cs",
    "difficulty": 2,
    "title": "Просроченные напоминания",
    "prompt": "Добавь выборку напоминаний, у которых вышел срок, — по ней будем слать повторы",
    "tests": "Passed!  - Failed: 0, Passed: 5, Skipped: 0, Total: 5, Duration: 22 ms",
    "diff": "--- a/src/Reminders/ReminderQuery.cs\n+++ b/src/Reminders/ReminderQuery.cs\n@@ -9,13 +9,25 @@ public class ReminderQuery\n     private readonly ClinicContext _db;\n \n     public ReminderQuery(ClinicContext db) => _db = db;\n \n-    public IQueryable<Reminder> Pending() =>\n-        _db.Reminders.Where(r => !r.Sent);\n+    public IQueryable<Reminder> Pending() =>\n+        _db.Reminders.Where(r => !r.Sent);\n+\n+    public async Task<List<Reminder>> OverdueAsync(int limit = 100)\n+    {\n+        var now = DateTime.Now;\n+\n+        return await _db.Reminders\n+            .Where(r => !r.Sent && r.DueAt <= now)\n+            .OrderBy(r => r.DueAt)\n+            .Take(limit)\n+            .ToListAsync();\n+    }\n \n     public Task<int> CountAsync() => _db.Reminders.CountAsync();\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/Reminders/ReminderQuery.cs",
        "kind": "wrong",
        "tag": "naive-datetime",
        "explain": "DateTime.Now отдаёт время машины в её часовом поясе, а DueAt лежит в базе в UTC.",
        "consequence": "Разработчик в Москве видит, что напоминания уезжают на три часа раньше срока, а на сервере в UTC всё «работает». Летом сдвиг меняется, и баг воспроизводится по-разному в марте и в ноябре.",
        "line": 18
      }
    ],
    "decoys": [
      {
        "why": "Лимит по умолчанию сотня — если просрочек больше, часть уедет в следующий проход, и это осознанно",
        "line": 23
      },
      {
        "why": "Метод возвращает IQueryable наружу — течёт абстракция базы, но поведение от этого не ломается",
        "line": 14
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "cs-sync-over-async-001",
    "stack": "cs",
    "difficulty": 4,
    "title": "Кэш справочника пород",
    "prompt": "Закэшируй справочник пород в памяти, чтобы не ходить в базу на каждый экран",
    "tests": "Passed!  - Failed: 0, Passed: 11, Skipped: 0, Total: 11, Duration: 87 ms",
    "diff": "--- a/src/Breeds/BreedCache.cs\n+++ b/src/Breeds/BreedCache.cs\n@@ -11,15 +11,30 @@ public class BreedCache\n     private readonly IMemoryCache _cache;\n     private readonly ClinicContext _db;\n \n     public BreedCache(IMemoryCache cache, ClinicContext db)\n     {\n         _cache = cache;\n         _db = db;\n     }\n \n-    public Task<IReadOnlyList<Breed>> AllAsync() =>\n-        LoadAsync();\n+    public IReadOnlyList<Breed> All() =>\n+        _cache.GetOrCreate(\"breeds\", entry =>\n+        {\n+            entry.AbsoluteExpirationRelativeToNow = TimeSpan.FromHours(1);\n+\n+            return LoadAsync().Result;\n+        })!;\n \n     private async Task<IReadOnlyList<Breed>> LoadAsync() =>\n         await _db.Breeds.OrderBy(b => b.Name).ToListAsync();\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/Breeds/BreedCache.cs",
        "kind": "wrong",
        "tag": "sync-over-async",
        "explain": "Асинхронный поход в базу ждут блокирующим .Result внутри синхронного метода. Поток из пула стоит всё время запроса, а на классическом контексте синхронизации это ещё и взаимная блокировка.",
        "consequence": "Пока кэш холодный, каждый запрос занимает поток целиком. На всплеске после деплоя пул исчерпывается, и сервис перестаёт отвечать на всё — включая ручку здоровья, по которой оркестратор его тут же перезапускает, снова с холодным кэшем.",
        "line": 25
      }
    ],
    "decoys": [
      {
        "why": "Час жизни без инвалидации при правке справочника — вопрос к свежести данных, но не причина зависаний",
        "line": 23
      },
      {
        "why": "Сортировка на стороне базы без индекса по имени — лишняя работа, но справочник крошечный",
        "line": 29
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "docker-compose-two-holes-002",
    "stack": "docker",
    "difficulty": 4,
    "title": "Вынесли базу в отдельный сервис",
    "prompt": "Раздели апи и базу по контейнерам, чтобы можно было обновлять их по отдельности",
    "tests": "[+] Running 2/2\n ✔ Container vet-postgres  Started\n ✔ Container vet-api       Started\n\nsmoke: POST /api/visits 201 · GET /api/visits 200 (14 записей)",
    "diff": "--- a/docker-compose.yml\n+++ b/docker-compose.yml\n@@ -1,14 +1,36 @@\n services:\n   api:\n     image: vet-api:0.6.0\n     restart: unless-stopped\n     environment:\n       DATABASE_URL: postgres://vet:vet@postgres:5432/vet\n     ports:\n       - \"3000:3000\"\n+    depends_on:\n+      - postgres\n \n+  postgres:\n+    image: postgres:16.4-alpine\n+    restart: unless-stopped\n+    environment:\n+      POSTGRES_USER: vet\n+      POSTGRES_PASSWORD: vet\n+      POSTGRES_DB: vet\n+    ports:\n+      - \"5432:5432\"\n+    volumes:\n+      - pgdata:/var/lib/postgresql/data\n+\n+volumes:\n+  pgdata:\n",
    "clean": false,
    "bugs": [
      {
        "file": "docker-compose.yml",
        "kind": "wrong",
        "tag": "default-credentials-in-config",
        "explain": "Пароль базы записан в файл, который лежит в репозитории, и он же — словарное слово. Пароль берут из переменной окружения и падают, если её нет.",
        "consequence": "Пароль от прод-базы известен всем, у кого есть доступ к репозиторию, и остаётся в истории навсегда. Менять его придётся вместе со строками подключения во всех сервисах.",
        "line": 17
      },
      {
        "file": "docker-compose.yml",
        "kind": "wrong",
        "tag": "database-port-published",
        "explain": "Порт базы проброшен наружу, на все интерфейсы хоста. Апи ходит в базу по внутренней сети композа, публиковать порт для этого не нужно вовсе.",
        "consequence": "Постгрес со словарным паролем оказывается доступен из интернета. Находят такое сканеры за часы, а не за месяцы.",
        "line": 20
      }
    ],
    "decoys": [
      {
        "why": "зависимость без условия готовности — апи стартует раньше базы и переподключается, неаккуратно, но не дыра",
        "line": 9
      },
      {
        "why": "данные на именованном томе — как раз правильно",
        "line": 22
      },
      {
        "why": "апи наружу торчать и должен, это его работа",
        "line": 8
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "docker-copy-before-install-001",
    "stack": "docker",
    "difficulty": 2,
    "title": "Апи в контейнере",
    "prompt": "Заверни апи в докер, чтобы поднималось одной командой",
    "tests": "[+] Building 41.3s (11/11) FINISHED\n => => naming to docker.io/library/vet-api:0.4.1\n[+] Running 1/1\n ✔ Container vet-api  Started\n\nhealthcheck: ok (200 /ready)",
    "diff": "--- a/Dockerfile\n+++ b/Dockerfile\n@@ -1,3 +1,22 @@\n+FROM node:22.11-bookworm-slim\n+\n+WORKDIR /app\n+\n+# ставим зависимости и собираем\n+COPY . .\n+RUN npm ci --omit=dev\n+RUN npm run build\n+\n+ENV NODE_ENV=production\n+ENV PORT=3000\n+EXPOSE 3000\n+\n+USER node\n+\n+HEALTHCHECK --interval=30s --timeout=3s \\\n+  CMD node -e \"fetch('http://127.0.0.1:3000/ready').then(r=>process.exit(r.ok?0:1))\"\n+\n+CMD [\"node\", \"dist/server.js\"]\n",
    "clean": false,
    "bugs": [
      {
        "file": "Dockerfile",
        "kind": "wrong",
        "tag": "docker-copy-before-install",
        "explain": "Слой с зависимостями зависит от всего репозитория: любая правка исходника инвалидирует кэш, и npm ci запускается заново. Копировать package.json и package-lock.json надо отдельным слоем до установки, а остальное — после.",
        "consequence": "Сборка на CI из сорока секунд превращается в четыре минуты, и так на каждый коммит. Замечают это не сразу, а через месяц, когда деплой перестаёт помещаться в перерыв.",
        "line": 6
      }
    ],
    "decoys": [
      {
        "why": "процесс не от root — так и надо, это как раз правильно",
        "line": 14
      },
      {
        "why": "версия зафиксирована явно, а не latest — претензий нет",
        "line": 1
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "docker-dev-env-in-prod-001",
    "stack": "docker",
    "difficulty": 2,
    "title": "Образ админки",
    "prompt": "Собери образ админки так, чтобы можно было посмотреть подробную ошибку, если что-то упадёт",
    "tests": "[+] Building 36.9s (10/10) FINISHED\n => => naming to docker.io/library/vet-admin:0.3.0\n[+] Running 1/1\n ✔ Container vet-admin  Started\n\nGET /health 200 · GET /admin 200 · размер образа 412MB",
    "diff": "--- a/admin.Dockerfile\n+++ b/admin.Dockerfile\n@@ -1,8 +1,24 @@\n FROM node:22.11-bookworm-slim\n \n WORKDIR /app\n \n COPY package.json package-lock.json ./\n-RUN npm ci --omit=dev\n+RUN npm ci\n \n COPY . .\n RUN npm run build\n \n+ENV NODE_ENV=development\n+ENV LOG_LEVEL=debug\n+ENV PORT=8080\n+\n+EXPOSE 8080\n+USER node\n+\n CMD [\"node\", \"dist/admin.js\"]\n",
    "clean": false,
    "bugs": [
      {
        "file": "admin.Dockerfile",
        "kind": "wrong",
        "tag": "dev-mode-in-prod-image",
        "explain": "От NODE_ENV зависит не только уровень логов: фреймворки в режиме разработки отключают кэш шаблонов, не минифицируют ответы и отдают полный стектрейс в теле ошибки. Просили подробную ошибку — её дают LOG_LEVEL и нормальный сбор логов, а не режим разработки.",
        "consequence": "Админка работает вдвое медленнее и на первой же пятисотке показывает пользователю пути на диске и куски кода. Замечают это, когда скриншот такой ошибки прилетает в поддержку от клиента.",
        "line": 11
      }
    ],
    "decoys": [
      {
        "why": "без --omit=dev в образ едут дев-зависимости — лишние сто мегабайт, но не ошибка поведения",
        "line": 6
      },
      {
        "why": "подробные логи как раз и просили — многовато для прода, но это настройка",
        "line": 12
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "docker-healthcheck-shallow-001",
    "stack": "docker",
    "difficulty": 3,
    "title": "Проверка готовности контейнера",
    "prompt": "Добавь healthcheck, чтобы оркестратор не слал трафик в неподнявшийся контейнер",
    "tests": "[+] Running 2/2\n ✔ Container vet-postgres  Healthy\n ✔ Container vet-api       Healthy\n\ndocker inspect --format='{{.State.Health.Status}}' vet-api\nhealthy",
    "diff": "--- a/docker-compose.yml\n+++ b/docker-compose.yml\n@@ -1,16 +1,34 @@\n services:\n   api:\n     image: vet-api:0.4.1\n     restart: unless-stopped\n     environment:\n       DATABASE_URL: ${DATABASE_URL:?нужен DATABASE_URL}\n     ports:\n       - \"3000:3000\"\n+    healthcheck:\n+      test: [\"CMD\", \"curl\", \"-fsS\", \"http://127.0.0.1:3000/\"]\n+      interval: 10s\n+      timeout: 3s\n+      retries: 3\n+      start_period: 5s\n+    depends_on:\n+      postgres:\n+        condition: service_healthy\n \n   postgres:\n     image: postgres:16.4-alpine\n     restart: unless-stopped\n+    healthcheck:\n+      test: [\"CMD-SHELL\", \"pg_isready -U vet\"]\n+      interval: 10s\n+      timeout: 3s\n+      retries: 5\n     volumes:\n       - pgdata:/var/lib/postgresql/data\n \n volumes:\n   pgdata:\n",
    "clean": false,
    "bugs": [
      {
        "file": "docker-compose.yml",
        "kind": "wrong",
        "tag": "healthcheck-checks-nothing",
        "explain": "Корневая ручка отдаёт статику и живёт без базы. Контейнер отчитывается «healthy», ещё не открыв ни одного соединения к постгресу. Проверять надо ту ручку, которая ходит в зависимости, — /ready.",
        "consequence": "После выкатки балансировщик пускает трафик в контейнер, который не может ответить ни на один реальный запрос. Пользователи видят пятисотые, а мониторинг — зелёный healthcheck, и первые двадцать минут инцидента уходят на спор, кто врёт.",
        "line": 10
      }
    ],
    "decoys": [
      {
        "why": "пять секунд на старт маловато для холодного джаваскрипта, но это вопрос настройки, а не ошибка",
        "line": 14
      },
      {
        "why": "апи ждёт готовности базы — как раз правильно",
        "line": 17
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "docker-latest-tag-001",
    "stack": "docker",
    "difficulty": 1,
    "title": "Образ для обработчика очереди",
    "prompt": "Собери отдельный образ под воркер, который разгребает очередь напоминаний",
    "tests": "[+] Building 28.4s (9/9) FINISHED\n => => naming to docker.io/library/vet-worker:0.2.0\n[+] Running 1/1\n ✔ Container vet-worker  Started\n\nworker: подключился к очереди, задач в работе 0",
    "diff": "--- a/worker.Dockerfile\n+++ b/worker.Dockerfile\n@@ -1,3 +1,20 @@\n+FROM node:latest\n+\n+WORKDIR /app\n+\n+COPY package.json package-lock.json ./\n+RUN npm ci --omit=dev\n+\n+COPY src ./src\n+\n+ENV NODE_ENV=production\n+ENV QUEUE_URL=${QUEUE_URL:?нужен QUEUE_URL}\n+\n+USER node\n+\n+CMD [\"node\", \"src/worker.js\"]\n",
    "clean": false,
    "bugs": [
      {
        "file": "worker.Dockerfile",
        "kind": "wrong",
        "tag": "base-image-floats",
        "explain": "Тег latest — это не версия, а указатель, который меняется без спроса. Сегодня за ним двадцать вторая нода, завтра двадцать четвёртая. Базовый образ фиксируют версией, а лучше ещё и дайджестом.",
        "consequence": "Сборка, которая проходила месяц, однажды падает или, хуже, не падает — воркер поднимается на новой ноде с другим поведением таймеров. Виноватым выглядит последний коммит, хотя его автор не трогал ничего похожего.",
        "line": 1
      }
    ],
    "decoys": [
      {
        "why": "зависимости отдельным слоем до исходников — правильный порядок, кэш не ломается",
        "line": 5
      },
      {
        "why": "подстановка в ENV на этапе сборки выглядит странно, но это просто значение по умолчанию",
        "line": 11
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "docker-secret-in-layer-001",
    "stack": "docker",
    "difficulty": 4,
    "title": "Приватный пакет в сборке",
    "prompt": "Нам нужен наш приватный npm-пакет в образе, добавь токен в сборку",
    "tests": "[+] Building 52.7s (13/13) FINISHED\n => [4/8] RUN npm ci --omit=dev                              31.2s\n => => added 214 packages, including @vet/billing-core\n => => naming to docker.io/library/vet-api:0.5.0\n\ntrivy image vet-api:0.5.0 → 0 CRITICAL, 0 HIGH",
    "diff": "--- a/Dockerfile\n+++ b/Dockerfile\n@@ -1,14 +1,26 @@\n FROM node:22.11-bookworm-slim\n \n WORKDIR /app\n \n+ARG NPM_TOKEN\n+RUN printf \"//registry.npmjs.org/:_authToken=%s\\n\" \"$NPM_TOKEN\" > .npmrc\n+\n COPY package.json package-lock.json ./\n RUN npm ci --omit=dev\n \n+RUN rm -f .npmrc\n+\n COPY src ./src\n RUN npm run build\n \n ENV NODE_ENV=production\n USER node\n \n CMD [\"node\", \"dist/server.js\"]\n",
    "clean": false,
    "bugs": [
      {
        "file": "Dockerfile",
        "kind": "wrong",
        "tag": "secret-baked-into-layer",
        "explain": "Слой с токеном остаётся в образе навсегда: следующий RUN его не удаляет, он лишь добавляет слой поверх. Токен достаётся из образа командой docker history. Для этого есть RUN --mount=type=secret, который в слой ничего не пишет.",
        "consequence": "Образ уезжает в реестр, а вместе с ним и токен публикации в npm. Утечка обнаруживается не сканером — он ничего криминального не видит, — а тем, что от вашего имени опубликован пакет.",
        "line": 6
      }
    ],
    "decoys": [
      {
        "why": "удаление выглядит как решение проблемы — в этом и подвох, но сама строка не ошибка",
        "line": 11
      },
      {
        "why": "зависимости отдельным слоем до исходников — правильный порядок",
        "line": 8
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "docker-volume-missing-001",
    "stack": "docker",
    "difficulty": 5,
    "title": "Хранилище файлов клиники",
    "prompt": "Подними рядом minio под снимки и анализы, чтобы не держать файлы в базе",
    "tests": "[+] Running 3/3\n ✔ Container vet-postgres  Healthy\n ✔ Container vet-minio     Healthy\n ✔ Container vet-api       Healthy\n\nsmoke: загружен снимок 2.1MB → отдан обратно, контрольная сумма совпала",
    "diff": "--- a/docker-compose.yml\n+++ b/docker-compose.yml\n@@ -1,20 +1,40 @@\n services:\n   api:\n     image: vet-api:0.5.0\n     restart: unless-stopped\n     environment:\n       DATABASE_URL: ${DATABASE_URL:?}\n       S3_ENDPOINT: http://minio:9000\n     depends_on:\n       minio:\n         condition: service_healthy\n \n+  minio:\n+    image: minio/minio:RELEASE.2026-05-14T02-52-50Z\n+    restart: unless-stopped\n+    command: server /data --console-address \":9001\"\n+    environment:\n+      MINIO_ROOT_USER: ${MINIO_USER:?}\n+      MINIO_ROOT_PASSWORD: ${MINIO_PASSWORD:?}\n+    healthcheck:\n+      test: [\"CMD\", \"mc\", \"ready\", \"local\"]\n+      interval: 15s\n+      retries: 5\n+\n   postgres:\n     image: postgres:16.4-alpine\n     restart: unless-stopped\n     volumes:\n       - pgdata:/var/lib/postgresql/data\n \n volumes:\n   pgdata:\n",
    "clean": false,
    "bugs": [
      {
        "file": "docker-compose.yml",
        "kind": "missing",
        "tag": "container-data-not-persisted",
        "explain": "У minio нет тома: каталог /data живёт в слое контейнера. У постгреса рядом том есть — и это делает пропуск незаметным, глаз видит volumes ниже и успокаивается. Нужен свой именованный том и запись о нём в volumes.",
        "consequence": "Всё работает ровно до первого docker compose up с новым образом: контейнер пересоздаётся, и снимки за всё время исчезают. Восстановить нечего — бэкапят базу, а файлы никто не бэкапил, потому что все были уверены, что они на томе.",
        "line": 15
      }
    ],
    "decoys": [
      {
        "why": "версия прибита намертво — именно так и надо",
        "line": 13
      },
      {
        "why": "пароль из переменной окружения и падение без неё — правильно",
        "line": 18
      },
      {
        "why": "http внутри сети композа — нормально, наружу это не торчит",
        "line": 7
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "go-context-leak-001",
    "stack": "go",
    "difficulty": 3,
    "title": "Таймаут на поход в биллинг",
    "prompt": "Ограничь поход в биллинг двумя секундами, а то ручка висит, пока он думает",
    "tests": "ok  \tapi/internal/billing\t0.212s\n=== RUN   TestCharge_TimesOut\n--- PASS: TestCharge_TimesOut (0.20s)\nPASS",
    "diff": "--- a/internal/billing/client.go\n+++ b/internal/billing/client.go\n@@ -18,14 +18,27 @@ type Client struct {\n \thttp *http.Client\n }\n \n-func (c *Client) Charge(ctx context.Context, id string, kop int64) error {\n-\treq, err := http.NewRequestWithContext(ctx, http.MethodPost, c.url(id), body(kop))\n+const chargeTimeout = 2 * time.Second\n+\n+func (c *Client) Charge(ctx context.Context, id string, kop int64) error {\n+\tctx, cancel := context.WithTimeout(ctx, chargeTimeout)\n+\n+\treq, err := http.NewRequestWithContext(ctx, http.MethodPost, c.url(id), body(kop))\n \tif err != nil {\n \t\treturn fmt.Errorf(\"build request: %w\", err)\n \t}\n \n \tresp, err := c.http.Do(req)\n \tif err != nil {\n \t\treturn fmt.Errorf(\"charge %s: %w\", id, err)\n \t}\n \tdefer resp.Body.Close()\n \n+\tif resp.StatusCode != http.StatusOK {\n+\t\treturn fmt.Errorf(\"charge %s: status %d\", id, resp.StatusCode)\n+\t}\n+\n+\tcancel()\n \treturn nil\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "internal/billing/client.go",
        "kind": "missing",
        "tag": "context-leak",
        "explain": "cancel вызывается только на счастливом пути. Любой ранний return — и таймер контекста остаётся жить до срабатывания. Здесь нужен defer cancel().",
        "consequence": "Каждый неуспешный платёж оставляет в памяти таймер и горутину до двух секунд. На спокойном трафике незаметно, в пике биллинга — рост памяти и профиль, забитый context.WithTimeout, ровно тогда, когда разбираться некогда.",
        "line": 24
      }
    ],
    "decoys": [
      {
        "why": "Тело ответа при ошибочном статусе не читается — соединение переиспользуется хуже, но это не утечка",
        "line": 38
      },
      {
        "why": "Таймаут прибит константой вместо настройки — к этому справедливо придраться, но просили именно две секунды",
        "line": 21
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "go-error-ignored-001",
    "stack": "go",
    "difficulty": 1,
    "title": "Конфиг из файла",
    "prompt": "Пусть сервис читает настройки из config.json, а если файла нет — работает на дефолтах",
    "tests": "ok  \tapi/internal/config\t0.008s\nok  \tapi/internal/server\t0.114s",
    "diff": "--- a/internal/config/config.go\n+++ b/internal/config/config.go\n@@ -12,10 +12,24 @@ type Config struct {\n \tAddr    string\n \tTimeout time.Duration\n }\n \n-func Load() Config {\n-\treturn defaults\n+var defaults = Config{Addr: \":8080\", Timeout: 5 * time.Second}\n+\n+func Load(path string) Config {\n+\tcfg := defaults\n+\n+\tdata, err := os.ReadFile(path)\n+\tif err != nil {\n+\t\treturn cfg\n+\t}\n+\n+\tjson.Unmarshal(data, &cfg)\n+\n+\treturn cfg\n }\n \n func (c Config) String() string {\n \treturn fmt.Sprintf(\"%s (timeout %s)\", c.Addr, c.Timeout)\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "internal/config/config.go",
        "kind": "wrong",
        "tag": "error-ignored",
        "explain": "Ошибка разбора не проверяется, а Unmarshal при этом успевает записать часть полей.",
        "consequence": "Опечатка в config.json — и сервис поднимается с наполовину заполненными настройками: адрес из файла, таймаут дефолтный. Никакой строчки в логе, разбираться будут по симптомам через неделю.",
        "line": 26
      }
    ],
    "decoys": [
      {
        "why": "Отсутствующий файл молча даёт дефолты — спорно, но это ровно то, что просили в задаче",
        "line": 23
      },
      {
        "why": "Пакетная переменная вместо функции — её можно перезаписать из тестов, но на поведение это не влияет",
        "line": 16
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "go-nil-map-write-001",
    "stack": "go",
    "difficulty": 1,
    "title": "Метки для метрик",
    "prompt": "Собери метки для метрики визита: клиника обязательно, врач если известен",
    "tests": "ok  \tapi/internal/metrics\t0.004s\n=== RUN   TestLabels_WithDoctor\n--- PASS: TestLabels_WithDoctor (0.00s)\nPASS",
    "diff": "--- a/internal/metrics/labels.go\n+++ b/internal/metrics/labels.go\n@@ -5,14 +5,26 @@ package metrics\n \n type Visit struct {\n \tClinicID string\n \tDoctorID string\n }\n \n-func Labels(v Visit) map[string]string {\n-\treturn map[string]string{\"clinic\": v.ClinicID}\n+func Labels(v Visit) map[string]string {\n+\tvar labels map[string]string\n+\n+\tif v.DoctorID != \"\" {\n+\t\tlabels = map[string]string{\"doctor\": v.DoctorID}\n+\t}\n+\n+\tlabels[\"clinic\"] = v.ClinicID\n+\n+\treturn labels\n }\n \n func Count(labels map[string]string) int {\n \treturn len(labels)\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "internal/metrics/labels.go",
        "kind": "wrong",
        "tag": "nil-map-write",
        "explain": "Если врач неизвестен, мапа так и осталась nil, а запись в nil-мапу — это паника, а не пустая мапа.",
        "consequence": "Любой визит без врача — например, забор анализов — роняет обработчик метрик, а вместе с ним и запрос. Тест кормит функцию визитом с врачом и до паники не доходит.",
        "line": 18
      }
    ],
    "decoys": [
      {
        "why": "Мапа создаётся внутри условия — некрасиво, но само по себе законно",
        "line": 15
      },
      {
        "why": "len от nil-мапы работает и вернёт ноль — тут как раз всё в порядке",
        "line": 24
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "go-slice-aliasing-001",
    "stack": "go",
    "difficulty": 2,
    "title": "Первая страница отчёта",
    "prompt": "Разбей отчёт на первую страницу и остаток, в конец первой добавь строку «итого»",
    "tests": "ok  \tapi/internal/report\t0.006s\n=== RUN   TestSplit_FirstPageHasTotal\n--- PASS: TestSplit_FirstPageHasTotal (0.00s)\nPASS",
    "diff": "--- a/internal/report/split.go\n+++ b/internal/report/split.go\n@@ -8,12 +8,26 @@ type Row struct {\n \tAmount int\n }\n \n-func Split(rows []Row) []Row {\n-\treturn rows\n+const pageSize = 10\n+\n+func Split(rows []Row) ([]Row, []Row) {\n+\tsort.Slice(rows, func(i, j int) bool {\n+\t\treturn rows[i].At.Before(rows[j].At)\n+\t})\n+\n+\tif len(rows) <= pageSize {\n+\t\treturn rows, nil\n+\t}\n+\n+\thead := rows[:pageSize]\n+\ttail := rows[pageSize:]\n+\n+\thead = append(head, Row{Kind: \"total\", Amount: sum(rows)})\n+\n+\treturn head, tail\n }\n \n func sum(rows []Row) int {\n \ttotal := 0\n \tfor _, r := range rows {\n \t\ttotal += r.Amount\n \t}\n \treturn total\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "internal/report/split.go",
        "kind": "wrong",
        "tag": "slice-shared-backing",
        "explain": "head и tail смотрят в один массив. append пишет «итого» ровно в ту ячейку, где лежит первая строка tail, и затирает её.",
        "consequence": "Одиннадцатая строка отчёта исчезает и заменяется итогом — на второй странице её нет, в сумме она есть. Тест на первую страницу зелёный, потому что смотрит только на неё.",
        "line": 25
      }
    ],
    "decoys": [
      {
        "why": "sort.Slice сортирует переданный слайс на месте — вопрос к API функции, но не причина пропажи строки",
        "line": 15
      },
      {
        "why": "Ранний выход возвращает nil вторым значением — вызывающему придётся помнить про nil, но это законно",
        "line": 18
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "go-wrapped-error-compare-001",
    "stack": "go",
    "difficulty": 4,
    "title": "Повтор при нехватке средств",
    "prompt": "Не повторяй платёж, если денег не хватило — только на сетевых ошибках",
    "tests": "ok  \tapi/internal/billing\t0.104s\n=== RUN   TestRetry_StopsOnInsufficient\n--- PASS: TestRetry_StopsOnInsufficient (0.00s)\n=== RUN   TestRetry_RetriesOnTimeout\n--- PASS: TestRetry_RetriesOnTimeout (0.10s)\nPASS",
    "diff": "--- a/internal/billing/retry.go\n+++ b/internal/billing/retry.go\n@@ -6,16 +6,30 @@ import (\n \t\"time\"\n )\n \n var ErrInsufficientFunds = errors.New(\"insufficient funds\")\n \n-func WithRetry(fn func() error) error {\n-\treturn fn()\n+const attempts = 3\n+\n+func WithRetry(fn func() error) error {\n+\tvar err error\n+\n+\tfor i := 0; i < attempts; i++ {\n+\t\terr = fn()\n+\t\tif err == nil {\n+\t\t\treturn nil\n+\t\t}\n+\n+\t\tif err == ErrInsufficientFunds {\n+\t\t\treturn err\n+\t\t}\n+\n+\t\ttime.Sleep(time.Duration(i+1) * 100 * time.Millisecond)\n+\t}\n+\n+\treturn fmt.Errorf(\"after %d attempts: %w\", attempts, err)\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "internal/billing/retry.go",
        "kind": "wrong",
        "tag": "wrapped-error-compare",
        "explain": "Charge оборачивает ошибку через %w, а сравнение идёт по ==. Обёрнутая ошибка никогда не равна исходной — нужен errors.Is.",
        "consequence": "Отказ «недостаточно средств» считается временным и повторяется трижды. Клиенту уходят три уведомления от банка, а в поддержку — обращение про «списали несколько раз». Тест зелёный, потому что возвращает голую ErrInsufficientFunds, а не обёрнутую.",
        "line": 22
      }
    ],
    "decoys": [
      {
        "why": "Пауза без джиттера — на массовом сбое все клиенты пойдут одной волной, но повтор при этом работает",
        "line": 26
      },
      {
        "why": "Сюда попадает только последняя ошибка, предыдущие теряются — обидно при разборе, но не баг логики",
        "line": 29
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "java-bigdecimal-scale-001",
    "stack": "java",
    "difficulty": 4,
    "title": "Сверка оплаты с чеком",
    "prompt": "Сверяй пришедшую оплату с суммой чека и помечай чек оплаченным, если сходится",
    "tests": "Tests run: 9, Failures: 0, Errors: 0, Skipped: 0\n[INFO] BUILD SUCCESS",
    "diff": "--- a/src/main/java/clinic/billing/PaymentMatcher.java\n+++ b/src/main/java/clinic/billing/PaymentMatcher.java\n@@ -12,15 +12,30 @@ public class PaymentMatcher {\n \n     private final ReceiptRepository receipts;\n \n-    public boolean match(Payment payment) {\n-        return false;\n+    public boolean match(Payment payment) {\n+        Receipt receipt = receipts.byId(payment.receiptId());\n+\n+        if (receipt == null || receipt.isPaid()) {\n+            return false;\n+        }\n+\n+        BigDecimal expected = receipt.total();\n+        BigDecimal actual = payment.amount();\n+\n+        if (!expected.equals(actual)) {\n+            return false;\n+        }\n+\n+        receipts.markPaid(receipt.id(), payment.id());\n+\n+        return true;\n     }\n \n     public List<Receipt> unpaid() {\n         return receipts.unpaid();\n     }\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/main/java/clinic/billing/PaymentMatcher.java",
        "kind": "wrong",
        "tag": "bigdecimal-scale",
        "explain": "equals у BigDecimal учитывает масштаб: 1200.00 и 1200.0 равными не считаются. Сравнивать суммы нужно через compareTo.",
        "consequence": "Банк присылает суммы с двумя знаками, а часть чеков хранит их с одним — такие платежи молча не находят свой чек. Клиент оплатил, в системе долг, и звонит он не в поддержку, а в клинику на ресепшн.",
        "line": 25
      }
    ],
    "decoys": [
      {
        "why": "Повторная оплата тихо отбрасывается вместо возврата — вопрос к процессу, но не к сравнению сумм",
        "line": 18
      },
      {
        "why": "Пометка и проверка идут двумя обращениями без блокировки — гонка возможна, но она не про несовпадение сумм",
        "line": 29
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "java-error-swallowed-001",
    "stack": "java",
    "difficulty": 2,
    "title": "Отправка чеков в очередь",
    "prompt": "Складывай чеки в очередь пачкой, один плохой чек не должен рушить всю пачку",
    "tests": "Tests run: 8, Failures: 0, Errors: 0, Skipped: 0\n[INFO] BUILD SUCCESS",
    "diff": "--- a/src/main/java/clinic/billing/ReceiptPublisher.java\n+++ b/src/main/java/clinic/billing/ReceiptPublisher.java\n@@ -14,14 +14,29 @@ public class ReceiptPublisher {\n \n     private static final Logger log = LoggerFactory.getLogger(ReceiptPublisher.class);\n \n-    public int publish(List<Receipt> receipts) {\n-        return 0;\n+    public int publish(List<Receipt> receipts) {\n+        int sent = 0;\n+\n+        for (Receipt receipt : receipts) {\n+            try {\n+                queue.send(topic, receipt.toJson());\n+            } catch (Exception e) {\n+                log.warn(\"receipt {} skipped\", receipt.id());\n+            }\n+\n+            sent++;\n+        }\n+\n+        log.info(\"published {} of {}\", sent, receipts.size());\n+\n+        return sent;\n     }\n \n     public boolean isHealthy() {\n         return queue.isConnected();\n     }\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/main/java/clinic/billing/ReceiptPublisher.java",
        "kind": "wrong",
        "tag": "error-swallowed",
        "explain": "Счётчик увеличивается и после исключения — он стоит вне try, а не в конце успешной ветки.",
        "consequence": "Метод отчитывается «отправлено 500», в очереди 480. Расхождение всплывает через месяц при сверке с бухгалтерией, и восстанавливать придётся по логам уровня warn, которые к тому времени уже ротировались.",
        "line": 27
      }
    ],
    "decoys": [
      {
        "why": "Ловится вся Exception сразу — широковато, и стек в лог не пишется, но не из-за этого сбивается счёт",
        "line": 23
      },
      {
        "why": "Лог на каждую пачку без идентификатора пачки — искать потом неудобно",
        "line": 30
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "java-integer-division-001",
    "stack": "java",
    "difficulty": 1,
    "title": "Загрузка кабинетов за день",
    "prompt": "Посчитай процент загрузки кабинета: занятые минуты от рабочих",
    "tests": "Tests run: 5, Failures: 0, Errors: 0, Skipped: 0\n[INFO] BUILD SUCCESS",
    "diff": "--- a/src/main/java/clinic/stats/RoomLoad.java\n+++ b/src/main/java/clinic/stats/RoomLoad.java\n@@ -7,14 +7,27 @@ public class RoomLoad {\n \n     private static final int WORKING_MINUTES = 600;\n \n-    public double percent(int busyMinutes) {\n-        return 0.0;\n+    public double percent(int busyMinutes) {\n+        if (busyMinutes <= 0) {\n+            return 0.0;\n+        }\n+\n+        return (busyMinutes / WORKING_MINUTES) * 100.0;\n+    }\n+\n+    public String label(int busyMinutes) {\n+        return Math.round(percent(busyMinutes)) + \"%\";\n     }\n \n     public boolean isOverbooked(int busyMinutes) {\n         return busyMinutes > WORKING_MINUTES;\n     }\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/main/java/clinic/stats/RoomLoad.java",
        "kind": "wrong",
        "tag": "integer-division",
        "explain": "Оба числа целые, поэтому деление тоже целое: всё, что меньше рабочего дня, превращается в ноль ещё до умножения на сто.",
        "consequence": "Загрузка любого кабинета показывается как 0% — кроме полностью занятого, у которого выйдет ровно 100%. Отчёт выглядит правдоподобно и им пользуются, планируя закупку оборудования.",
        "line": 15
      }
    ],
    "decoys": [
      {
        "why": "Округление до целых процентов теряет десятые — так и задумано для подписи",
        "line": 19
      },
      {
        "why": "Ровно рабочий день не считается переработкой — граничный случай, но осознанный",
        "line": 23
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "java-reference-equality-001",
    "stack": "java",
    "difficulty": 1,
    "title": "Роль из токена",
    "prompt": "Проверяй по токену, что роль врачебная, иначе не пускай в карточку",
    "tests": "Tests run: 6, Failures: 0, Errors: 0, Skipped: 0\n[INFO] BUILD SUCCESS",
    "diff": "--- a/src/main/java/clinic/auth/RoleGuard.java\n+++ b/src/main/java/clinic/auth/RoleGuard.java\n@@ -10,13 +10,26 @@ public class RoleGuard {\n \n     private static final String VET = \"vet\";\n \n-    public boolean canOpenCard(Token token) {\n-        return token != null;\n+    public boolean canOpenCard(Token token) {\n+        if (token == null || token.isExpired()) {\n+            return false;\n+        }\n+\n+        String role = token.claim(\"role\");\n+\n+        return role == VET;\n+    }\n+\n+    public boolean canEditCard(Token token) {\n+        return canOpenCard(token) && token.claim(\"scope\").contains(\"write\");\n     }\n \n     public String describe(Token token) {\n         return token == null ? \"anonymous\" : token.claim(\"role\");\n     }\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/main/java/clinic/auth/RoleGuard.java",
        "kind": "wrong",
        "tag": "reference-equality",
        "explain": "Строки сравниваются по ссылке. Литерал из константы лежит в пуле, а роль из токена собрана в рантайме — это разные объекты.",
        "consequence": "Врач получает отказ в доступе к карточке, а вот сравнение с пустой ролью иногда срабатывает — поведение зависит от того, попала строка в пул или нет. Тест зелёный, потому что кладёт в токен ту же литеральную константу.",
        "line": 20
      }
    ],
    "decoys": [
      {
        "why": "Проверка scope через contains — «write» найдётся и внутри «no-write», но роль ломается не здесь",
        "line": 24
      },
      {
        "why": "Метод для логов может вернуть null, если claim отсутствует — неприятно, но к доступу отношения не имеет",
        "line": 28
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "java-shared-dateformat-001",
    "stack": "java",
    "difficulty": 3,
    "title": "Дата приёма в выгрузке",
    "prompt": "В выгрузке для страховой дата должна быть в формате dd.MM.yyyy, вынеси форматирование в одно место",
    "tests": "Tests run: 12, Failures: 0, Errors: 0, Skipped: 0\n[INFO] BUILD SUCCESS",
    "diff": "--- a/src/main/java/clinic/export/VisitRow.java\n+++ b/src/main/java/clinic/export/VisitRow.java\n@@ -9,15 +9,29 @@ public class VisitRow {\n \n     private final Visit visit;\n \n-    public String date() {\n-        return visit.getAt().toString();\n+    private static final SimpleDateFormat DATE = new SimpleDateFormat(\"dd.MM.yyyy\");\n+\n+    public String date() {\n+        return DATE.format(visit.getAt());\n+    }\n+\n+    public String amount() {\n+        return String.format(\"%.2f\", visit.getKop() / 100.0);\n     }\n \n     public String toCsv() {\n-        return String.join(\";\", visit.getId(), date());\n+        return String.join(\";\", visit.getId(), date(), amount());\n     }\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/main/java/clinic/export/VisitRow.java",
        "kind": "wrong",
        "tag": "not-thread-safe-shared",
        "explain": "SimpleDateFormat хранит состояние разбора внутри себя и потокобезопасным не является, а лежит он в статике — один на все потоки.",
        "consequence": "Выгрузку собирают в несколько потоков, и раз в сотню строк дата приезжает от соседней записи или вовсе мусором вроде 31.12.0002. Страховая отказывает по всей выгрузке, а воспроизвести на одном потоке невозможно.",
        "line": 12
      }
    ],
    "decoys": [
      {
        "why": "Сумма форматируется без явной локали — на машине с запятой в разделителе csv поедет, но это другая проблема",
        "line": 19
      },
      {
        "why": "Точка с запятой без экранирования полей — сломается, если в поле окажется разделитель",
        "line": 23
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "js-alert-swallow-001",
    "stack": "js",
    "difficulty": 1,
    "title": "Сохранение карточки",
    "prompt": "Показывай пользователю, если карточка не сохранилась",
    "tests": "PASS  src/PatientForm.test.tsx (5)\n  ✓ сохраняет карточку\n  ✓ показывает ошибку\n\nTests  5 passed (5)",
    "diff": "--- a/src/PatientForm.tsx\n+++ b/src/PatientForm.tsx\n@@ -1,20 +1,36 @@\n import { useState } from 'react'\n \n import { api } from './api'\n import type { PatientDraft } from './types'\n \n export function PatientForm({ initial }: { initial: PatientDraft }) {\n   const [values, setValues] = useState(initial)\n   const [saved, setSaved] = useState(false)\n+  const [busy, setBusy] = useState(false)\n \n   async function onSubmit(values: PatientDraft) {\n-    await api.savePatient(values)\n+    setBusy(true)\n+\n+    try {\n+      await api.savePatient(values)\n+      setSaved(true)\n+    } catch {\n+      alert('Не удалось сохранить')\n+    } finally {\n+      setBusy(false)\n+    }\n   }\n \n-  return <Form values={values} onChange={setValues} onSubmit={onSubmit} />\n+  return (\n+    <Form values={values} onChange={setValues} onSubmit={onSubmit} disabled={busy} />\n+  )\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/PatientForm.tsx",
        "kind": "wrong",
        "tag": "error-shown-but-lost",
        "explain": "Исключение поймано, показано человеку и выброшено. Ни причины, ни стека, ни записи в мониторинге не осталось.",
        "consequence": "Врач видит «не удалось сохранить» и звонит в поддержку, а в Sentry по этому времени пусто. Воспроизвести нечем: неизвестно даже, была это валидация, сеть или пятисотка.",
        "line": 18
      }
    ],
    "decoys": [
      {
        "why": "флаг успеха внутри try — так и правильно, до него дело дойдёт только без исключения",
        "line": 16
      },
      {
        "why": "сброс флага в finally — как раз правильно, иначе форма зависла бы после ошибки",
        "line": 19
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "js-cache-invalidation-001",
    "stack": "js",
    "difficulty": 2,
    "title": "Кэш пользователей",
    "prompt": "Добавь кэш в getUser, а то мы дёргаем базу на каждый чих",
    "tests": "PASS  src/user-cache.test.ts (3)\n  ✓ возвращает пользователя\n  ✓ второй вызов не ходит в базу\n  ✓ updateUser сохраняет изменения\n\nTests  3 passed (3)\nBuild  ✓ built in 1.24s",
    "diff": "--- a/src/user-cache.ts\n+++ b/src/user-cache.ts\n@@ -1,8 +1,17 @@\n import { db } from './db'\n import type { User } from './types'\n \n+const cache = new Map<number, User>()\n+\n export async function getUser(id: number): Promise<User> {\n-  return db.users.findById(id)\n+  const hit = cache.get(id)\n+  if (hit) return hit\n+\n+  const user = await db.users.findById(id)\n+  cache.set(id, user)\n+  return user\n }\n+\n+export async function updateUser(id: number, patch: Partial<User>) {\n+  await db.users.update(id, patch)\n+}\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/user-cache.ts",
        "kind": "missing",
        "tag": "cache-no-invalidation",
        "explain": "updateUser пишет в базу, но не трогает кэш — записи в нём живут вечно.",
        "consequence": "Пользователь меняет имя и продолжает видеть старое. На локалке процесс живёт минуту и всё «работает»; в проде инстанс живёт неделями. Классический баг, который воспроизводится только у клиента.",
        "line": 16
      }
    ],
    "decoys": [
      {
        "why": "Map без ограничения размера — вопрос к памяти, но не баг этой задачи",
        "line": 4
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "js-catch-null-001",
    "stack": "js",
    "difficulty": 2,
    "title": "Загрузка карточки пациента",
    "prompt": "Пусть страница не падает, если карточка не загрузилась",
    "tests": "PASS  src/patient.test.ts (3)\n  ✓ отдаёт карточку\n  ✓ не бросает при ошибке сети\n\nTests  3 passed (3)",
    "diff": "--- a/src/patient.ts\n+++ b/src/patient.ts\n@@ -1,16 +1,31 @@\n import { api } from './api'\n import type { Patient } from './types'\n \n const EMPTY_HISTORY: Visit[] = []\n \n export async function loadPatient(id: number): Promise<Patient> {\n-  return api.get(`/patients/${id}`)\n+  const patient = await api.get(`/patients/${id}`).catch(() => null)\n+\n+  return patient ?? { id, name: 'Без имени', visits: EMPTY_HISTORY }\n }\n \n+export async function loadPatientPage(id: number) {\n+  const patient = await loadPatient(id)\n+\n+  return {\n+    patient,\n+    canPrescribe: patient.visits.length > 0,\n+  }\n+}\n+\n export function displayName(patient: Patient): string {\n   return patient.name.trim() || 'Без имени'\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/patient.ts",
        "kind": "wrong",
        "tag": "error-swallowed",
        "explain": "Отвалившаяся сеть и отсутствующий пациент теперь неразличимы: и то и другое превращается в пустую карточку.",
        "consequence": "Врач при упавшем бэкенде видит карточку с пустой историей визитов и делает назначение вслепую. Ошибки в мониторинге нет, потому что никто не бросил исключение.",
        "line": 7
      }
    ],
    "decoys": [
      {
        "why": "выдуманная заглушка вместо ошибки — следствие, а не причина",
        "line": 9
      },
      {
        "why": "право выписать назначение выводится из истории — спорная логика, но она следствие, а не причина",
        "line": 17
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "js-clean-abort-001",
    "stack": "js",
    "difficulty": 5,
    "title": "Отмена устаревшего запроса",
    "prompt": "Отменяй предыдущий запрос, когда пользователь печатает дальше",
    "tests": "PASS  src/search.test.ts (7)\n  ✓ отдаёт результат последнего запроса\n  ✓ отменённый запрос не пишет в состояние\n  ✓ не глотает настоящую ошибку\n\nTests  7 passed (7)",
    "diff": "--- a/src/search.ts\n+++ b/src/search.ts\n@@ -3,10 +3,20 @@\n-export async function search(query: string) {\n-  const res = await fetch(`/api/search?q=${encodeURIComponent(query)}`)\n-  return res.json()\n+let inFlight: AbortController | undefined\n+\n+export async function search(query: string) {\n+  inFlight?.abort()\n+\n+  const controller = new AbortController()\n+  inFlight = controller\n+\n+  try {\n+    const res = await fetch(`/api/search?q=${encodeURIComponent(query)}`, {\n+      signal: controller.signal,\n+    })\n+    return await res.json()\n+  } finally {\n+    if (inFlight === controller) inFlight = undefined\n+  }\n }\n",
    "clean": true,
    "bugs": [],
    "decoys": [
      {
        "why": "модульная переменная выглядит опасно, но именно она и хранит текущий запрос — иначе отменять нечего",
        "line": 3
      },
      {
        "why": "проверка кажется избыточной — она как раз и не даёт затереть уже начатый следующий запрос",
        "line": 17
      },
      {
        "why": "await перед return внутри try выглядит лишним — но без него finally отработает раньше разбора тела",
        "line": 15
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "js-clean-debounce-001",
    "stack": "js",
    "difficulty": 4,
    "title": "Поиск по клиентам",
    "prompt": "Не дёргай поиск на каждую букву, подожди, пока человек допечатает",
    "tests": "PASS  src/debounce.test.ts (5)\n  ✓ вызывает один раз за серию\n  ✓ отдаёт последние аргументы\n  ✓ таймер сбрасывается\n\nTests  5 passed (5)\nBuild  ✓ built in 0.71s",
    "diff": "--- a/src/debounce.ts\n+++ b/src/debounce.ts\n@@ -1,3 +1,14 @@\n+export function debounce<A extends unknown[]>(fn: (...args: A) => void, ms: number) {\n+  let timer: ReturnType<typeof setTimeout> | undefined\n+\n+  return (...args: A) => {\n+    if (timer !== undefined) clearTimeout(timer)\n+\n+    timer = setTimeout(() => {\n+      timer = undefined\n+      fn(...args)\n+    }, ms)\n+  }\n+}\n",
    "clean": true,
    "bugs": [],
    "decoys": [
      {
        "why": "явная проверка выглядит лишней — clearTimeout(undefined) безопасен, но и вреда в ней нет",
        "line": 5
      },
      {
        "why": "замыкание на общий таймер выглядит как гонка, но вызовы в JS однопоточные",
        "line": 2
      },
      {
        "why": "аргументы берутся из последнего вызова — так debounce и должен работать",
        "line": 9
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "js-clean-generator-001",
    "stack": "js",
    "difficulty": 5,
    "title": "Потоковая выгрузка",
    "prompt": "Перепиши выгрузку, чтобы не держала весь список в памяти",
    "tests": "PASS  src/export.test.ts (6)\n  ✓ отдаёт все строки\n  ✓ не падает на пустой таблице\n  ✓ последняя страница неполная\n\nTests  6 passed (6)\nBuild  ✓ built in 0.98s",
    "diff": "--- a/src/export.ts\n+++ b/src/export.ts\n@@ -3,5 +3,13 @@\n-export async function exportAll(): Promise<string> {\n-  const rows = await db.visits.findAll()\n-  return rows.map(toCsvLine).join('\\n')\n+export async function* exportAll(): AsyncGenerator<string> {\n+  let offset = 0\n+  const size = 500\n+\n+  while (true) {\n+    const rows = await db.visits.findPage(offset, size)\n+    if (rows.length === 0) return\n+    for (const row of rows) yield toCsvLine(row)\n+    offset += rows.length\n+  }\n }\n",
    "clean": true,
    "bugs": [],
    "decoys": [
      {
        "why": "смена сигнатуры на генератор ломает вызывающий код — но это видно на сборке, а сборка зелёная",
        "line": 3
      },
      {
        "why": "while (true) выглядит опасно, но выход есть и он корректный",
        "line": 7
      },
      {
        "why": "offset += rows.length, а не += size — выглядит ошибкой, а на деле именно так и правильно для неполной последней страницы",
        "line": 11
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "js-console-left-001",
    "stack": "js",
    "difficulty": 1,
    "title": "Отладка выгрузки",
    "prompt": "Разберись, почему выгрузка иногда пустая, и почини",
    "tests": "PASS  src/export.test.ts (4)\n  ✓ выгружает строки\n  ✓ пустой список не падает\n\nTests  4 passed (4)",
    "diff": "--- a/src/export.ts\n+++ b/src/export.ts\n@@ -1,16 +1,32 @@\n import type { Row } from './types'\n \n function toLine(row: Row): string {\n   return [row.date, row.patient, row.service, row.total].join(';')\n }\n \n+const HEADER = ['Дата', 'Пациент', 'Услуга', 'Сумма'].join(';')\n+\n export function toCsv(rows: Row[]): string {\n-  return rows.map(toLine).join('\\n')\n+  console.log('rows', rows)\n+\n+  if (!rows.length) return ''\n+\n+  return [HEADER, ...rows.map(toLine)].join('\\n')\n }\n+\n+export function download(rows: Row[], filename: string) {\n+  const blob = new Blob([toCsv(rows)], { type: 'text/csv;charset=utf-8' })\n+  const url = URL.createObjectURL(blob)\n+\n+  Object.assign(document.createElement('a'), { href: url, download: filename }).click()\n+  URL.revokeObjectURL(url)\n+}\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/export.ts",
        "kind": "wrong",
        "tag": "debug-left-in-code",
        "explain": "Отладочный вывод остался в коде и печатает весь массив на каждый вызов.",
        "consequence": "На выгрузке в двадцать тысяч строк консоль браузера забивается целиком, вкладка подвисает на несколько секунд, а в логах сервера, если это уедет на бэкенд, окажутся персональные данные владельцев.",
        "line": 10
      }
    ],
    "decoys": [
      {
        "why": "ранний выход на пустом списке — как раз то, о чём просили",
        "line": 12
      },
      {
        "why": "ссылка не добавлена в документ — в старых браузерах не сработает, но это не про выгрузку",
        "line": 21
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "js-effect-cleanup-001",
    "stack": "js",
    "difficulty": 3,
    "title": "Автообновление очереди",
    "prompt": "Пусть очередь на приём сама обновляется раз в 5 секунд",
    "tests": "PASS  src/Queue.test.tsx (3)\n  ✓ рисует очередь\n  ✓ обновляет по таймеру\n\nTests  3 passed (3)",
    "diff": "--- a/src/Queue.tsx\n+++ b/src/Queue.tsx\n@@ -6,6 +6,12 @@\n export function Queue({ clinicId }: { clinicId: number }) {\n   const [items, setItems] = useState<QueueItem[]>([])\n+\n+  useEffect(() => {\n+    const id = setInterval(() => {\n+      fetchQueue(clinicId).then(setItems)\n+    }, 5000)\n+  }, [clinicId])\n \n   return <QueueTable items={items} />\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/Queue.tsx",
        "kind": "missing",
        "tag": "effect-without-cleanup",
        "explain": "Эффект заводит интервал и не возвращает функцию очистки. Каждая смена clinicId добавляет ещё один живой таймер.",
        "consequence": "Регистратор за смену переключает клиники десяток раз — к обеду в браузере десять таймеров долбят бэкенд каждые пять секунд и наперегонки перезаписывают список. Выглядит как «очередь скачет».",
        "line": 13
      }
    ],
    "decoys": [
      {
        "why": "результат запроса не отменяется при размонтировании — тоже проблема, но вторичная",
        "line": 11
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "js-floating-promise-001",
    "stack": "js",
    "difficulty": 2,
    "title": "Уведомление о визите",
    "prompt": "Отправляй уведомление владельцу, когда визит закрыли",
    "tests": "PASS  src/visit.test.ts (4)\n  ✓ закрывает визит\n  ✓ шлёт уведомление\n\nTests  4 passed (4)\nBuild  ✓ built in 0.87s",
    "diff": "--- a/src/visit.ts\n+++ b/src/visit.ts\n@@ -1,18 +1,34 @@\n import { audit } from './audit'\n import { db } from './db'\n import { notify } from './notify'\n \n export async function closeVisit(id: number) {\n   const visit = await db.visits.close(id)\n+\n+  notify(visit.ownerId, `Визит ${visit.id} закрыт`)\n+\n   await audit.record('visit.closed', visit.id)\n+\n   return visit\n }\n+\n+export async function closeAll(ids: number[]) {\n+  const closed = []\n+\n+  for (const id of ids) {\n+    closed.push(await closeVisit(id))\n+  }\n+\n+  return closed\n+}\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/visit.ts",
        "kind": "wrong",
        "tag": "floating-promise",
        "explain": "notify возвращает промис, но его никто не ждёт и ошибку из него никто не поймает.",
        "consequence": "Упавшее уведомление становится unhandled rejection: владелец ничего не получил, в логах пусто, а под Node с strict-режимом это ещё и валит процесс целиком.",
        "line": 8
      }
    ],
    "decoys": [
      {
        "why": "аудит после уведомления — порядок спорный, но корректности не ломает",
        "line": 10
      },
      {
        "why": "последовательное закрытие вместо параллельного — медленно, зато предсказуемо",
        "line": 19
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "js-index-key-001",
    "stack": "js",
    "difficulty": 4,
    "title": "Редактируемый список назначений",
    "prompt": "Дай удалять строки назначений прямо в списке",
    "tests": "PASS  src/Prescriptions.test.tsx (5)\n  ✓ рисует список\n  ✓ удаляет строку\n\nTests  5 passed (5)",
    "diff": "--- a/src/Prescriptions.tsx\n+++ b/src/Prescriptions.tsx\n@@ -1,18 +1,35 @@\n import type { Prescription } from './types'\n \n export function Prescriptions({ items, onChange }: Props) {\n+  function remove(index: number) {\n+    onChange(items.filter((_, i) => i !== index))\n+  }\n+\n+  if (items.length === 0) {\n+    return <p className=\"empty\">Назначений пока нет</p>\n+  }\n+\n   return (\n     <ul>\n-      {items.map((item) => <Row key={item.id} item={item} />)}\n+      {items.map((item, i) => (\n+        <Row key={i} item={item} onRemove={() => remove(i)} />\n+      ))}\n     </ul>\n   )\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/Prescriptions.tsx",
        "kind": "wrong",
        "tag": "index-as-key",
        "explain": "Ключом стал индекс вместо item.id. При удалении строки React переиспользует узлы не по назначению, и внутреннее состояние строк съезжает на соседние.",
        "consequence": "Врач удаляет второе назначение — введённая в третьей строке дозировка перескакивает во вторую. Данные в state правильные, на экране нет, и заметно это только если смотреть внимательно.",
        "line": 15
      }
    ],
    "decoys": [
      {
        "why": "передавать индекс в обработчик удаления нормально, если список не переупорядочивают",
        "line": 14
      },
      {
        "why": "удаление через filter не мутирует исходный массив — здесь всё честно",
        "line": 5
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "js-json-clone-001",
    "stack": "js",
    "difficulty": 3,
    "title": "Копия черновика назначения",
    "prompt": "Сделай копию черновика, чтобы правки не меняли оригинал",
    "tests": "PASS  src/draft.test.ts (4)\n  ✓ копия независима\n  ✓ вложенные поля копируются\n\nTests  4 passed (4)",
    "diff": "--- a/src/draft.ts\n+++ b/src/draft.ts\n@@ -1,12 +1,26 @@\n import { nanoid } from 'nanoid'\n import type { Prescription } from './types'\n \n export function createDraft(visitId: number): Prescription {\n   return {\n     id: nanoid(),\n     visitId,\n     items: [],\n     createdAt: new Date(),\n   }\n }\n+\n+export function cloneDraft(draft: Prescription): Prescription {\n+  return JSON.parse(JSON.stringify(draft))\n+}\n+\n+export function duplicateForVisit(draft: Prescription, visitId: number): Prescription {\n+  const copy = cloneDraft(draft)\n+\n+  copy.id = nanoid()\n+  copy.visitId = visitId\n+  copy.createdAt = new Date()\n+\n+  return copy\n+}\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/draft.ts",
        "kind": "wrong",
        "tag": "json-clone-loses-types",
        "explain": "Такое копирование превращает Date в строку, теряет undefined и Map, а на циклической ссылке падает.",
        "consequence": "Дата назначения после копирования становится строкой, и первый же вызов .getTime() падает с TypeError — но не в момент копирования, а позже и в другом файле. Искать будут не там.",
        "line": 14
      }
    ],
    "decoys": [
      {
        "why": "тип возвращаемого значения обещает Prescription, хотя внутри any — следствие, а не причина",
        "line": 13
      },
      {
        "why": "мутация только что созданной копии — некрасиво, но безопасно: объект больше нигде не виден",
        "line": 20
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "js-local-date-001",
    "stack": "js",
    "difficulty": 4,
    "title": "Дата визита в отчёте",
    "prompt": "Группируй визиты по дням для дневного отчёта",
    "tests": "PASS  src/report.test.ts (6)\n  ✓ группирует визиты по дню\n  ✓ пустой день не попадает в отчёт\n\nTests  6 passed (6)",
    "diff": "--- a/src/report.ts\n+++ b/src/report.ts\n@@ -1,16 +1,33 @@\n import { groupBy } from './util'\n import type { Visit } from './types'\n \n+function dayOf(visit: Visit): string {\n+  return new Date(visit.startsAt).toISOString().slice(0, 10)\n+}\n+\n export function groupByDay(visits: Visit[]) {\n-  return groupBy(visits, (v) => v.startsAt)\n+  return groupBy(visits, dayOf)\n }\n+\n+export function dailyTotals(visits: Visit[]) {\n+  const byDay = groupByDay(visits)\n+\n+  return Object.entries(byDay)\n+    .map(([day, items]) => ({\n+      day,\n+      count: items.length,\n+      revenue: items.reduce((sum, v) => sum + v.total, 0),\n+    }))\n+    .sort((a, b) => a.day.localeCompare(b.day))\n+}\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/report.ts",
        "kind": "wrong",
        "tag": "utc-vs-local-day",
        "explain": "toISOString отдаёт день по UTC, а клиника живёт по своему часовому поясу.",
        "consequence": "Вечерние визиты в Краснодаре уезжают в отчёт следующего дня, а в Владивостоке — предыдущего. Дневная выручка не сходится ровно на вечерний приём, и никто не понимает почему.",
        "line": 5
      }
    ],
    "decoys": [
      {
        "why": "группировка сама по себе корректна, вопрос в том, что считает ключом",
        "line": 9
      },
      {
        "why": "сортировка строк формата ГГГГ-ММ-ДД лексикографически — работает верно",
        "line": 21
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "js-order-dependency-001",
    "stack": "js",
    "difficulty": 5,
    "title": "Параллельная загрузка карточки",
    "prompt": "Грузи карточку и историю визитов параллельно, чтобы страница открывалась быстрее",
    "tests": "PASS  src/load.test.ts (7)\n  ✓ грузит карточку и историю\n  ✓ отдаёт данные вместе\n  ✓ не падает на пустой истории\n\nTests  7 passed (7)",
    "diff": "--- a/src/load.ts\n+++ b/src/load.ts\n@@ -6,10 +6,16 @@\n export async function loadPatientPage(id: number) {\n-  const patient = await api.getPatient(id)\n-  const visits = await api.getVisits(patient.clinicId)\n+  const [patient, visits] = await Promise.all([\n+    api.getPatient(id),\n+    api.getVisits(currentClinicId()),\n+  ])\n \n   return { patient, visits }\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/load.ts",
        "kind": "wrong",
        "tag": "broken-data-dependency",
        "explain": "Раньше клиника бралась из загруженной карточки, теперь — из текущего контекста. Запросы распараллелили, разорвав зависимость между ними.",
        "consequence": "Пока врач смотрит пациентов своей клиники, разницы нет. Стоит открыть карточку пациента из другого филиала по прямой ссылке — и в историю визитов подтянутся чужие приёмы. Тесты этого не видят, потому что в фикстурах клиника одна.",
        "line": 9
      }
    ],
    "decoys": [
      {
        "why": "Promise.all падает целиком при отказе любого из двух — обсуждаемо, но просили именно параллельно",
        "line": 7
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "js-shallow-merge-001",
    "stack": "js",
    "difficulty": 3,
    "title": "Настройки клиники",
    "prompt": "Сделай функцию, которая накатывает настройки клиники поверх дефолтных",
    "tests": "PASS  src/settings.test.ts (4)\n  ✓ берёт значение из дефолтов\n  ✓ перекрывает значение клиники\n\nTests  4 passed (4)",
    "diff": "--- a/src/settings.ts\n+++ b/src/settings.ts\n@@ -1,16 +1,32 @@\n export interface Settings {\n   theme: 'light' | 'dark'\n   notifications: {\n     email: boolean\n     sms: boolean\n   }\n }\n \n+export const DEFAULTS: Settings = {\n+  theme: 'light',\n+  notifications: { email: true, sms: false },\n+}\n+\n+export function resolveSettings(clinic: Partial<Settings>): Settings {\n+  return { ...DEFAULTS, ...clinic }\n+}\n+\n+export function describe(settings: Settings): string {\n+  const channels = [\n+    settings.notifications.email && 'почта',\n+    settings.notifications.sms && 'смс',\n+  ].filter(Boolean)\n+\n+  return channels.length ? channels.join(', ') : 'уведомления выключены'\n+}\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/settings.ts",
        "kind": "wrong",
        "tag": "shallow-merge",
        "explain": "Спред копирует только верхний уровень. Клиника, задавшая notifications.sms, теряет notifications.email, а не переопределившая notifications получает ссылку на общий объект дефолтов.",
        "consequence": "Одна клиника меняет настройку уведомлений — она меняется у всех, кто не переопределял этот блок, потому что объект в памяти один. Воспроизводится только на проде, где клиник больше одной.",
        "line": 15
      }
    ],
    "decoys": [
      {
        "why": "константа объявлена ниже использования — для const это хойстится и работает, читается плохо, но не ломается",
        "line": 9
      },
      {
        "why": "filter(Boolean) на массиве с false — работает, хотя типизирован так себе",
        "line": 22
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "js-slice-pagination-001",
    "stack": "js",
    "difficulty": 2,
    "title": "Пагинация справочника",
    "prompt": "Разбей справочник пород на страницы по 20 штук",
    "tests": "PASS  src/paginate.test.ts (5)\n  ✓ первая страница\n  ✓ последняя страница неполная\n\nTests  5 passed (5)",
    "diff": "--- a/src/paginate.ts\n+++ b/src/paginate.ts\n@@ -1,10 +1,27 @@\n import type { Breed } from './types'\n \n+const PAGE_SIZE = 20\n+\n+export function page<T>(items: T[], pageNumber: number): T[] {\n+  const from = pageNumber * PAGE_SIZE\n+\n+  return items.slice(from, from + PAGE_SIZE)\n+}\n+\n+export function pageCount(total: number): number {\n+  return Math.max(1, Math.ceil(total / PAGE_SIZE))\n+}\n+\n+export function breedsPage(all: Breed[], pageNumber: number) {\n+  return {\n+    items: page(all, pageNumber),\n+    pages: pageCount(all.length),\n+    current: pageNumber,\n+  }\n+}\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/paginate.ts",
        "kind": "wrong",
        "tag": "off-by-one-pagination",
        "explain": "Страницы в интерфейсе нумеруются с единицы, а смещение считается так, будто с нуля.",
        "consequence": "Первая страница показывает записи с 21-й, а первые двадцать пород не видит никто. Заметят не сразу: список длинный, а начало алфавита никто не проверяет.",
        "line": 6
      }
    ],
    "decoys": [
      {
        "why": "нет валидации отрицательного номера — slice это переживёт, поведение не ломается",
        "line": 5
      },
      {
        "why": "минимум одна страница даже на пустом списке — так интерфейсу удобнее, и это верно",
        "line": 12
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "js-sort-mutates-001",
    "stack": "js",
    "difficulty": 3,
    "title": "Сортировка списка приёмов",
    "prompt": "Покажи приёмы по времени, самые ранние сверху",
    "tests": "PASS  src/AppointmentList.test.tsx (3)\n  ✓ рисует все приёмы\n  ✓ сортирует по времени\n\nTests  3 passed (3)",
    "diff": "--- a/src/AppointmentList.tsx\n+++ b/src/AppointmentList.tsx\n@@ -1,16 +1,31 @@\n import type { Appointment } from './types'\n \n function renderRow(item: Appointment) {\n   return (\n     <li key={item.id}>\n       {formatTime(item.startsAt)} — {item.petName}\n     </li>\n   )\n }\n \n export function AppointmentList({ items }: { items: Appointment[] }) {\n-  return <ul>{items.map(renderRow)}</ul>\n+  const sorted = items.sort((a, b) => a.startsAt - b.startsAt)\n+\n+  if (sorted.length === 0) {\n+    return <p className=\"empty\">На сегодня приёмов нет</p>\n+  }\n+\n+  return <ul>{sorted.map(renderRow)}</ul>\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/AppointmentList.tsx",
        "kind": "wrong",
        "tag": "props-mutated-in-place",
        "explain": "Array.sort сортирует на месте, а items — это пропс. Компонент молча переставляет массив, который ему не принадлежит.",
        "consequence": "Родитель хранит тот же массив в состоянии, его порядок меняется без setState — и соседний список, читающий те же данные, начинает рисовать не то. Ловится днями, потому что зависит от порядка рендера.",
        "line": 12
      }
    ],
    "decoys": [
      {
        "why": "нет key у строк — реальная претензия к React, но не эта подлянка",
        "line": 18
      },
      {
        "why": "пустое состояние — полезное добавление, не имеющее отношения к порядку",
        "line": 15
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "js-state-stale-001",
    "stack": "js",
    "difficulty": 3,
    "title": "Счётчик принятых пациентов",
    "prompt": "Считай, сколько пациентов принято за смену",
    "tests": "PASS  src/Counter.test.tsx (4)\n  ✓ увеличивает счётчик\n  ✓ сбрасывается в начале смены\n\nTests  4 passed (4)",
    "diff": "--- a/src/Counter.tsx\n+++ b/src/Counter.tsx\n@@ -5,8 +5,13 @@\n export function Counter() {\n   const [count, setCount] = useState(0)\n \n+  function onAccepted() {\n+    setCount(count + 1)\n+    logAccepted(count + 1)\n+  }\n+\n   return <button onClick={onAccepted}>Принят: {count}</button>\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/Counter.tsx",
        "kind": "wrong",
        "tag": "stale-state-update",
        "explain": "Обновление опирается на count из замыкания, а не на предыдущее состояние. Два вызова в одном тике дадут +1, а не +2.",
        "consequence": "Регистратор быстро отмечает подряд пришедшую очередь — счётчик отстаёт. Расхождение маленькое и случайное, поэтому его списывают на «показалось», а к концу смены цифра в отчёте не бьётся с числом визитов.",
        "line": 9
      }
    ],
    "decoys": [
      {
        "why": "дублирование выражения выглядит небрежно — но именно здесь значение уже посчитано верно",
        "line": 10
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "js-test-mocks-itself-001",
    "stack": "js",
    "difficulty": 4,
    "title": "Тесты на getUser",
    "prompt": "Покрой getUser тестами, а то он у нас голый",
    "tests": "PASS  src/user-service.test.ts (2)\n  ✓ нормализует имя\n  ✓ подставляет роль по умолчанию\n\nTests  2 passed (2)",
    "diff": "--- a/src/user-service.test.ts\n+++ b/src/user-service.test.ts\n@@ -1,3 +1,17 @@\n import { describe, expect, test, vi } from 'vitest'\n-import { getUser } from './user-service'\n+import { getUser, normalizeName } from './user-service'\n+\n+vi.mock('./api', () => ({\n+  fetchUser: async (id: number) => ({ id, name: 'Боб', role: 'vet' }),\n+}))\n+\n+describe('getUser', () => {\n+  test('нормализует имя', () => {\n+    expect(normalizeName('  боб ')).toBe('Боб')\n+  })\n+\n+  test('подставляет роль по умолчанию', async () => {\n+    const user = await getUser(2)\n+    expect(user.role).toBe('vet')\n+  })\n+})\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/user-service.test.ts",
        "kind": "wrong",
        "tag": "test-asserts-the-mock",
        "explain": "Роль 'vet' положил в ответ сам мок. Тест сверяет мок с самим собой, а подстановку роли по умолчанию не трогает вовсе.",
        "consequence": "Логику дефолтной роли можно удалить целиком — тесты останутся зелёными. Обнаружится в тот день, когда у половины пользователей роль окажется пустой.",
        "line": 15
      }
    ],
    "decoys": [
      {
        "why": "мок объявлен на весь файл и утечёт в соседние тесты — придирка справедливая, но ломает не он",
        "line": 5
      },
      {
        "why": "тест на чистой функции, здесь как раз всё честно",
        "line": 10
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "js-two-cache-writes-002",
    "stack": "js",
    "difficulty": 4,
    "title": "Кэш карточки владельца",
    "prompt": "Закэшируй карточку владельца, а то на приёме её дёргают по десять раз",
    "tests": "PASS  src/owner-card.test.ts (6)\n  ✓ первый вызов идёт в базу\n  ✓ второй вызов берёт из кэша\n  ✓ после переименования отдаёт новое имя\n\nTests  6 passed (6)",
    "diff": "--- a/src/owner-card.ts\n+++ b/src/owner-card.ts\n@@ -1,18 +1,38 @@\n import { db } from './db.ts'\n \n const cache = new Map<number, Owner>()\n \n export async function getOwner(id: number): Promise<Owner> {\n   const hit = cache.get(id)\n   if (hit) return hit\n \n   const owner = await db.owners.byId(id)\n   cache.set(id, owner)\n   return owner\n }\n \n+export async function renameOwner(id: number, fullName: string): Promise<void> {\n+  await db.owners.update(id, { fullName })\n+  cache.set(id, { ...cache.get(id)!, fullName })\n+}\n+\n+export async function attachPhone(id: number, phone: string): Promise<void> {\n+  await db.owners.update(id, { phone })\n+}\n+\n+export async function deleteOwner(id: number): Promise<void> {\n+  await db.owners.remove(id)\n+  cache.delete(id)\n+}\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/owner-card.ts",
        "kind": "wrong",
        "tag": "cache-updated-from-itself",
        "explain": "Кэш обновляется из самого себя, а не из базы: если записи там ещё нет, `cache.get(id)!` вернёт undefined и в кэш ляжет объект из одного поля. Проще и надёжнее — удалить ключ и дать следующему чтению сходить в базу.",
        "consequence": "Владелец, которого переименовали до первого просмотра карточки, начинает отдаваться без телефона и адреса. На приёме это видно как «карточка сломалась», а воспроизвести не выходит: у того, кто проверяет, карточка уже в кэше целиком.",
        "line": 16
      },
      {
        "file": "src/owner-card.ts",
        "kind": "missing",
        "tag": "cache-no-invalidation",
        "explain": "Телефон записан в базу, а кэш не тронут вовсе — рядом, в переименовании, про кэш не забыли. Строкой ниже должен быть сброс ключа.",
        "consequence": "Клиент меняет телефон, регистратура звонит по старому. Живёт это до перезапуска процесса, то есть неделями.",
        "line": 20
      }
    ],
    "decoys": [
      {
        "why": "кэш без ограничения размера — вопрос к памяти, но не та ошибка, которую здесь ищут",
        "line": 3
      },
      {
        "why": "при удалении кэш сброшен — здесь как раз всё правильно",
        "line": 25
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "php-array-merge-reindexes-001",
    "stack": "php",
    "difficulty": 4,
    "title": "Цены по прайсу клиники",
    "prompt": "Наложи прайс конкретной клиники поверх общего, ключ — код услуги",
    "tests": "PHPUnit 11.2.0\n\n........                           8 / 8 (100%)\n\nOK (8 tests, 15 assertions)",
    "diff": "--- a/src/Pricing/PriceList.php\n+++ b/src/Pricing/PriceList.php\n@@ -12,16 +12,30 @@ final class PriceList\n \n     public function __construct(private PriceRepository $prices) {}\n \n-    public function forClinic(int $clinicId): array\n-    {\n-        return $this->prices->common();\n+    /** @return array<int|string, int> код услуги => цена в копейках */\n+    public function forClinic(int $clinicId): array\n+    {\n+        $common = $this->prices->common();\n+        $own = $this->prices->byClinic($clinicId);\n+\n+        $merged = array_merge($common, $own);\n+\n+        ksort($merged);\n+\n+        return $merged;\n     }\n \n     public function priceOf(int $clinicId, string $code): ?int\n     {\n         return $this->forClinic($clinicId)[$code] ?? null;\n     }\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/Pricing/PriceList.php",
        "kind": "wrong",
        "tag": "array-merge-reindexes",
        "explain": "array_merge перенумеровывает числовые ключи вместо того, чтобы их перекрывать. Коды услуг вроде «1024» превращаются в 0, 1, 2 — а строковые коды при этом перекрываются нормально.",
        "consequence": "Клиника переопределила цену услуги с числовым кодом — и вместо своей цены получила чужую, из строки под тем же порядковым номером. Косметическая стрижка по цене операции появляется в счёте у клиента, а не в тестах: там коды буквенные.",
        "line": 21
      }
    ],
    "decoys": [
      {
        "why": "Сортировка ключей меняет порядок вывода прайса — заметно на экране, но цены от неё не зависят",
        "line": 23
      },
      {
        "why": "Каждый вызов пересобирает весь прайс — расточительно, но правильно",
        "line": 30
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "php-falsy-zero-001",
    "stack": "php",
    "difficulty": 3,
    "title": "Остаток вакцины на складе",
    "prompt": "Показывай остаток вакцины на складе, если он известен, иначе пиши «нет данных»",
    "tests": "PHPUnit 11.2.0\n\n.......                            7 / 7 (100%)\n\nOK (7 tests, 12 assertions)",
    "diff": "--- a/src/Stock/StockView.php\n+++ b/src/Stock/StockView.php\n@@ -11,16 +11,30 @@ final class StockView\n \n     public function __construct(private StockRepository $stock) {}\n \n-    public function line(string $sku): string\n-    {\n-        return '';\n+    public function line(string $sku): string\n+    {\n+        $item = $this->stock->find($sku);\n+\n+        if ($item === null) {\n+            return 'нет данных';\n+        }\n+\n+        $left = $item['quantity'];\n+\n+        if (empty($left)) {\n+            return 'нет данных';\n+        }\n+\n+        return sprintf('%s: %d шт.', $item['name'], $left);\n     }\n \n     public function isLow(string $sku): bool\n     {\n         return ($this->stock->find($sku)['quantity'] ?? 0) < self::LOW;\n     }\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/Stock/StockView.php",
        "kind": "wrong",
        "tag": "falsy-vs-none",
        "explain": "empty() истинно и для нуля. Остаток «ноль штук» — это известные данные, а не их отсутствие.",
        "consequence": "Кончившаяся вакцина показывается как «нет данных», и регистратор записывает на неё приём вместо того, чтобы заказать. Тесты проверяют пустой склад и склад с пятью дозами — ровно тот случай, когда доз ноль, никто не написал.",
        "line": 24
      }
    ],
    "decoys": [
      {
        "why": "Название подставляется без экранирования — для html это важно, но метод отдаёт строку для текста",
        "line": 28
      },
      {
        "why": "Ещё один поход в репозиторий за теми же данными — лишний запрос, но ответ верный",
        "line": 33
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "php-json-decode-shape-001",
    "stack": "php",
    "difficulty": 1,
    "title": "Ответ лаборатории",
    "prompt": "Разбери ответ лаборатории и достань из него статус анализа",
    "tests": "PHPUnit 11.2.0\n\n....                               4 / 4 (100%)\n\nOK (4 tests, 7 assertions)",
    "diff": "--- a/src/Lab/LabResponse.php\n+++ b/src/Lab/LabResponse.php\n@@ -8,14 +8,27 @@ final class LabResponse\n \n     public const UNKNOWN = 'unknown';\n \n-    public function status(string $body): string\n-    {\n-        return self::UNKNOWN;\n+    public function status(string $body): string\n+    {\n+        $data = json_decode($body);\n+\n+        if (!is_array($data)) {\n+            return self::UNKNOWN;\n+        }\n+\n+        return $data['status'] ?? self::UNKNOWN;\n+    }\n+\n+    public function isReady(string $body): bool\n+    {\n+        return $this->status($body) === 'ready';\n     }\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/Lab/LabResponse.php",
        "kind": "wrong",
        "tag": "json-decode-shape",
        "explain": "Без второго аргумента json_decode отдаёт объект, а не массив. Проверка is_array сразу отсекает нормальный ответ.",
        "consequence": "Статус любого анализа всегда «unknown»: готовые результаты не показываются в карточке, и врач звонит в лабораторию вручную. Тесты кормят функцию json-массивом верхнего уровня, а лаборатория отдаёт объект.",
        "line": 13
      }
    ],
    "decoys": [
      {
        "why": "Отсутствующий ключ честно превращается в «unknown» — правильная защита",
        "line": 19
      },
      {
        "why": "Разбор тела повторяется на каждый вызов — лишняя работа, но результат тот же",
        "line": 24
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "php-loose-comparison-001",
    "stack": "php",
    "difficulty": 1,
    "title": "Код подтверждения из смс",
    "prompt": "Проверяй код из смс, три попытки, потом код сгорает",
    "tests": "PHPUnit 11.2.0\n\n.....                              5 / 5 (100%)\n\nOK (5 tests, 9 assertions)",
    "diff": "--- a/src/Auth/SmsCode.php\n+++ b/src/Auth/SmsCode.php\n@@ -14,15 +14,29 @@ final class SmsCode\n \n     public function __construct(private Storage $storage) {}\n \n-    public function verify(string $phone, string $code): bool\n-    {\n-        return false;\n+    public function verify(string $phone, string $code): bool\n+    {\n+        $stored = $this->storage->get(\"sms:$phone\");\n+\n+        if ($stored === null || $stored['attempts'] >= self::MAX_ATTEMPTS) {\n+            return false;\n+        }\n+\n+        $this->storage->increment(\"sms:$phone\", 'attempts');\n+\n+        if ($stored['code'] == $code) {\n+            $this->storage->forget(\"sms:$phone\");\n+\n+            return true;\n+        }\n+\n+        return false;\n     }\n \n     public function issue(string $phone): string\n     {\n         return (string) random_int(100000, 999999);\n     }\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/Auth/SmsCode.php",
        "kind": "wrong",
        "tag": "loose-comparison",
        "explain": "Нестрогое сравнение приводит строки к числам. «0123456» и «123456» станут равны, а код вида «0e12345» совпадёт с любым другим таким же нулевым представлением.",
        "consequence": "Подобрать код становится заметно проще, чем один шанс из миллиона, а тесты этого не видят: в них лежат обычные шестизначные строки без ведущих нулей.",
        "line": 27
      }
    ],
    "decoys": [
      {
        "why": "Попытка засчитывается до проверки — верное решение, хотя выглядит странно",
        "line": 25
      },
      {
        "why": "Код не записывается в хранилище прямо здесь — это делает вызывающий, но глазу зацепиться есть за что",
        "line": 38
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "php-sql-injection-001",
    "stack": "php",
    "difficulty": 2,
    "title": "Поиск владельца по фамилии",
    "prompt": "Добавь поиск владельцев по фамилии с сортировкой по выбранному столбцу",
    "tests": "PHPUnit 11.2.0\n\n......                             6 / 6 (100%)\n\nOK (6 tests, 11 assertions)",
    "diff": "--- a/src/Owners/OwnerSearch.php\n+++ b/src/Owners/OwnerSearch.php\n@@ -10,16 +10,30 @@ final class OwnerSearch\n \n     private const SORTABLE = ['last_name', 'created_at'];\n \n-    public function find(string $query): array\n-    {\n-        return [];\n+    public function find(string $query, string $sort = 'last_name'): array\n+    {\n+        if (!in_array($sort, self::SORTABLE, true)) {\n+            $sort = 'last_name';\n+        }\n+\n+        $sql = \"SELECT id, last_name, phone FROM owners\n+                WHERE last_name ILIKE '%\" . $query . \"%'\n+                ORDER BY $sort\n+                LIMIT 50\";\n+\n+        return $this->db->query($sql)->fetchAll();\n     }\n \n     public function byId(int $id): ?array\n     {\n         return $this->db->prepare('SELECT * FROM owners WHERE id = ?')->execute([$id])->fetch();\n     }\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/Owners/OwnerSearch.php",
        "kind": "wrong",
        "tag": "sql-injection",
        "explain": "Строка из формы приклеивается к запросу. Соседний метод byId работает через подготовленный запрос, а этот — нет.",
        "consequence": "Кавычка в поле поиска ломает запрос, а подобранная строка отдаёт наружу таблицу владельцев целиком: телефоны, адреса, паспорта. Тесты ищут «Иванов» и ничего не замечают.",
        "line": 20
      }
    ],
    "decoys": [
      {
        "why": "Столбец сортировки тоже подставляется в текст запроса, но он предварительно сверен с белым списком",
        "line": 21
      },
      {
        "why": "Жёсткий лимит без постраничности — часть результатов не увидят, но это не дыра",
        "line": 22
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "py-assert-true-001",
    "stack": "py",
    "difficulty": 1,
    "title": "Тест расчёта дозы",
    "prompt": "Покрой тестом расчёт дозы по весу",
    "tests": "==== test session starts ====\ncollected 3 items\n\ntests/test_dosage.py ...              [100%]\n\n==== 3 passed in 0.08s ====",
    "diff": "--- a/tests/test_dosage.py\n+++ b/tests/test_dosage.py\n@@ -1,14 +1,31 @@\n import pytest\n \n from dosage import calculate, MAX_SINGLE_DOSE\n \n \n+def test_dosage_for_small_pet():\n+    result = calculate(weight_kg=3.5, drug=\"amoxicillin\")\n+    assert result is not None\n+\n+\n+def test_dosage_for_large_pet():\n+    result = calculate(weight_kg=42.0, drug=\"amoxicillin\")\n+    assert result\n+\n+\n+def test_dosage_is_capped():\n+    result = calculate(weight_kg=120.0, drug=\"amoxicillin\")\n+    assert result <= MAX_SINGLE_DOSE\n+\n+\n+def test_unknown_drug_raises():\n+    with pytest.raises(KeyError):\n+        calculate(weight_kg=5.0, drug=\"нет-такого\")\n",
    "clean": false,
    "bugs": [
      {
        "file": "tests/test_dosage.py",
        "kind": "wrong",
        "tag": "assert-without-expectation",
        "explain": "Тест проверяет, что функция что-то вернула, но не проверяет, что именно. Любое число пройдёт.",
        "consequence": "Ошибка в формуле дозы не будет поймана вообще: тест зелёный при любом результате. Здесь это не абстрактный риск — по этой цифре препарат назначают животному.",
        "line": 8
      }
    ],
    "decoys": [
      {
        "why": "второй тест по сути ровно такой же — но подлянку достаточно отметить один раз",
        "line": 11
      },
      {
        "why": "вот этот тест проверяет настоящее свойство — на него как раз претензий нет",
        "line": 18
      },
      {
        "why": "KeyError наружу вместо своего исключения — вопрос к коду, а не к тесту",
        "line": 22
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "py-class-attr-001",
    "stack": "py",
    "difficulty": 3,
    "title": "Корзина назначений",
    "prompt": "Сделай класс, в который врач складывает назначения по ходу приёма",
    "tests": "==== test session starts ====\ncollected 3 items\n\ntests/test_cart.py ...                [100%]\n\n==== 3 passed in 0.11s ====",
    "diff": "--- a/cart.py\n+++ b/cart.py\n@@ -1,2 +1,12 @@\n+class PrescriptionCart:\n+    items = []\n+\n+    def __init__(self, visit_id):\n+        self.visit_id = visit_id\n+\n+    def add(self, prescription):\n+        self.items.append(prescription)\n+\n+    def clear(self):\n+        self.items = []\n",
    "clean": false,
    "bugs": [
      {
        "file": "cart.py",
        "kind": "wrong",
        "tag": "shared-class-attribute",
        "explain": "items объявлен на классе, а не в __init__, поэтому список один на все экземпляры. add дописывает в общий.",
        "consequence": "Два приёма в одном процессе — и назначения одного пациента появляются в карточке другого. Хуже того, clear создаёт экземпляру собственный атрибут, так что после первой очистки поведение у объектов расходится и баг перестаёт воспроизводиться.",
        "line": 2
      }
    ],
    "decoys": [
      {
        "why": "clear через присваивание, а не через .clear() — как раз это и маскирует подлянку, но само по себе корректно",
        "line": 11
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "py-clean-batching-001",
    "stack": "py",
    "difficulty": 4,
    "title": "Батчевая рассылка",
    "prompt": "Отправляй напоминания пачками по сотне, чтобы не класть SMS-шлюз",
    "tests": "==== test session starts ====\ncollected 7 items\n\ntests/test_batch.py .......           [100%]\n\n==== 7 passed in 0.34s ====",
    "diff": "--- a/batch.py\n+++ b/batch.py\n@@ -1,3 +1,16 @@\n+def chunks(items, size):\n+    batch = []\n+    for item in items:\n+        batch.append(item)\n+        if len(batch) == size:\n+            yield batch\n+            batch = []\n+\n+    if batch:\n+        yield batch\n+\n+def send_all(reminders):\n+    for batch in chunks(reminders, 100):\n+        gateway.send_many(batch)\n",
    "clean": true,
    "bugs": [],
    "decoys": [
      {
        "why": "переприсваивание вместо batch.clear() выглядит небрежно — но здесь оно обязательно, иначе отданная пачка обнулится у получателя",
        "line": 7
      },
      {
        "why": "хвост отдельным условием кажется лишним — без него теряется последняя неполная пачка",
        "line": 9
      },
      {
        "why": "нет обработки ошибки шлюза — вопрос справедливый, но в задаче просили только батчи",
        "line": 14
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "py-dict-mutation-001",
    "stack": "py",
    "difficulty": 3,
    "title": "Нормализация прайса",
    "prompt": "Приведи цены из внешнего прайса к нашему формату",
    "tests": "==== test session starts ====\ncollected 5 items\n\ntests/test_price.py .....             [100%]\n\n==== 5 passed in 0.19s ====",
    "diff": "--- a/price.py\n+++ b/price.py\n@@ -1,15 +1,33 @@\n import logging\n \n log = logging.getLogger(__name__)\n \n \n def parse_row(row):\n     code, raw = row.split(\";\", 1)\n     return code.strip(), float(raw) if raw.strip() else None\n \n \n+def normalize(raw_prices):\n+    for code, price in raw_prices.items():\n+        if price is None:\n+            del raw_prices[code]\n+        else:\n+            raw_prices[code] = round(price * 100)\n+\n+    return raw_prices\n+\n+\n+def load(rows):\n+    prices = dict(parse_row(r) for r in rows)\n+    log.info(\"прочитано позиций: %s\", len(prices))\n+\n+    return normalize(prices)\n",
    "clean": false,
    "bugs": [
      {
        "file": "price.py",
        "kind": "wrong",
        "tag": "mutating-while-iterating",
        "explain": "Словарь меняется прямо во время обхода. Python бросит RuntimeError, как только в прайсе встретится хотя бы одна пустая цена.",
        "consequence": "На тестовых данных без пустых цен всё зелёное, а первый же реальный прайс поставщика с прочерком в строке роняет ночной импорт целиком. Утром прайс не обновился, и никто не знает почему.",
        "line": 14
      }
    ],
    "decoys": [
      {
        "why": "функция и меняет аргумент, и возвращает его — путано, но само по себе не ломает",
        "line": 18
      },
      {
        "why": "лог до нормализации покажет число до отсева — мелочь",
        "line": 23
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "py-encoding-compare-001",
    "stack": "py",
    "difficulty": 3,
    "title": "Сверка ФИО владельца",
    "prompt": "Сверяй ФИО владельца с тем, что пришло из госуслуг",
    "tests": "==== test session starts ====\ncollected 4 items\n\ntests/test_match.py ....              [100%]\n\n==== 4 passed in 0.10s ====",
    "diff": "--- a/matching.py\n+++ b/matching.py\n@@ -1,13 +1,29 @@\n from dataclasses import dataclass\n \n \n @dataclass\n class Person:\n     full_name: str\n     birth_date: str\n \n \n+def same_person(ours, theirs):\n+    a = ours.full_name.strip().lower()\n+    b = theirs.full_name.strip().lower()\n+\n+    return a == b\n+\n+\n+def merge_candidates(ours, incoming):\n+    matched, unmatched = [], []\n+\n+    for person in incoming:\n+        if any(same_person(person, mine) for mine in ours):\n+            matched.append(person)\n+        else:\n+            unmatched.append(person)\n+\n+    return matched, unmatched\n",
    "clean": false,
    "bugs": [
      {
        "file": "matching.py",
        "kind": "missing",
        "tag": "unicode-normalization",
        "explain": "Строки сравниваются побайтово, без нормализации Unicode. «Ё» и «е», а также визуально одинаковые последовательности в разных формах NFC и NFD дают неравенство.",
        "consequence": "Владельцы с «ё» в фамилии не сопоставляются с госуслугами и заводятся вторым дублем. Обнаружится это на этапе, когда у одного человека уже два личных кабинета и разные истории визитов.",
        "line": 14
      }
    ],
    "decoys": [
      {
        "why": "strip и lower тут как раз к месту, только их недостаточно",
        "line": 11
      },
      {
        "why": "два списка одной строкой — стиль, к делу не относится",
        "line": 18
      },
      {
        "why": "квадратичный перебор — на больших выгрузках медленно, но результат верный",
        "line": 21
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "py-error-swallowed-001",
    "stack": "py",
    "difficulty": 1,
    "title": "Устойчивый синк",
    "prompt": "Сделай синк устойчивым, а то один упавший элемент роняет весь прогон",
    "tests": "==== test session starts ====\ncollected 4 items\n\ntests/test_sync.py ....                [100%]\n\n==== 4 passed in 0.31s ====",
    "diff": "--- a/sync.py\n+++ b/sync.py\n@@ -12,6 +12,9 @@\n def sync_all(items):\n     ok = 0\n     for item in items:\n-        push(item)\n-        ok += 1\n+        try:\n+            push(item)\n+        except Exception as e:\n+            logger.warning(\"push failed: %s\", e)\n+        ok += 1\n     return ok\n",
    "clean": false,
    "bugs": [
      {
        "file": "sync.py",
        "kind": "wrong",
        "tag": "error-swallowed",
        "explain": "ok увеличивается независимо от того, упал push или нет — счётчик стоит вне try.",
        "consequence": "Синк отчитывается «отправлено 500», отправлено 480. Расхождение всплывает через месяц, когда клиент не находит своих данных, и разбираться придётся по логам уровня warning, которые никто не читает.",
        "line": 19
      }
    ],
    "decoys": [
      {
        "why": "except Exception — широкий перехват, придирка справедливая, но не баг этой задачи",
        "line": 17
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "py-falsy-zero-001",
    "stack": "py",
    "difficulty": 3,
    "title": "Вес пациента в карточке",
    "prompt": "Подставляй прошлый вес, если в этот приём его не измеряли",
    "tests": "==== test session starts ====\ncollected 4 items\n\ntests/test_chart.py ....              [100%]\n\n==== 4 passed in 0.13s ====",
    "diff": "--- a/chart.py\n+++ b/chart.py\n@@ -1,15 +1,32 @@\n from dataclasses import dataclass\n \n \n @dataclass\n class Visit:\n     id: int\n     weight: float | None\n     temperature: float | None\n \n \n+def effective_weight(visit, previous):\n+    if not visit.weight:\n+        return previous.weight\n+\n+    return visit.weight\n+\n+\n+def weight_delta(visit, previous):\n+    now = effective_weight(visit, previous)\n+\n+    if previous.weight is None:\n+        return None\n+\n+    return round(now - previous.weight, 2)\n+\n+\n def formatted(value, unit):\n     return \"—\" if value is None else f\"{value} {unit}\"\n",
    "clean": false,
    "bugs": [
      {
        "file": "chart.py",
        "kind": "wrong",
        "tag": "falsy-vs-none",
        "explain": "not срабатывает не только на None, но и на 0 и на 0.0. «Не измеряли» и «измерили и получили ноль» становятся одним случаем.",
        "consequence": "Ошибка весов или ввод нуля подменяются прошлым весом молча, и в карточке появляется правдоподобное число, которого никто не измерял. По нему считают дозу препарата.",
        "line": 12
      }
    ],
    "decoys": [
      {
        "why": "previous не проверяется на None — упадёт шумно и сразу, в отличие от подлянки",
        "line": 13
      },
      {
        "why": "здесь как раз проверка на None, а не на ложность — так и надо",
        "line": 21
      },
      {
        "why": "форматирование через тернарник — вкусовщина",
        "line": 28
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "py-float-money-001",
    "stack": "py",
    "difficulty": 3,
    "title": "Итог по чеку",
    "prompt": "Посчитай итог по позициям чека со скидкой",
    "tests": "==== test session starts ====\ncollected 6 items\n\ntests/test_billing.py ......           [100%]\n\n==== 6 passed in 0.29s ====",
    "diff": "--- a/billing.py\n+++ b/billing.py\n@@ -1,14 +1,32 @@\n from dataclasses import dataclass\n \n \n @dataclass\n class Item:\n     name: str\n     price: float\n     qty: int\n \n \n+def total(items, discount_percent=0):\n+    subtotal = 0.0\n+    for item in items:\n+        subtotal += item.price * item.qty\n+\n+    return round(subtotal * (1 - discount_percent / 100), 2)\n+\n+\n+def split_between(items, payers):\n+    if payers < 1:\n+        raise ValueError(\"плательщиков должно быть хотя бы один\")\n+\n+    per_payer = total(items) / payers\n+\n+    return [round(per_payer, 2)] * payers\n+\n+\n def as_receipt_line(item):\n     return f\"{item.name} x{item.qty}\"\n",
    "clean": false,
    "bugs": [
      {
        "file": "billing.py",
        "kind": "wrong",
        "tag": "float-money",
        "explain": "Деньги считаются в float. 0.1 + 0.2 в двоичной дроби не даёт 0.3, и ошибка копится на каждой позиции.",
        "consequence": "Итог по чеку из тридцати позиций расходится с суммой строк на копейку, и это ровно та копейка, из-за которой не сходится кассовая смена. Разбираться будет бухгалтер, а не программист.",
        "line": 12
      }
    ],
    "decoys": [
      {
        "why": "округление в конце выглядит как затычка — оно и есть затычка, но не первопричина",
        "line": 16
      },
      {
        "why": "скидка числом, а не долей — неудобно, но считается верно",
        "line": 11
      },
      {
        "why": "при делении на троих сумма частей не сойдётся с итогом — отдельная беда, но не эта",
        "line": 25
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "py-generator-consumed-001",
    "stack": "py",
    "difficulty": 5,
    "title": "Отчёт по движению склада",
    "prompt": "Посчитай по движениям склада и сумму, и количество позиций",
    "tests": "==== test session starts ====\ncollected 5 items\n\ntests/test_stock_report.py .....      [100%]\n\n==== 5 passed in 0.21s ====",
    "diff": "--- a/stock_report.py\n+++ b/stock_report.py\n@@ -1,16 +1,33 @@\n from dataclasses import dataclass\n \n \n @dataclass\n class Report:\n     total: int\n     positions: int\n \n \n def build_report(clinic_id):\n-    moves = list(fetch_moves(clinic_id))\n-    total = sum(m.delta * m.price for m in moves)\n-    positions = len({m.sku for m in moves})\n+    moves = (m for m in fetch_moves(clinic_id) if m.delta != 0)\n+\n+    total = sum(m.delta * m.price for m in moves)\n+    positions = len({m.sku for m in moves})\n \n     return Report(total=total, positions=positions)\n+\n+\n+def build_all(clinic_ids):\n+    return {cid: build_report(cid) for cid in clinic_ids}\n",
    "clean": false,
    "bugs": [
      {
        "file": "stock_report.py",
        "kind": "wrong",
        "tag": "generator-consumed-twice",
        "explain": "moves теперь генератор, а не список. Первый проход его исчерпывает, второй получает пустоту.",
        "consequence": "Сумма считается верно, а число позиций всегда ноль. Ноль выглядит как «движений не было», а не как поломка, поэтому отчёт неделями считают правдой и по нему решают, что закупать.",
        "line": 14
      }
    ],
    "decoys": [
      {
        "why": "замена списка на генератор — как раз разумная оптимизация, ломает не она сама",
        "line": 11
      },
      {
        "why": "первый проход отрабатывает совершенно нормально — в этом и коварство",
        "line": 13
      },
      {
        "why": "словарное включение по клиникам — читаемо и верно",
        "line": 20
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "py-mutable-default-001",
    "stack": "py",
    "difficulty": 2,
    "title": "Накопитель ошибок импорта",
    "prompt": "Собери ошибки построчно при импорте прайса, чтобы показать их разом",
    "tests": "==== test session starts ====\ncollected 5 items\n\ntests/test_import.py .....            [100%]\n\n==== 5 passed in 0.44s ====",
    "diff": "--- a/importer.py\n+++ b/importer.py\n@@ -18,7 +18,12 @@\n-def import_price(rows):\n-    return [parse(row) for row in rows]\n+def import_price(rows, errors=[]):\n+    parsed = []\n+    for i, row in enumerate(rows):\n+        try:\n+            parsed.append(parse(row))\n+        except ValueError as e:\n+            errors.append(f\"строка {i}: {e}\")\n+    return parsed, errors\n",
    "clean": false,
    "bugs": [
      {
        "file": "importer.py",
        "kind": "wrong",
        "tag": "mutable-default-arg",
        "explain": "Список по умолчанию создаётся один раз при объявлении функции, а не на каждый вызов. Он общий для всех импортов за время жизни процесса.",
        "consequence": "Второй импорт прайса показывает ошибки первого, третий — обоих. Клиника видит претензии к строкам, которых в её файле нет, и приходит в поддержку с скриншотом, который невозможно воспроизвести на пустом процессе.",
        "line": 18
      }
    ],
    "decoys": [
      {
        "why": "функция стала возвращать кортеж вместо списка — вызывающий код чинить придётся, но сборка это ловит",
        "line": 25
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "py-naive-datetime-001",
    "stack": "py",
    "difficulty": 4,
    "title": "Напоминание о вакцинации",
    "prompt": "Шли напоминание, если до ревакцинации осталось меньше недели",
    "tests": "==== test session starts ====\ncollected 4 items\n\ntests/test_reminders.py ....          [100%]\n\n==== 4 passed in 0.18s ====",
    "diff": "--- a/reminders.py\n+++ b/reminders.py\n@@ -1,15 +1,32 @@\n from datetime import datetime, timedelta\n \n from db import session\n from models import Vaccination\n \n+WINDOW = timedelta(days=7)\n+\n+\n+def needs_reminder(vaccination):\n+    now = datetime.now()\n+\n+    return vaccination.due_at - now <= WINDOW\n+\n+\n+def due_reminders(clinic_id):\n+    upcoming = (\n+        session.query(Vaccination)\n+        .filter(Vaccination.clinic_id == clinic_id)\n+        .filter(Vaccination.reminded_at.is_(None))\n+        .all()\n+    )\n+\n+    return [v for v in upcoming if needs_reminder(v)]\n+\n+\n def mark_reminded(vaccination):\n     vaccination.reminded_at = datetime.utcnow()\n     session.commit()\n",
    "clean": false,
    "bugs": [
      {
        "file": "reminders.py",
        "kind": "wrong",
        "tag": "naive-datetime",
        "explain": "datetime.now() отдаёт время без зоны и по времени машины, а due_at приходит из базы в UTC с зоной.",
        "consequence": "На сервере в UTC сравнение просто съезжает на разницу поясов, а если due_at всё-таки с таймзоной — Python бросит TypeError прямо в задаче по расписанию. Напоминания либо уходят не в тот день, либо не уходят вовсе, и узнают об этом от клиентов.",
        "line": 10
      }
    ],
    "decoys": [
      {
        "why": "окно константой — как раз хорошо, придраться не к чему",
        "line": 6
      },
      {
        "why": "фильтр «ещё не напоминали» на месте, дублей от него не будет",
        "line": 19
      },
      {
        "why": "utcnow тоже наивный, но здесь он только записывается, а не сравнивается",
        "line": 27
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "py-open-encoding-001",
    "stack": "py",
    "difficulty": 2,
    "title": "Выгрузка справочника",
    "prompt": "Выгружай справочник услуг в CSV для бухгалтерии",
    "tests": "==== test session starts ====\ncollected 4 items\n\ntests/test_export.py ....             [100%]\n\n==== 4 passed in 0.22s ====",
    "diff": "--- a/export.py\n+++ b/export.py\n@@ -1,14 +1,31 @@\n import csv\n from datetime import date\n \n \n def export_filename(clinic_id):\n     return f\"services-{clinic_id}-{date.today():%Y-%m-%d}.csv\"\n \n \n+def export_services(services, path):\n+    with open(path, \"w\", newline=\"\") as f:\n+        writer = csv.writer(f)\n+        writer.writerow([\"Код\", \"Название\", \"Цена\"])\n+        writer.writerows([s.code, s.name, s.price] for s in services)\n+\n+\n+def export_all(clinics, target_dir):\n+    written = []\n+\n+    for clinic in clinics:\n+        path = f\"{target_dir}/{export_filename(clinic.id)}\"\n+        export_services(clinic.services, path)\n+        written.append(path)\n+\n+    return written\n",
    "clean": false,
    "bugs": [
      {
        "file": "export.py",
        "kind": "missing",
        "tag": "platform-default-encoding",
        "explain": "Кодировка не указана, поэтому берётся системная. На сервере это UTF-8, на машине бухгалтера под Windows — cp1251.",
        "consequence": "Файл, собранный в тестах и на проде, открывается по-разному, а на части машин выгрузка падает с UnicodeEncodeError на первом же названии с ё. Классический баг, который не воспроизводится у того, кто его чинит.",
        "line": 10
      }
    ],
    "decoys": [
      {
        "why": "заголовок без BOM — Excel откроет криво, но это следствие той же причины",
        "line": 12
      },
      {
        "why": "склейка пути строкой вместо os.path.join — на Windows криво, но не ломает выгрузку",
        "line": 20
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "py-path-join-001",
    "stack": "py",
    "difficulty": 3,
    "title": "Путь до вложения",
    "prompt": "Собери путь до файла вложения по имени, которое пришло из формы",
    "tests": "==== test session starts ====\ncollected 5 items\n\ntests/test_storage.py .....           [100%]\n\n==== 5 passed in 0.15s ====",
    "diff": "--- a/storage.py\n+++ b/storage.py\n@@ -1,14 +1,29 @@\n import os\n \n UPLOADS = \"/var/vetmanager/uploads\"\n ALLOWED = {\".pdf\", \".jpg\", \".png\"}\n \n \n def is_allowed(filename):\n     return os.path.splitext(filename)[1].lower() in ALLOWED\n \n \n+def attachment_path(filename):\n+    return os.path.join(UPLOADS, filename)\n+\n+\n+def read_attachment(filename):\n+    if not is_allowed(filename):\n+        raise ValueError(\"недопустимый тип файла\")\n+\n+    with open(attachment_path(filename), \"rb\") as f:\n+        return f.read()\n+\n+\n def attachment_url(filename):\n     return f\"/files/{filename}\"\n",
    "clean": false,
    "bugs": [
      {
        "file": "storage.py",
        "kind": "wrong",
        "tag": "path-traversal",
        "explain": "os.path.join с абсолютным вторым аргументом молча отбрасывает первый, а ../ он не нормализует вовсе. Имя приходит из формы.",
        "consequence": "Запрос с filename вида /etc/passwd или ../../config.py отдаёт файл за пределами каталога загрузок. Это уже не баг, а дыра: читать можно всё, до чего дотягивается пользователь процесса.",
        "line": 12
      }
    ],
    "decoys": [
      {
        "why": "путь захардкожен — в конфиг бы его, но опасность не здесь",
        "line": 3
      },
      {
        "why": "проверка расширения выглядит защитой — но она про тип файла, а не про то, где он лежит",
        "line": 16
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "py-report-two-holes-002",
    "stack": "py",
    "difficulty": 5,
    "title": "Выгрузка визитов за месяц",
    "prompt": "Сделай выгрузку визитов за месяц в csv, бухгалтерия просит",
    "tests": "PASS  tests/test_export.py (5)\n  ✓ выгружает визиты за период\n  ✓ пишет заголовок\n  ✓ пустой месяц даёт пустой файл\n\n5 passed in 0.42s",
    "diff": "--- a/app/export.py\n+++ b/app/export.py\n@@ -1,20 +1,42 @@\n import csv\n from datetime import date, datetime\n from decimal import Decimal\n \n from app.db import session\n from app.models import Visit\n \n \n+def export_month(year: int, month: int, path: str) -> int:\n+    start = date(year, month, 1)\n+    end = date(year + (month == 12), month % 12 + 1, 1)\n+\n+    visits = (\n+        session.query(Visit)\n+        .filter(Visit.created_at >= start)\n+        .filter(Visit.created_at <= end)\n+        .order_by(Visit.created_at)\n+        .all()\n+    )\n+\n+    with open(path, \"w\") as f:\n+        writer = csv.writer(f)\n+        writer.writerow([\"дата\", \"питомец\", \"владелец\", \"сумма\"])\n+\n+        for v in visits:\n+            writer.writerow([\n+                v.created_at.strftime(\"%d.%m.%Y\"),\n+                v.patient.name,\n+                v.patient.owner.name,\n+                float(v.total),\n+            ])\n+\n+    return len(visits)\n",
    "clean": false,
    "bugs": [
      {
        "file": "app/export.py",
        "kind": "wrong",
        "tag": "off-by-one-boundary",
        "explain": "Правая граница включена, а end — это уже первое число следующего месяца. Визиты, заведённые ровно в полночь первого числа, попадают и в этот отчёт, и в следующий. Нужно строгое сравнение.",
        "consequence": "Годовая сверка не сходится на несколько визитов, и найти их можно только выгрузив все двенадцать месяцев и сравнив построчно.",
        "line": 16
      },
      {
        "file": "app/export.py",
        "kind": "wrong",
        "tag": "float-money",
        "explain": "Сумма хранится Decimal, а в файл уходит float. 1834.85 превращается в 1834.8500000000001 или, наоборот, теряет копейку при сложении в экселе.",
        "consequence": "Бухгалтерия складывает столбец и получает расхождение с кассой на копейки. Каждый месяц заново, и каждый раз это ищут в кассе, а не в выгрузке.",
        "line": 30
      },
      {
        "file": "app/export.py",
        "kind": "wrong",
        "tag": "platform-default-encoding",
        "explain": "Файл открыт без указания кодировки: на сервере это utf-8, на машине бухгалтера — cp1251. Ещё и newline не задан, из-за чего csv на Windows пишет пустую строку между записями.",
        "consequence": "Файл открывается кракозябрами у половины получателей. Списывают на «эксель кривой» и пересылают друг другу скриншоты.",
        "line": 21
      }
    ],
    "decoys": [
      {
        "why": "сортировка без вторичного ключа — при равных датах порядок не гарантирован, но для выгрузки это не важно",
        "line": 17
      },
      {
        "why": "переход через декабрь выглядит хитро, но считает верно",
        "line": 11
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "py-retry-broad-001",
    "stack": "py",
    "difficulty": 4,
    "title": "Повтор отправки в ОФД",
    "prompt": "Повторяй отправку чека в ОФД, если сеть моргнула",
    "tests": "==== test session starts ====\ncollected 6 items\n\ntests/test_ofd.py ......              [100%]\n\n==== 6 passed in 1.02s ====",
    "diff": "--- a/ofd.py\n+++ b/ofd.py\n@@ -14,6 +14,14 @@\n-def send_receipt(receipt):\n-    return client.post(\"/receipts\", receipt.as_dict())\n+def send_receipt(receipt):\n+    for attempt in range(5):\n+        try:\n+            return client.post(\"/receipts\", receipt.as_dict())\n+        except Exception:\n+            time.sleep(2 ** attempt)\n+\n+    raise OfdUnavailable(receipt.id)\n",
    "clean": false,
    "bugs": [
      {
        "file": "ofd.py",
        "kind": "wrong",
        "tag": "retry-on-non-retryable",
        "explain": "Повторяется всё подряд, включая отказы, которые повтором не лечатся: невалидный чек, просроченный токен, отказ фискального накопителя.",
        "consequence": "Один битый чек отправляется пять раз с нарастающей паузой, забивая очередь на полминуты, и в ОФД может уехать пять дублей одного чека. Настоящую причину при этом не видно: исходное исключение потеряно, наружу летит OfdUnavailable.",
        "line": 18
      }
    ],
    "decoys": [
      {
        "why": "экспоненциальная пауза без джиттера — на масштабе будет синхронный наплыв, но не сейчас",
        "line": 19
      },
      {
        "why": "своё исключение вместо исходного — часть той же проблемы, но не её корень",
        "line": 21
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "py-shadow-builtin-001",
    "stack": "py",
    "difficulty": 1,
    "title": "Фильтр просроченных вакцин",
    "prompt": "Отбери вакцинации, у которых вышел срок",
    "tests": "==== test session starts ====\ncollected 4 items\n\ntests/test_vaccines.py ....           [100%]\n\n==== 4 passed in 0.12s ====",
    "diff": "--- a/vaccines.py\n+++ b/vaccines.py\n@@ -1,14 +1,32 @@\n from datetime import date\n \n \n def is_due(vaccination, today):\n     return vaccination.due_at <= today\n \n \n+def expired(vaccinations, today):\n+    list = []\n+    for v in vaccinations:\n+        if v.due_at < today:\n+            list.append(v)\n+\n+    return list\n+\n+\n+def summary(vaccinations):\n+    today = date.today()\n+\n+    return {\n+        \"expired\": len(expired(vaccinations, today)),\n+        \"due\": sum(1 for v in vaccinations if is_due(v, today)),\n+    }\n+\n+\n def next_due(vaccinations):\n     return min((v.due_at for v in vaccinations), default=None)\n",
    "clean": false,
    "bugs": [
      {
        "file": "vaccines.py",
        "kind": "wrong",
        "tag": "builtin-shadowed",
        "explain": "Имя list перекрывает встроенный тип внутри функции.",
        "consequence": "Пока в функции больше ничего нет — работает. Первый же, кто допишет сюда list(...) или аннотацию, получит TypeError про то, что список не вызывается, и потратит полчаса, потому что ошибка указывает не туда.",
        "line": 9
      }
    ],
    "decoys": [
      {
        "why": "сравнение без учёта часового пояса — вопрос есть, но зависит от того, что кладут в due_at",
        "line": 11
      },
      {
        "why": "просроченные попадают и в due тоже — спорная семантика, но не поломка",
        "line": 22
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "rb-integer-division-001",
    "stack": "rb",
    "difficulty": 1,
    "title": "Доля оплаченных визитов",
    "prompt": "Покажи на дашборде долю оплаченных визитов за день в процентах",
    "tests": "....\n\nFinished in 0.014 seconds (files took 0.37 seconds to load)\n4 examples, 0 failures",
    "diff": "--- a/app/dashboards/payment_share.rb\n+++ b/app/dashboards/payment_share.rb\n@@ -4,14 +4,27 @@ class PaymentShare\n \n   def initialize(visits)\n     @visits = visits\n   end\n \n-  def percent\n-    0\n+  def percent\n+    return 0 if @visits.empty?\n+\n+    paid = @visits.count(&:paid?)\n+\n+    (paid / @visits.size) * 100\n+  end\n+\n+  def label\n+    \"#{percent.round}% оплачено\"\n   end\n \n   def total_kop\n     @visits.sum(&:total_kop)\n   end\n end\n",
    "clean": false,
    "bugs": [
      {
        "file": "app/dashboards/payment_share.rb",
        "kind": "wrong",
        "tag": "integer-division",
        "explain": "Деление двух целых в руби тоже целое: пока оплачены не все визиты, результат — ноль.",
        "consequence": "Дашборд показывает «0% оплачено» весь день и внезапно «100%» вечером, когда закроют последний визит. По этой цифре администратор решает, звонить ли должникам.",
        "line": 14
      }
    ],
    "decoys": [
      {
        "why": "Подсчёт в памяти вместо запроса к базе — на дне визитов это допустимо",
        "line": 12
      },
      {
        "why": "round у целого числа ничего не меняет — лишний вызов, но безвредный",
        "line": 18
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "rb-mutable-constant-001",
    "stack": "rb",
    "difficulty": 2,
    "title": "Поля выгрузки для страховой",
    "prompt": "Собери список полей выгрузки: базовые всегда, плюс поля страховой, если она указана",
    "tests": "......\n\nFinished in 0.018 seconds (files took 0.38 seconds to load)\n6 examples, 0 failures",
    "diff": "--- a/app/exports/visit_export.rb\n+++ b/app/exports/visit_export.rb\n@@ -4,16 +4,29 @@ class VisitExport\n \n   BASE_FIELDS = %w[id visited_at pet_name total].freeze\n   INSURANCE_FIELDS = %w[policy_number insurer_code].freeze\n \n-  def fields(_visit)\n-    BASE_FIELDS\n+  def fields(visit)\n+    result = BASE_FIELDS\n+\n+    if visit.insurance.present?\n+      result += INSURANCE_FIELDS\n+    end\n+\n+    if visit.discount.present?\n+      result << 'discount_percent'\n+    end\n+\n+    result\n   end\n \n   def header(visit)\n     fields(visit).map { |f| I18n.t(\"export.#{f}\") }.join(';')\n   end\n end\n",
    "clean": false,
    "bugs": [
      {
        "file": "app/exports/visit_export.rb",
        "kind": "wrong",
        "tag": "mutable-constant",
        "explain": "Если страховки не было, result — это сам BASE_FIELDS, а не копия: += создаёт новый массив, а << дописывает в существующий. freeze спасает от этого только в строгом режиме.",
        "consequence": "Первый же визит со скидкой добавляет колонку в константу на весь процесс — дальше она лезет во все выгрузки подряд, включая те, где скидки нет. Перезапуск «чинит», поэтому баг переживёт несколько разборов.",
        "line": 16
      }
    ],
    "decoys": [
      {
        "why": "Здесь как раз создаётся новый массив — выглядит непоследовательно рядом с соседней веткой, но это правильный вариант",
        "line": 12
      },
      {
        "why": "Перевод по интерполированному ключу — если перевода нет, в шапку уедет техническое имя",
        "line": 23
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "rb-n-plus-one-001",
    "stack": "rb",
    "difficulty": 3,
    "title": "Сводка по приёмам за день",
    "prompt": "Собери сводку за день: приём, кличка, владелец и сумма — одним списком",
    "tests": ".....\n\nFinished in 0.126 seconds (files took 0.41 seconds to load)\n5 examples, 0 failures",
    "diff": "--- a/app/queries/day_summary.rb\n+++ b/app/queries/day_summary.rb\n@@ -5,16 +5,30 @@ class DaySummary\n \n   def initialize(clinic, date)\n     @clinic = clinic\n     @date = date\n   end\n \n-  def rows\n-    []\n+  def rows\n+    visits = @clinic.visits\n+                    .where(visited_at: @date.all_day)\n+                    .includes(:pet)\n+                    .order(:visited_at)\n+\n+    visits.map do |visit|\n+      {\n+        at: visit.visited_at,\n+        pet: visit.pet.name,\n+        owner: visit.pet.owner.last_name,\n+        total: visit.positions.sum(:kop),\n+      }\n+    end\n   end\n \n   def total_kop\n     rows.sum { |r| r[:total] }\n   end\n end\n",
    "clean": false,
    "bugs": [
      {
        "file": "app/queries/day_summary.rb",
        "kind": "wrong",
        "tag": "n-plus-one",
        "explain": "includes подтянул только питомцев. Владелец и позиции догружаются отдельным запросом на каждую строку — нужно includes(pet: :owner) и предзагрузка позиций.",
        "consequence": "На двадцати приёмах разница незаметна, на дне загруженной клиники это сотни запросов и секунды ожидания на самом частом экране. Тесты гоняют три визита и укладываются в любые рамки.",
        "line": 21
      }
    ],
    "decoys": [
      {
        "why": "Сумма считается базой, а не в руби — само по себе это правильный выбор",
        "line": 22
      },
      {
        "why": "Итог пересобирает весь список заново — лишняя работа, но результат верный",
        "line": 28
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "rb-rescue-nil-001",
    "stack": "rb",
    "difficulty": 1,
    "title": "Аватар питомца из внешнего сервиса",
    "prompt": "Подтягивай фото питомца из сервиса картинок, если не получилось — ставь заглушку",
    "tests": "....\n\nFinished in 0.031 seconds (files took 0.4 seconds to load)\n4 examples, 0 failures",
    "diff": "--- a/app/services/avatar_fetcher.rb\n+++ b/app/services/avatar_fetcher.rb\n@@ -6,15 +6,28 @@ class AvatarFetcher\n \n   PLACEHOLDER = '/images/pet-placeholder.png'\n \n-  def call(pet)\n-    PLACEHOLDER\n+  def call(pet)\n+    url = Rails.cache.fetch(\"avatar:#{pet.id}\", expires_in: 1.day) do\n+      fetch_remote(pet) rescue nil\n+    end\n+\n+    url.presence || PLACEHOLDER\n+  end\n+\n+  private\n+\n+  def fetch_remote(pet)\n+    response = @client.get(\"/pets/#{pet.external_id}/photo\")\n+\n+    response.success? ? response.body['url'] : nil\n   end\n end\n",
    "clean": false,
    "bugs": [
      {
        "file": "app/services/avatar_fetcher.rb",
        "kind": "wrong",
        "tag": "error-swallowed",
        "explain": "Модификатор rescue ловит вообще всё, включая опечатку в имени метода и обрыв соединения, и превращает в nil без единой записи в лог.",
        "consequence": "Сервис картинок сменил формат ответа — у всех питомцев разом заглушка, алертов нет, в мониторинге ровно ничего. Заметят, когда напишет клиент; на разбор уйдёт день, потому что искать будут не там.",
        "line": 11
      }
    ],
    "decoys": [
      {
        "why": "Неудача тоже попадает в кэш на сутки — вопрос к времени жизни, но саму ошибку прячет не это",
        "line": 10
      },
      {
        "why": "Неуспешный статус превращается в nil без разбора кода ответа — грубо, но осознанно",
        "line": 22
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "rb-symbol-string-key-001",
    "stack": "rb",
    "difficulty": 4,
    "title": "Настройки напоминаний из вебхука",
    "prompt": "Принимай настройки напоминаний вебхуком и накладывай их поверх дефолтных",
    "tests": ".......\n\nFinished in 0.042 seconds (files took 0.39 seconds to load)\n7 examples, 0 failures",
    "diff": "--- a/app/services/reminder_settings.rb\n+++ b/app/services/reminder_settings.rb\n@@ -4,17 +4,31 @@ class ReminderSettings\n \n   DEFAULTS = { channel: 'sms', hours_before: 24, enabled: true }.freeze\n \n-  def self.apply(clinic, payload)\n-    DEFAULTS\n+  def self.apply(clinic, payload)\n+    incoming = JSON.parse(payload)\n+\n+    settings = DEFAULTS.merge(incoming.symbolize_keys)\n+\n+    if settings[:hours_before].to_i <= 0\n+      settings[:hours_before] = DEFAULTS[:hours_before]\n+    end\n+\n+    clinic.update!(reminder_settings: settings)\n+\n+    settings\n+  end\n+\n+  def self.for(clinic)\n+    clinic.reminder_settings.presence || DEFAULTS\n   end\n end\n",
    "clean": false,
    "bugs": [
      {
        "file": "app/services/reminder_settings.rb",
        "kind": "wrong",
        "tag": "symbol-string-key",
        "explain": "Сохранили с символьными ключами, но из jsonb-колонки они возвращаются строками. Читающий код обращается к :channel и получает nil.",
        "consequence": "Настройки сохраняются и на экране выглядят применёнными, а рассыльщик берёт дефолты и шлёт смс за сутки вместо выбранных двух часов. Тест проверяет apply и его же возвращаемое значение, а обратное чтение из базы — нет.",
        "line": 22
      }
    ],
    "decoys": [
      {
        "why": "Полезная нагрузка вебхука сливается с дефолтами без белого списка полей — лишние ключи доедут до базы",
        "line": 10
      },
      {
        "why": "Разбор без обработки ошибки — битый вебхук поднимет исключение, но это как раз видно сразу",
        "line": 8
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "rs-blocking-in-async-001",
    "stack": "rs",
    "difficulty": 4,
    "title": "Выгрузка карточки в pdf",
    "prompt": "Отдавай карточку питомца в pdf, файл шаблона лежит рядом с бинарником",
    "tests": "running 3 tests\ntest export::tests::renders_card ... ok\ntest export::tests::uses_template ... ok\n\ntest result: ok. 3 passed; 0 failed; 0 ignored\n     Running `target/release/api`",
    "diff": "--- a/src/export.rs\n+++ b/src/export.rs\n@@ -8,15 +8,29 @@ use crate::card::Card;\n \n const TEMPLATE: &str = \"assets/card.tpl\";\n \n-pub async fn card_pdf(card: &Card) -> Result<Vec<u8>, ExportError> {\n-    Err(ExportError::NotImplemented)\n+pub async fn card_pdf(card: &Card) -> Result<Vec<u8>, ExportError> {\n+    let template = std::fs::read_to_string(TEMPLATE)?;\n+\n+    let filled = template\n+        .replace(\"{{name}}\", &card.name)\n+        .replace(\"{{owner}}\", &card.owner);\n+\n+    let bytes = render(&filled).await?;\n+\n+    if bytes.is_empty() {\n+        return Err(ExportError::EmptyRender);\n+    }\n+\n+    Ok(bytes)\n }\n \n async fn render(html: &str) -> Result<Vec<u8>, ExportError> {\n     renderer::html_to_pdf(html).await.map_err(ExportError::Render)\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/export.rs",
        "kind": "wrong",
        "tag": "blocking-in-async",
        "explain": "Синхронное чтение файла внутри async-функции блокирует поток исполнителя целиком — вместе со всеми задачами, которые на нём стоят.",
        "consequence": "Пока диск отвечает медленно, встают не только выгрузки, а вообще все запросы, попавшие на тот же поток. Выглядит как случайные таймауты в никак не связанных ручках — самый неприятный вид инцидента.",
        "line": 12
      }
    ],
    "decoys": [
      {
        "why": "Подстановка в шаблон без экранирования — кличка с угловыми скобками попортит разметку, но это не про блокировки",
        "line": 15
      },
      {
        "why": "Пустой результат считается ошибкой — спорное решение, зато явное",
        "line": 21
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "rs-errors-dropped-001",
    "stack": "rs",
    "difficulty": 2,
    "title": "Импорт прайса поставщика",
    "prompt": "Загрузи прайс из csv и верни позиции, битые строки не должны валить импорт",
    "tests": "running 4 tests\ntest price::tests::reads_all_rows ... ok\ntest price::tests::skips_header ... ok\n\ntest result: ok. 4 passed; 0 failed; 0 ignored",
    "diff": "--- a/src/price.rs\n+++ b/src/price.rs\n@@ -7,14 +7,27 @@ pub struct Item {\n     pub kop: i64,\n }\n \n-pub fn load(csv: &str) -> Vec<Item> {\n-    Vec::new()\n+pub fn load(csv: &str) -> Vec<Item> {\n+    let mut items: Vec<Item> = csv\n+        .lines()\n+        .skip(1)\n+        .map(parse_row)\n+        .filter_map(Result::ok)\n+        .collect();\n+\n+    items.sort_by(|a, b| a.sku.cmp(&b.sku));\n+    items\n }\n \n fn parse_row(line: &str) -> Result<Item, ParseError> {\n     let mut cells = line.split(';');\n \n     let sku = cells.next().ok_or(ParseError::Shape)?.to_string();\n     let kop = cells.next().ok_or(ParseError::Shape)?.parse()?;\n \n     Ok(Item { sku, kop })\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/price.rs",
        "kind": "wrong",
        "tag": "errors-dropped",
        "explain": "Битые строки не «не валят импорт», а бесследно исчезают: ни счётчика, ни строчки в логе. Отличить пустой прайс от полностью нечитаемого невозможно.",
        "consequence": "Поставщик сменил разделитель — импорт отработал «успешно» и оставил каталог с четырьмя позициями из шестисот. Заметят по выручке, а не по алерту.",
        "line": 15
      }
    ],
    "decoys": [
      {
        "why": "Заголовок отбрасывается по позиции, а не по содержимому — сломается, если поставщик уберёт шапку",
        "line": 13
      },
      {
        "why": "Сортировка строк по байтам, а не по локали — для артикулов это нормально",
        "line": 18
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "rs-index-out-of-range-001",
    "stack": "rs",
    "difficulty": 1,
    "title": "Инициалы владельца",
    "prompt": "Сделай инициалы владельца из фамилии и имени для шапки карточки",
    "tests": "running 4 tests\ntest initials::tests::two_words ... ok\ntest initials::tests::trims_spaces ... ok\n\ntest result: ok. 4 passed; 0 failed; 0 ignored",
    "diff": "--- a/src/initials.rs\n+++ b/src/initials.rs\n@@ -3,13 +3,25 @@\n use crate::owner::Owner;\n \n-pub fn initials(owner: &Owner) -> String {\n-    String::new()\n+/// «Иванов Пётр» → «И. П.»\n+pub fn initials(owner: &Owner) -> String {\n+    let parts: Vec<&str> = owner.full_name.split_whitespace().collect();\n+\n+    let last = parts[0];\n+    let first = parts[1];\n+\n+    format!(\n+        \"{}. {}.\",\n+        last.chars().next().unwrap_or(' '),\n+        first.chars().next().unwrap_or(' ')\n+    )\n }\n \n pub fn short_name(owner: &Owner) -> String {\n     owner.full_name.split_whitespace().next().unwrap_or(\"\").to_string()\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/initials.rs",
        "kind": "wrong",
        "tag": "index-out-of-range",
        "explain": "Обращение по индексу к вектору, в котором может быть одно слово. Соседние строки аккуратно используют unwrap_or, а здесь — прямой доступ.",
        "consequence": "Владелец, записанный одним словом — а таких в регистратуре хватает, — роняет отрисовку карточки паникой. Тесты гоняют только полные «Фамилия Имя».",
        "line": 10
      }
    ],
    "decoys": [
      {
        "why": "Пробел вместо буквы при пустой строке даст странные инициалы — некрасиво, но не падение",
        "line": 14
      },
      {
        "why": "Соседний метод делает то же самое безопасно — сравнение полезное, но баг не в нём",
        "line": 20
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "rs-integer-overflow-001",
    "stack": "rs",
    "difficulty": 3,
    "title": "Итог по чеку",
    "prompt": "Посчитай сумму чека в копейках с учётом количества и скидки",
    "tests": "running 6 tests\ntest bill::tests::sums_positions ... ok\ntest bill::tests::applies_discount ... ok\ntest bill::tests::empty_bill_is_zero ... ok\n\ntest result: ok. 6 passed; 0 failed; 0 ignored",
    "diff": "--- a/src/bill.rs\n+++ b/src/bill.rs\n@@ -5,15 +5,29 @@ pub struct Position {\n     pub qty: u32,\n }\n \n-pub fn total(positions: &[Position], discount_percent: u8) -> i32 {\n-    0\n+/// Итог в копейках. Скидка целыми процентами, как в прайсе клиники.\n+pub fn total(positions: &[Position], discount_percent: u8) -> i32 {\n+    let mut sum: i32 = 0;\n+\n+    for p in positions {\n+        sum += p.kop * p.qty as i32;\n+    }\n+\n+    let discount = sum / 100 * discount_percent as i32;\n+\n+    sum - discount\n }\n \n pub fn format(kop: i32) -> String {\n     format!(\"{},{:02} ₽\", kop / 100, kop % 100)\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/bill.rs",
        "kind": "wrong",
        "tag": "integer-overflow",
        "explain": "Копейки в i32: потолок — примерно двадцать один миллион рублей. В отладочной сборке это паника, в релизной — молчаливое переполнение в минус.",
        "consequence": "Оптовый счёт клинике на два десятка миллионов уходит с отрицательным итогом, и платёжка его принимает. Тесты считают чеки на пару тысяч рублей и до потолка не доходят никогда.",
        "line": 13
      }
    ],
    "decoys": [
      {
        "why": "Целочисленное деление до умножения теряет копейки на скидке — рубль туда-сюда, но не тот масштаб",
        "line": 16
      },
      {
        "why": "На отрицательной сумме формат даст минус в неожиданном месте — косметика вывода",
        "line": 22
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "rs-unwrap-panics-001",
    "stack": "rs",
    "difficulty": 1,
    "title": "Разбор веса из формы",
    "prompt": "Принимай вес питомца из формы и складывай в карточку, вес приходит строкой",
    "tests": "running 5 tests\ntest weight::tests::parses_integer ... ok\ntest weight::tests::parses_fraction ... ok\ntest weight::tests::rounds_to_grams ... ok\n\ntest result: ok. 5 passed; 0 failed; 0 ignored",
    "diff": "--- a/src/weight.rs\n+++ b/src/weight.rs\n@@ -4,12 +4,24 @@ use crate::card::Card;\n \n pub struct Weight(pub f64);\n \n-pub fn apply(card: &mut Card, raw: &str) {\n-    card.weight = None;\n+/// Вес приходит из формы строкой: «7», «7.4», иногда с пробелами по краям.\n+pub fn apply(card: &mut Card, raw: &str) {\n+    let cleaned = raw.trim().replace(',', \".\");\n+\n+    let kilos: f64 = cleaned.parse().unwrap();\n+\n+    card.weight = Some(Weight(round_to_grams(kilos)));\n+    card.weighed_at = Some(Utc::now());\n }\n \n+fn round_to_grams(kilos: f64) -> f64 {\n+    (kilos * 1000.0).round() / 1000.0\n+}\n+\n pub fn format(w: &Weight) -> String {\n     format!(\"{:.3} кг\", w.0)\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/weight.rs",
        "kind": "wrong",
        "tag": "unwrap-panics",
        "explain": "unwrap на данных из формы: любая непустая ерунда вроде «семь» или «7 кг» — это паника, а не ошибка валидации.",
        "consequence": "Регистратор опечатался в поле веса — и вместо подсветки поля получил пятисотку, а в проде это ещё и снятый с обслуживания поток воркера. Тесты кормят функцию только правильными строками, поэтому зелёные.",
        "line": 11
      }
    ],
    "decoys": [
      {
        "why": "Замена запятой на точку без локали — грубовато, но для веса работает",
        "line": 9
      },
      {
        "why": "Округление через умножение на тысячу копит погрешность f64 — заметно на очень больших числах, но не на весе кота",
        "line": 18
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sh-cd-unchecked-001",
    "stack": "sh",
    "difficulty": 4,
    "title": "Сборка отчёта в рабочем каталоге",
    "prompt": "Собери отчёт во временном каталоге и положи результат рядом с исходниками",
    "tests": "ok 1 - собирает отчёт\nok 2 - убирает временный каталог\nok 3 - возвращает код 0\n\n1..3\n# tests 3\n# pass  3",
    "diff": "--- a/scripts/build-report.sh\n+++ b/scripts/build-report.sh\n@@ -5,17 +5,32 @@\n set -uo pipefail\n \n ROOT=$(pwd)\n WORK=$(mktemp -d)\n \n cleanup() {\n   rm -rf \"$WORK\"\n }\n trap cleanup EXIT\n \n-echo \"сборка отключена\"\n+cd \"$WORK\"\n+\n+cp \"$ROOT\"/templates/*.tpl .\n+\n+report-gen --out report.pdf\n+\n+mv report.pdf \"$ROOT/build/\"\n \n exit 0\n",
    "clean": false,
    "bugs": [
      {
        "file": "scripts/build-report.sh",
        "kind": "wrong",
        "tag": "cd-unchecked",
        "explain": "set -e здесь нет — только -uo pipefail. Неудачный cd не остановит скрипт, и всё дальнейшее выполнится в каталоге запуска.",
        "consequence": "Шаблоны копируются в корень репозитория, отчёт собирается там же, а cleanup потом сносит временный каталог — не тот, где лежит мусор. На машине разработчика это грязный git status, на раннере — файлы, попавшие в артефакт сборки.",
        "line": 15
      }
    ],
    "decoys": [
      {
        "why": "Шаблон раскрывается оболочкой без кавычек по звёздочке — так и надо, иначе маска не сработает",
        "line": 17
      },
      {
        "why": "Ловушка только на EXIT, без сигналов — по Ctrl+C каталог останется, но это мелочь рядом с основным",
        "line": 13
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sh-force-push-main-001",
    "stack": "sh",
    "difficulty": 3,
    "title": "Скрипт выкладки релиза",
    "prompt": "Собери скрипт релиза: обнови версию, поставь тег и отправь всё в репозиторий",
    "tests": "$ ./scripts/release.sh 0.5.3\nверсия обновлена: 0.5.3\ntag v0.5.3 создан\nEverything up-to-date\n\n$ git tag --list 'v0.5.3'\nv0.5.3",
    "diff": "--- a/scripts/release.sh\n+++ b/scripts/release.sh\n@@ -1,6 +1,24 @@\n #!/usr/bin/env bash\n set -euo pipefail\n \n version=${1:?нужен номер версии}\n \n+git fetch origin main\n+git switch main\n+\n+npm version \"$version\" --no-git-tag-version\n+git add package.json package-lock.json\n+git commit -m \"chore(release): $version\"\n+\n+git tag \"v$version\"\n+\n+# отправляем ветку и тег\n+git push --force origin main\n+git push origin \"v$version\"\n+\n+echo \"версия обновлена: $version\"\n+echo \"tag v$version создан\"\n",
    "clean": false,
    "bugs": [
      {
        "file": "scripts/release.sh",
        "kind": "wrong",
        "tag": "force-push-shared-branch",
        "explain": "Ветка релиза общая. --force затирает на сервере всё, что появилось там после git fetch, — то есть чужие коммиты, влитые за последние секунды. Здесь --force не нужен вовсе, а если бы был нужен, то только --force-with-lease.",
        "consequence": "Коллега мёржит пул-реквест, пока идёт релиз, и его коммит исчезает с сервера. Локально он у автора остался, но узнают об этом через неделю по отсутствующей фиче, и восстанавливать историю приходится по рефлогам.",
        "line": 16
      }
    ],
    "decoys": [
      {
        "why": "тег без -a, то есть лёгкий, — вопрос вкуса, а не ошибка",
        "line": 13
      },
      {
        "why": "версия ставится без своего тега, тег ставим ниже сами — так и задумано",
        "line": 9
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sh-git-add-all-secrets-001",
    "stack": "sh",
    "difficulty": 3,
    "title": "Скрипт снятия дампа схемы",
    "prompt": "Пусть скрипт снимает схему базы и коммитит её, чтобы изменения схемы были видны в истории",
    "tests": "$ ./scripts/dump-schema.sh\nсхема снята: db/schema.sql (214 строк)\n[main 4c1e88a] chore(db): обновлена схема\n 1 file changed, 6 insertions(+), 2 deletions(-)\n\n$ git status\nnothing to commit, working tree clean",
    "diff": "--- a/scripts/dump-schema.sh\n+++ b/scripts/dump-schema.sh\n@@ -1,6 +1,24 @@\n #!/usr/bin/env bash\n set -euo pipefail\n \n source .env\n \n+pg_dump --schema-only --no-owner \"$DATABASE_URL\" > db/schema.sql\n+\n+lines=$(wc -l < db/schema.sql)\n+echo \"схема снята: db/schema.sql ($lines строк)\"\n+\n+git add -A\n+git commit -m \"chore(db): обновлена схема\"\n+\n+git push origin main\n",
    "clean": false,
    "bugs": [
      {
        "file": "scripts/dump-schema.sh",
        "kind": "wrong",
        "tag": "commit-everything-blindly",
        "explain": "add -A забирает всё, что оказалось в рабочей копии: чужие незакоммиченные правки, временные дампы и .env, если он не в .gitignore, — а скрипт двумя строками выше как раз показывает, что .env лежит рядом. Коммитить надо ровно тот файл, который сняли.",
        "consequence": "Строка подключения к проду с паролем уезжает в историю репозитория. Удалить её следующим коммитом мало: она остаётся в истории навсегда, и пароль придётся менять — вместе со всеми, кто его прописал у себя.",
        "line": 11
      }
    ],
    "decoys": [
      {
        "why": "чтение переменных из .env — обычный способ, вопрос не к нему",
        "line": 4
      },
      {
        "why": "только схема, без владельцев и данных — ровно то, что просили",
        "line": 6
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sh-git-rebase-published-001",
    "stack": "sh",
    "difficulty": 4,
    "title": "Автообновление веток от свежего main",
    "prompt": "Пусть бот раз в час подтягивает свежий main во все открытые ветки, чтобы они не отставали",
    "tests": "$ ./scripts/refresh-branches.sh\nветка feature/vaccine-plan: обновлена\nветка feature/stock-report: обновлена\nветка fix/owner-search: обновлена\n\nобновлено веток: 3, конфликтов: 0",
    "diff": "--- a/scripts/refresh-branches.sh\n+++ b/scripts/refresh-branches.sh\n@@ -1,6 +1,26 @@\n #!/usr/bin/env bash\n set -euo pipefail\n \n git fetch origin --prune\n \n+branches=$(git for-each-ref --format='%(refname:short)' refs/remotes/origin \\\n+  | grep -v 'origin/main' | sed 's|origin/||')\n+\n+for branch in $branches; do\n+  git switch \"$branch\"\n+  git rebase origin/main\n+  git push --force-with-lease origin \"$branch\"\n+  echo \"ветка $branch: обновлена\"\n+done\n+\n+git switch main\n+echo \"обновлено веток: $(echo \"$branches\" | wc -w), конфликтов: 0\"\n",
    "clean": false,
    "bugs": [
      {
        "file": "scripts/refresh-branches.sh",
        "kind": "wrong",
        "tag": "rebase-published-branch",
        "explain": "Rebase переписывает коммиты, а ветки опубликованы: над ними работают люди и висят пул-реквесты с комментариями к строкам. Ветку, которую видят другие, обновляют мёрджем, а не ребейзом — либо ребейзят её только те, кто в ней работает, и только осознанно.",
        "consequence": "У всех, кто работал с этими ветками, следующий git pull даёт расхождение и разбирается вручную. Комментарии ревьюеров в пул-реквестах отвязываются от строк и превращаются в «устаревшее», хотя обсуждение не закончено.",
        "line": 11
      }
    ],
    "decoys": [
      {
        "why": "с lease, а не голый --force — если бы ребейз был уместен, это правильная форма",
        "line": 12
      },
      {
        "why": "чистка исчезнувших веток — полезно и безопасно",
        "line": 4
      },
      {
        "why": "разбор списка веток текстом — хрупко, но работает",
        "line": 7
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sh-git-reset-hard-001",
    "stack": "sh",
    "difficulty": 2,
    "title": "Обновление рабочей копии на сервере",
    "prompt": "Сделай скрипт, который подтягивает свежий main на сервере перед перезапуском",
    "tests": "$ ./deploy/pull.sh\nиз origin/main: 3 коммита\nHEAD теперь на 9f2c1ab chore(release): 0.5.3\nсервисы перезапущены\n\n$ git status --porcelain\n(пусто)",
    "diff": "--- a/deploy/pull.sh\n+++ b/deploy/pull.sh\n@@ -1,5 +1,22 @@\n #!/usr/bin/env bash\n set -euo pipefail\n \n cd /srv/vet\n \n+git fetch origin main\n+\n+# приводим рабочую копию к состоянию origin\n+git reset --hard origin/main\n+\n+git log --oneline -1\n+\n+docker compose pull\n+docker compose up -d\n+\n+echo \"сервисы перезапущены\"\n",
    "clean": false,
    "bugs": [
      {
        "file": "deploy/pull.sh",
        "kind": "wrong",
        "tag": "reset-hard-drops-local-work",
        "explain": "reset --hard молча выбрасывает всё, что в рабочей копии отличается от origin: правку в конфиге, наспех поменянный лимит, файл, который кто-то оставил на сервере. Скрипт должен либо падать при грязной копии (git diff --quiet), либо не трогать её вовсе.",
        "consequence": "Однажды кто-то правит nginx.conf прямо на сервере, чтобы потушить инцидент, и уходит спать. Утренний деплой стирает правку, инцидент повторяется — и связать одно с другим не выходит, потому что в истории гита этой правки никогда не было.",
        "line": 9
      }
    ],
    "decoys": [
      {
        "why": "образы тянутся до перезапуска — правильный порядок",
        "line": 13
      },
      {
        "why": "fetch без merge — сам по себе безопасен",
        "line": 6
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sh-glob-unmatched-001",
    "stack": "sh",
    "difficulty": 1,
    "title": "Прогрев кэша после выкладки",
    "prompt": "После выкладки прогони прогревочные запросы из файлов в каталоге warmup",
    "tests": "ok 1 - дёргает все урлы\nok 2 - код возврата 0\n\n1..2\n# tests 2\n# pass  2",
    "diff": "--- a/scripts/warmup.sh\n+++ b/scripts/warmup.sh\n@@ -4,15 +4,28 @@\n #!/usr/bin/env bash\n set -euo pipefail\n \n BASE=${BASE:-https://clinic.example}\n WARMUP_DIR=${WARMUP_DIR:-warmup}\n \n-echo \"прогрев отключён\"\n+for file in \"$WARMUP_DIR\"/*.url; do\n+  url=$(cat \"$file\")\n+\n+  curl -fsS -o /dev/null \"$BASE$url\"\n+\n+  echo \"прогрет $url\"\n+done\n \n exit 0\n",
    "clean": false,
    "bugs": [
      {
        "file": "scripts/warmup.sh",
        "kind": "wrong",
        "tag": "glob-unmatched",
        "explain": "Если файлов нет, оболочка подставляет саму маску, и цикл выполнится один раз с несуществующим путём. Спасает nullglob или проверка на существование файла внутри цикла.",
        "consequence": "Пустой каталог прогрева не пропускает шаг, а роняет выкладку на cat: warmup/*.url: No such file. Ошибка выглядит как проблема с сетью, а причина в том, что каталог не доехал в образ.",
        "line": 10
      }
    ],
    "decoys": [
      {
        "why": "Ответ выбрасывается в /dev/null — так и надо, важен только сам поход",
        "line": 13
      },
      {
        "why": "Перевод строки в конце файла съедается подстановкой — здесь это работает в нашу пользу",
        "line": 11
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sh-pipefail-missing-001",
    "stack": "sh",
    "difficulty": 2,
    "title": "Проверка миграций перед деплоем",
    "prompt": "Перед деплоем проверь, что непрогнанных миграций нет, иначе останови выкладку",
    "tests": "ok 1 - падает на непрогнанной миграции\nok 2 - проходит на чистой базе\n\n1..2\n# tests 2\n# pass  2",
    "diff": "--- a/scripts/predeploy.sh\n+++ b/scripts/predeploy.sh\n@@ -3,16 +3,29 @@\n #!/usr/bin/env bash\n set -eu\n \n-echo \"проверок нет\"\n+DB_URL=${DB_URL:?нужен DB_URL}\n+\n+pending=$(migrate -database \"$DB_URL\" status | grep -c '^pending')\n+\n+if [ \"$pending\" -gt 0 ]; then\n+  echo \"непрогнанных миграций: $pending\" >&2\n+  exit 1\n+fi\n+\n+echo \"миграции в порядке\"\n \n exit 0\n",
    "clean": false,
    "bugs": [
      {
        "file": "scripts/predeploy.sh",
        "kind": "missing",
        "tag": "pipefail-missing",
        "explain": "В конвейере важен код возврата migrate, а без pipefail оболочка смотрит только на последнюю команду — на grep. Здесь нужен set -euo pipefail.",
        "consequence": "Упавший migrate отдаёт пустой вывод, grep честно считает ноль совпадений, проверка проходит — и выкладка идёт на базу без миграций. Тест подсовывает работающий migrate, поэтому оба случая зелёные.",
        "line": 4
      }
    ],
    "decoys": [
      {
        "why": "grep -c возвращает единицу, когда совпадений нет, — но это гасится подстановкой, а не проблемой строки",
        "line": 8
      },
      {
        "why": "Строка подключения приезжает из окружения и попадёт в вывод при ошибке — вопрос к секретам, не к проверке",
        "line": 6
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sh-rm-empty-var-001",
    "stack": "sh",
    "difficulty": 3,
    "title": "Чистка старых выгрузок",
    "prompt": "Раз в сутки чисти выгрузки старше недели, каталог задаётся переменной окружения",
    "tests": "ok 1 - удаляет файлы старше недели\nok 2 - не трогает свежие\nok 3 - пишет счётчик в лог\n\n1..3\n# tests 3\n# pass  3",
    "diff": "--- a/scripts/cleanup-exports.sh\n+++ b/scripts/cleanup-exports.sh\n@@ -4,16 +4,30 @@\n #!/usr/bin/env bash\n set -euo pipefail\n \n EXPORT_DIR=${EXPORT_DIR:-}\n KEEP_DAYS=${KEEP_DAYS:-7}\n \n-echo \"чистка выключена\"\n+before=$(find \"$EXPORT_DIR\" -type f | wc -l)\n+\n+find \"$EXPORT_DIR\"/ -type f -mtime +\"$KEEP_DAYS\" -delete\n+\n+after=$(find \"$EXPORT_DIR\" -type f | wc -l)\n+\n+echo \"удалено $((before - after)) файлов, осталось $after\"\n \n exit 0\n",
    "clean": false,
    "bugs": [
      {
        "file": "scripts/cleanup-exports.sh",
        "kind": "wrong",
        "tag": "rm-with-empty-var",
        "explain": "EXPORT_DIR по умолчанию пустая строка, а не путь. При незаданной переменной строка превращается в find / — обход всей файловой системы с удалением.",
        "consequence": "Забытая переменная в новом окружении означает не «скрипт ничего не сделал», а сутки восстановления сервера из бэкапа. Значение по умолчанию тут же, строкой выше, и выглядит безобидно.",
        "line": 12
      }
    ],
    "decoys": [
      {
        "why": "Каталог обходится трижды за запуск — расточительно, но безопасно",
        "line": 10
      },
      {
        "why": "Число дней подставляется в аргумент find без проверки — нечисловое значение уронит find с ошибкой, и это видно сразу",
        "line": 8
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sh-unquoted-expansion-001",
    "stack": "sh",
    "difficulty": 1,
    "title": "Выгрузка бэкапа на хранилище",
    "prompt": "Сложи вчерашние дампы в архив и отправь на хранилище, путь берётся из переменной",
    "tests": "ok 1 - собирает архив\nok 2 - пропускает пустой каталог\nok 3 - пишет имя архива в лог\n\n1..3\n# tests 3\n# pass  3",
    "diff": "--- a/scripts/upload-backup.sh\n+++ b/scripts/upload-backup.sh\n@@ -5,15 +5,28 @@ set -euo pipefail\n \n BACKUP_DIR=${BACKUP_DIR:-/var/backups/clinic}\n ARCHIVE=\"/tmp/clinic-$(date -u +%F).tar.gz\"\n \n-tar -czf \"$ARCHIVE\" \"$BACKUP_DIR\"\n+if [ ! -d \"$BACKUP_DIR\" ]; then\n+  echo \"нет каталога $BACKUP_DIR\" >&2\n+  exit 1\n+fi\n+\n+tar -czf $ARCHIVE \"$BACKUP_DIR\"\n+\n+echo \"собран $ARCHIVE\"\n+\n+rclone copy \"$ARCHIVE\" \"$REMOTE:clinic/\" --checksum\n \n rm -f \"$ARCHIVE\"\n",
    "clean": false,
    "bugs": [
      {
        "file": "scripts/upload-backup.sh",
        "kind": "wrong",
        "tag": "unquoted-expansion",
        "explain": "Единственная переменная без кавычек во всём файле. Оболочка разобьёт её по пробелам — путь с пробелом превратится в два аргумента.",
        "consequence": "Стоит хранилищу переехать в каталог с пробелом в имени — и tar создаст архив с обрезанным именем, а следом rm -f не найдёт его и оставит мусор в /tmp. Тест гоняется на пути без пробелов и остаётся зелёным.",
        "line": 14
      }
    ],
    "decoys": [
      {
        "why": "REMOTE нигде не задан по умолчанию — при set -u скрипт честно упадёт, и это заметно сразу",
        "line": 18
      },
      {
        "why": "Имя по дате перезапишет архив при повторном запуске в тот же день — спорно, но предсказуемо",
        "line": 7
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sql-boundary-001",
    "stack": "sql",
    "difficulty": 4,
    "title": "Отчёт за период",
    "prompt": "Добавь в отчёт фильтр по датам и пагинацию",
    "tests": "PASS  tests/report.test.ts (5)\n  ✓ отдаёт визиты за период\n  ✓ вторая страница не повторяет первую\n  ✓ пустой период отдаёт пусто\n\nTests  5 passed (5)",
    "diff": "--- a/src/report.sql\n+++ b/src/report.sql\n@@ -1,10 +1,26 @@\n -- Отчёт по визитам за период. Открывается из админки,\n -- :from и :to приходят из полей выбора даты как есть.\n \n SELECT v.id, v.created_at, p.name AS pet, o.name AS owner\n FROM visits v\n JOIN patients p ON p.id = v.patient_id\n JOIN owners o ON o.id = p.owner_id\n+WHERE v.clinic_id = :clinic\n+  AND v.created_at BETWEEN :from AND :to\n+  AND v.deleted_at IS NULL\n ORDER BY v.created_at\n+LIMIT :limit OFFSET :offset\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/report.sql",
        "kind": "wrong",
        "tag": "off-by-one-boundary",
        "explain": ":to приходит как дата без времени, то есть 2026-08-31 00:00:00. BETWEEN включает границу, но весь последний день периода в неё не попадает.",
        "consequence": "Месячный отчёт стабильно недосчитывает один день. Никто не замечает, пока бухгалтерия не сведёт цифры за год.",
        "line": 9
      }
    ],
    "decoys": [
      {
        "why": "сортировка без вторичного ключа — при равных датах порядок между страницами не гарантирован, но это другая ошибка",
        "line": 11
      },
      {
        "why": "мягко удалённые визиты отсеиваются — так и надо",
        "line": 10
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sql-clean-upsert-001",
    "stack": "sql",
    "difficulty": 4,
    "title": "Обновление остатков склада",
    "prompt": "Обнови остатки по приходу, а если позиции ещё нет — заведи её",
    "tests": "PASS  tests/stock.test.ts (5)\n  ✓ создаёт новую позицию\n  ✓ прибавляет к существующей\n  ✓ повторный прогон не задваивает\n\nTests  5 passed (5)",
    "diff": "--- a/src/stock_upsert.sql\n+++ b/src/stock_upsert.sql\n@@ -1,10 +1,26 @@\n -- Приход товара на склад клиники.\n -- Вызывается из обработчика накладной, по одной строке на позицию.\n \n+INSERT INTO stock (clinic_id, sku, quantity, updated_at)\n+VALUES (:clinic, :sku, :quantity, NOW())\n+ON CONFLICT (clinic_id, sku)\n+DO UPDATE SET\n+  quantity = stock.quantity + EXCLUDED.quantity,\n+  updated_at = EXCLUDED.updated_at\n+RETURNING clinic_id, sku, quantity;\n+\n+-- Движение пишем всегда, даже если позиция была заведена только что:\n+-- по этой таблице потом сходится инвентаризация.\n+INSERT INTO stock_moves (clinic_id, sku, delta, invoice_id, created_at)\n+VALUES (:clinic, :sku, :quantity, :invoice, NOW());\n",
    "clean": true,
    "bugs": [],
    "decoys": [
      {
        "why": "сложение выглядит как источник задвоения — но это приход, его и надо прибавлять, а конфликт ловится по ключу",
        "line": 8
      },
      {
        "why": "конфликт по паре колонок кажется хрупким — он и опирается на уникальный индекс, который для ON CONFLICT обязателен",
        "line": 6
      },
      {
        "why": "берётся время из вставляемой строки, а не NOW() заново — разницы нет, значение то же самое",
        "line": 9
      },
      {
        "why": "RETURNING тут не обязателен, но и не мешает",
        "line": 10
      },
      {
        "why": "две вставки без явной транзакции — вопрос справедливый, но в задаче про upsert",
        "line": 15
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sql-clean-window-001",
    "stack": "sql",
    "difficulty": 5,
    "title": "Последний визит каждого питомца",
    "prompt": "Для каждого питомца покажи его самый последний визит, по одной строке на питомца",
    "tests": "PASS  tests/last_visit.test.ts (6)\n  ✓ одна строка на питомца\n  ✓ берёт самый свежий визит\n  ✓ питомцы без визитов не попадают\n\nTests  6 passed (6)",
    "diff": "--- a/src/last_visit.sql\n+++ b/src/last_visit.sql\n@@ -1,3 +1,14 @@\n+WITH ranked AS (\n+  SELECT v.*,\n+    ROW_NUMBER() OVER (\n+      PARTITION BY v.pet_id\n+      ORDER BY v.started_at DESC, v.id DESC\n+    ) AS rn\n+  FROM visits v\n+  WHERE v.clinic_id = :clinic\n+)\n+SELECT pet_id, id AS visit_id, started_at\n+FROM ranked\n+WHERE rn = 1\n",
    "clean": true,
    "bugs": [],
    "decoys": [
      {
        "why": "ROW_NUMBER вместо RANK выглядит спорно — но нужна ровно одна строка, и RANK при равных датах вернул бы две",
        "line": 3
      },
      {
        "why": "вторичная сортировка кажется лишней — она как раз и делает результат детерминированным при совпадающих датах",
        "line": 5
      },
      {
        "why": "звёздочка в подзапросе — стилистически так себе, но снаружи колонки выбраны явно",
        "line": 2
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sql-count-inflate-001",
    "stack": "sql",
    "difficulty": 3,
    "title": "Счётчик визитов клиента",
    "prompt": "Добавь в список клиентов количество визитов и количество питомцев",
    "tests": "PASS  tests/clients.test.ts (6)\n  ✓ считает визиты\n  ✓ считает питомцев\n\nTests  6 passed (6)",
    "diff": "--- a/src/client_stats.sql\n+++ b/src/client_stats.sql\n@@ -1,12 +1,27 @@\n -- Карточка клиента в списке: сколько у него питомцев и сколько было визитов.\n \n-SELECT c.id, c.name\n+SELECT c.id, c.name, c.phone,\n+  COUNT(v.id) AS visits,\n+  COUNT(p.id) AS pets,\n+  MAX(v.started_at) AS last_visit\n FROM clients c\n+LEFT JOIN visits v ON v.client_id = c.id\n+LEFT JOIN pets p ON p.client_id = c.id\n WHERE c.clinic_id = :clinic\n+  AND c.deleted_at IS NULL\n GROUP BY c.id, c.name, c.phone\n ORDER BY c.name\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/client_stats.sql",
        "kind": "wrong",
        "tag": "count-inflated-by-join",
        "explain": "Два независимых JOIN дают декартово произведение: каждая строка визита умножается на число питомцев. Оба счётчика раздуваются, а не один.",
        "consequence": "У клиента с тремя питомцами и четырьмя визитами в отчёте будет двенадцать визитов и двенадцать питомцев. Цифры правдоподобные, поэтому проверять их никто не станет, а по ним считают нагрузку на клинику.",
        "line": 4
      }
    ],
    "decoys": [
      {
        "why": "группировка по двум полям вместо первичного ключа — многословно, но верно",
        "line": 12
      },
      {
        "why": "максимум по датам от размножения строк не страдает — единственная честная колонка здесь",
        "line": 6
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sql-group-by-missing-001",
    "stack": "sql",
    "difficulty": 2,
    "title": "Выручка по врачам",
    "prompt": "Посчитай выручку по каждому врачу за месяц",
    "tests": "PASS  tests/revenue.test.ts (4)\n  ✓ считает сумму по врачу\n  ✓ отдаёт имя врача\n\nTests  4 passed (4)",
    "diff": "--- a/src/revenue.sql\n+++ b/src/revenue.sql\n@@ -1,12 +1,26 @@\n -- Выручка по врачам за месяц. Читается из отчёта в админке.\n \n-SELECT d.id, SUM(i.total) AS revenue\n+SELECT d.id, d.name, d.clinic_id, SUM(i.total) AS revenue,\n+       COUNT(DISTINCT i.patient_id) AS patients\n FROM invoices i\n JOIN doctors d ON d.id = i.doctor_id\n+WHERE i.paid_at >= :month_start\n+  AND i.status = 'paid'\n GROUP BY d.id\n ORDER BY revenue DESC\n+LIMIT 100\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/revenue.sql",
        "kind": "wrong",
        "tag": "group-by-incomplete",
        "explain": "В выборке появились d.name и d.clinic_id, а группировка осталась только по d.id. MySQL в нестрогом режиме это проглотит и вернёт произвольное значение, PostgreSQL откажется выполнять запрос.",
        "consequence": "На дев-базе с ONLY_FULL_GROUP_BY выключенным всё работает, на проде с включённым запрос падает при первом же открытии отчёта. Либо, что хуже, не падает — и врач видит чужую клинику в своей строке.",
        "line": 9
      }
    ],
    "decoys": [
      {
        "why": "верхняя граница месяца не задана — отчёт захватит и следующий, но это другая ошибка",
        "line": 7
      },
      {
        "why": "DISTINCT здесь как раз к месту — без него пациенты бы задваивались",
        "line": 4
      },
      {
        "why": "сотня врачей на клинику — с запасом, но лимит без причины всё же лишний",
        "line": 11
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sql-implicit-cast-001",
    "stack": "sql",
    "difficulty": 3,
    "title": "Поиск по номеру карты",
    "prompt": "Добавь поиск пациента по номеру карты",
    "tests": "PASS  tests/search.test.ts (5)\n  ✓ находит по номеру\n  ✓ не находит чужую клинику\n\nTests  5 passed (5)",
    "diff": "--- a/src/find_pet.sql\n+++ b/src/find_pet.sql\n@@ -1,12 +1,27 @@\n -- Поиск пациента из строки в регистратуре.\n -- card_number хранится строкой: у части клиник в номере есть буквы.\n \n SELECT p.id, p.name, p.card_number, o.phone\n FROM pets p\n JOIN owners o ON o.id = p.owner_id\n-WHERE p.clinic_id = :clinic\n+WHERE p.clinic_id = :clinic\n+  AND p.archived_at IS NULL\n+  AND p.card_number = :query\n+ORDER BY p.name\n LIMIT 50\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/find_pet.sql",
        "kind": "wrong",
        "tag": "implicit-cast-kills-index",
        "explain": "card_number хранится строкой, а из формы приходит число. База приводит типы неявно и перестаёт использовать индекс по колонке.",
        "consequence": "Поиск по карте начинает делать полный проход по таблице визитов на несколько миллионов строк. На дев-базе в тысячу записей это незаметно, на проде регистратура жалуется, что поиск «думает» по десять секунд.",
        "line": 9
      }
    ],
    "decoys": [
      {
        "why": "лимит без сортировки даёт непредсказуемый набор — справедливо, но не про скорость",
        "line": 11
      },
      {
        "why": "архивных не показываем — правильно",
        "line": 8
      },
      {
        "why": "сортировка по кличке при поиске по номеру бессмысленна, но безвредна",
        "line": 10
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sql-left-join-where-001",
    "stack": "sql",
    "difficulty": 3,
    "title": "Клиенты без визитов",
    "prompt": "Покажи всех клиентов и их последний визит, включая тех, кто ещё не приходил",
    "tests": "PASS  tests/clients.test.ts (4)\n  ✓ отдаёт клиентов с визитами\n  ✓ фильтрует по клинике\n\nTests  4 passed (4)",
    "diff": "--- a/src/clients.sql\n+++ b/src/clients.sql\n@@ -1,12 +1,27 @@\n -- Список клиентов для отчёта по оттоку.\n -- Клиенты без визитов обязаны попадать в выборку — ради них отчёт и делается.\n \n SELECT c.id, c.name, c.created_at, v.started_at\n FROM clients c\n-JOIN visits v ON v.client_id = c.id\n+LEFT JOIN visits v ON v.client_id = c.id\n+WHERE v.clinic_id = :clinic\n+  AND c.deleted_at IS NULL\n ORDER BY v.started_at DESC\n+LIMIT 500\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/clients.sql",
        "kind": "wrong",
        "tag": "left-join-killed-by-where",
        "explain": "Условие на правую таблицу в WHERE отбрасывает строки, где визита нет: там v.clinic_id равен NULL. LEFT JOIN превращается обратно в INNER.",
        "consequence": "Запрос честно заменили на LEFT JOIN ровно ради клиентов без визитов — и именно они из выборки и пропали. Считают по нему отток и удивляются, что новых клиентов в отчёте ноль.",
        "line": 7
      }
    ],
    "decoys": [
      {
        "why": "сортировка без LIMIT по клиенту не даёт «последний визит» — отдельная недоделка, но не эта",
        "line": 9
      },
      {
        "why": "условие на левую таблицу — с ним LEFT JOIN ничего не теряет",
        "line": 8
      },
      {
        "why": "лимит без пагинации — отчёт обрежется молча, но это другая история",
        "line": 10
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sql-migration-backfill-lock-001",
    "stack": "sql",
    "difficulty": 5,
    "title": "Заполнить город у владельцев",
    "prompt": "У владельцев появился город. Заполни его из адреса для всех, у кого он пустой",
    "tests": "PASS  tests/backfill.test.ts (5)\n  ✓ город берётся из первой части адреса\n  ✓ пустой адрес оставляет null\n  ✓ повторный запуск ничего не меняет\n\nTests  5 passed (5)  ·  на тестовых данных: 40 строк, 12ms",
    "diff": "--- a/migrations/0044_owner_city_backfill.sql\n+++ b/migrations/0044_owner_city_backfill.sql\n@@ -1,3 +1,26 @@\n+-- Бэкфилл города. owners в проде — 118 тысяч строк,\n+-- таблица горячая: в неё пишет регистратура во время приёма.\n+\n+BEGIN;\n+\n+ALTER TABLE owners\n+  ADD COLUMN city text;\n+\n+UPDATE owners\n+   SET city = split_part(address, ',', 1)\n+ WHERE city IS NULL\n+   AND address IS NOT NULL;\n+\n+CREATE INDEX CONCURRENTLY IF NOT EXISTS owners_city_idx\n+  ON owners (city);\n+\n+COMMIT;\n",
    "clean": false,
    "bugs": [
      {
        "file": "migrations/0044_owner_city_backfill.sql",
        "kind": "wrong",
        "tag": "backfill-in-one-statement",
        "explain": "Один UPDATE на сто восемнадцать тысяч строк держит блокировки до конца транзакции и переписывает всю таблицу целиком. Бэкфилл гоняют батчами по несколько тысяч строк, отдельными транзакциями и вне миграции схемы.",
        "consequence": "На время бэкфилла регистратура не может сохранить ни одного владельца: запись встаёт в очередь за блокировкой. Приём идёт, люди в коридоре, а карточка не сохраняется — и никто не связывает это с выкаткой, потому что «мы же только колонку добавили».",
        "line": 9
      }
    ],
    "decoys": [
      {
        "why": "разбор адреса по запятой — грубо, но задача именно такая и была",
        "line": 10
      },
      {
        "why": "повторный запуск ничего не испортит — это как раз хорошо",
        "line": 11
      },
      {
        "why": "колонка без NOT NULL и без DEFAULT добавляется мгновенно — здесь всё правильно",
        "line": 7
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sql-migration-not-null-001",
    "stack": "sql",
    "difficulty": 3,
    "title": "Обязательный вид приёма",
    "prompt": "Добавь визитам вид приёма, он обязательный — первичный, повторный или вакцинация",
    "tests": "PASS  tests/migrations.test.ts (4)\n  ✓ миграция накатывается на пустую базу\n  ✓ колонка не принимает null\n  ✓ откат возвращает схему назад\n\nTests  4 passed (4)  ·  visits: 12 строк в тестовой базе",
    "diff": "--- a/migrations/0042_visit_kind.sql\n+++ b/migrations/0042_visit_kind.sql\n@@ -1,3 +1,24 @@\n+-- Вид приёма. В проде visits — самая большая таблица клиники,\n+-- на конец июля 4.1 млн строк.\n+\n+BEGIN;\n+\n+CREATE TYPE visit_kind AS ENUM ('first', 'repeat', 'vaccination');\n+\n+ALTER TABLE visits\n+  ADD COLUMN kind visit_kind NOT NULL;\n+\n+CREATE INDEX CONCURRENTLY IF NOT EXISTS visits_kind_idx\n+  ON visits (kind);\n+\n+COMMIT;\n",
    "clean": false,
    "bugs": [
      {
        "file": "migrations/0042_visit_kind.sql",
        "kind": "wrong",
        "tag": "not-null-without-default",
        "explain": "Добавить NOT NULL без DEFAULT в таблицу, где уже есть строки, нельзя: постгрес нечем заполнить существующие четыре миллиона. На пустой тестовой базе миграция проходит, на проде падает сразу. Делают в три шага: колонка с DEFAULT, бэкфилл батчами, потом ограничение.",
        "consequence": "Деплой останавливается на миграции, когда новый код уже выкачен и ждёт колонку. Прод лежит, пока кто-то вручную придумывает, чем заполнить историю за четыре года.",
        "line": 9
      }
    ],
    "decoys": [
      {
        "why": "перечисление вместо строки — спорно на будущее, но работает и не ломается",
        "line": 6
      },
      {
        "why": "миграция в транзакции — правильно, кроме одного нюанса ниже",
        "line": 4
      },
      {
        "why": "CONCURRENTLY внутри транзакции постгрес не разрешит — но это уже следствие, а не сама подлянка",
        "line": 11
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sql-migration-rename-column-001",
    "stack": "sql",
    "difficulty": 4,
    "title": "Переименование поля владельца",
    "prompt": "Переименуй owners.name в owners.full_name, а то путается с кличкой питомца",
    "tests": "PASS  tests/owners.test.ts (7)\n  ✓ владелец создаётся с full_name\n  ✓ поиск по фамилии работает\n  ✓ карточка отдаёт full_name\n\nTests  7 passed (7)  ·  миграция накатилась за 41ms",
    "diff": "--- a/migrations/0043_owner_full_name.sql\n+++ b/migrations/0043_owner_full_name.sql\n@@ -1,3 +1,20 @@\n+-- Переименование поля. Выкатка обычная: инстансы api обновляются\n+-- по одному, старые и новые какое-то время работают вместе.\n+\n+BEGIN;\n+\n+ALTER TABLE owners\n+  RENAME COLUMN name TO full_name;\n+\n+DROP INDEX IF EXISTS owners_name_idx;\n+\n+CREATE INDEX owners_full_name_idx\n+  ON owners (lower(full_name));\n+\n+COMMIT;\n",
    "clean": false,
    "bugs": [
      {
        "file": "migrations/0043_owner_full_name.sql",
        "kind": "wrong",
        "tag": "rename-breaks-running-code",
        "explain": "Переименование мгновенно ломает все инстансы, которые ещё работают на старом коде, — а во время выкатки они работают всегда. Так делают в три релиза: добавить новую колонку и писать в обе, перевести чтение, удалить старую.",
        "consequence": "На несколько минут выкатки половина запросов падает с «column owners.name does not exist». Откатить миграцию нельзя так же быстро, как код, поэтому окно растягивается до тех пор, пока не докатятся все инстансы.",
        "line": 7
      }
    ],
    "decoys": [
      {
        "why": "индекс по lower() под поиск без учёта регистра — правильно",
        "line": 11
      },
      {
        "why": "старый индекс убирают вместе с колонкой — само по себе не ошибка",
        "line": 9
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sql-not-in-null-001",
    "stack": "sql",
    "difficulty": 4,
    "title": "Услуги без назначений",
    "prompt": "Найди услуги, которые ни разу не назначали за год",
    "tests": "PASS  tests/services.test.ts (5)\n  ✓ отдаёт неиспользуемые услуги\n  ✓ учитывает границу года\n\nTests  5 passed (5)",
    "diff": "--- a/src/unused_services.sql\n+++ b/src/unused_services.sql\n@@ -1,11 +1,27 @@\n -- Услуги, которые за год ни разу не назначали.\n -- По отчёту решают, что выкинуть из прайса.\n \n SELECT s.id, s.name, s.price\n FROM services s\n+WHERE s.clinic_id = :clinic\n+  AND s.archived_at IS NULL\n+  AND s.id NOT IN (\n+    SELECT p.service_id FROM prescriptions p\n+    WHERE p.created_at >= :year_start\n+      AND p.clinic_id = :clinic\n+  )\n ORDER BY s.name\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/unused_services.sql",
        "kind": "wrong",
        "tag": "not-in-with-null",
        "explain": "Если в prescriptions.service_id встретится хоть один NULL, NOT IN даёт неизвестность вместо истины и выборка становится пустой.",
        "consequence": "Отчёт «неиспользуемые услуги» показывает ноль строк, и это выглядит как хорошая новость, а не как поломка. Ломается от одной осиротевшей записи, которую занесли год назад.",
        "line": 8
      }
    ],
    "decoys": [
      {
        "why": "подзапрос без DISTINCT — планировщику всё равно, на результат не влияет",
        "line": 9
      },
      {
        "why": "подзапрос ограничен той же клиникой — так и надо, иначе чужие назначения скрыли бы свои услуги",
        "line": 11
      },
      {
        "why": "архивные и так не в прайсе, фильтр лишний, но безобидный",
        "line": 7
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sql-select-star-001",
    "stack": "sql",
    "difficulty": 1,
    "title": "Список пациентов для мобильного",
    "prompt": "Отдай список пациентов для мобильного приложения",
    "tests": "PASS  tests/patients.test.ts (3)\n  ✓ отдаёт пациентов клиники\n  ✓ сортирует по кличке\n\nTests  3 passed (3)",
    "diff": "--- a/src/patients_api.sql\n+++ b/src/patients_api.sql\n@@ -1,12 +1,26 @@\n -- Список пациентов для мобильного приложения.\n -- Отдаётся как есть в JSON, поля наружу не фильтруются.\n \n-SELECT p.id, p.name\n-FROM pets p\n-WHERE p.clinic_id = :clinic\n+SELECT p.*, o.*\n+FROM pets p\n+JOIN owners o ON o.id = p.owner_id\n+WHERE p.clinic_id = :clinic\n+  AND p.archived_at IS NULL\n ORDER BY p.name\n+LIMIT :limit OFFSET :offset\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/patients_api.sql",
        "kind": "wrong",
        "tag": "select-star-leaks-columns",
        "explain": "Звёздочка по двум таблицам тянет наружу все колонки владельца — телефон, адрес, паспортные данные, если они там есть.",
        "consequence": "Мобильному приложению нужна кличка и вид животного, а в ответе API уезжают персональные данные владельцев. Любой, кто откроет devtools, увидит их целиком; при добавлении колонки в owners утечёт и она — молча.",
        "line": 4
      }
    ],
    "decoys": [
      {
        "why": "сортировка по кличке без учёта регистра и локали — косметика на фоне остального",
        "line": 9
      },
      {
        "why": "фильтр архивных добавлен верно, к нему претензий нет",
        "line": 8
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sql-transaction-missing-001",
    "stack": "sql",
    "difficulty": 4,
    "title": "Списание со склада при продаже",
    "prompt": "Списывай препарат со склада, когда пробили чек",
    "tests": "PASS  tests/sale.test.ts (6)\n  ✓ списывает остаток\n  ✓ пишет движение\n\nTests  6 passed (6)",
    "diff": "--- a/src/sell.sql\n+++ b/src/sell.sql\n@@ -1,10 +1,27 @@\n -- Продажа препарата с кассы. Вызывается из обработчика чека,\n -- по одному вызову на позицию в чеке.\n \n+UPDATE stock\n+SET quantity = quantity - :qty,\n+    updated_at = NOW()\n+WHERE clinic_id = :clinic AND sku = :sku;\n+\n+INSERT INTO stock_moves (clinic_id, sku, delta, receipt_id, created_at)\n+VALUES (:clinic, :sku, -:qty, :receipt, NOW());\n+\n+UPDATE receipts\n+SET items_count = items_count + 1\n+WHERE id = :receipt;\n",
    "clean": false,
    "bugs": [
      {
        "file": "src/sell.sql",
        "kind": "missing",
        "tag": "no-transaction-no-guard",
        "explain": "Два запроса идут без транзакции, и остаток уменьшается без проверки, что его хватает. Между ними может упасть что угодно, а quantity спокойно уходит в минус.",
        "consequence": "Падение между UPDATE и INSERT оставляет склад списанным без движения — сойтись инвентаризация уже не сможет. Параллельные продажи одного препарата уводят остаток в минус, и система продолжает продавать то, чего нет.",
        "line": 5
      }
    ],
    "decoys": [
      {
        "why": "NOW() вместо времени чека — расхождение в секунды, на фоне остального мелочь",
        "line": 10
      },
      {
        "why": "счётчик позиций инкрементом — при повторе обработки задвоится, но это следствие того же",
        "line": 13
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "sql-update-no-where-001",
    "stack": "sql",
    "difficulty": 1,
    "title": "Миграция статусов",
    "prompt": "Перенеси старый статус визита 'закрыт' в новое поле state",
    "tests": "PASS  tests/migrations.test.ts (3)\n  ✓ переносит закрытые визиты\n  ✓ миграция идемпотентна\n\nTests  3 passed (3)",
    "diff": "--- a/migrations/031_visit_state.sql\n+++ b/migrations/031_visit_state.sql\n@@ -1,8 +1,24 @@\n -- Переход со старого текстового status на нормализованный state.\n -- Старая колонка удаляется в 032, откатиться после неё будет нечем.\n \n ALTER TABLE visits ADD COLUMN state VARCHAR(16);\n \n CREATE INDEX idx_visits_state ON visits (clinic_id, state);\n+\n+UPDATE visits SET state = 'closed'\n+WHERE status = 'закрыт';\n+\n+UPDATE visits SET state = 'cancelled'\n+WHERE status = 'отменён';\n+\n+UPDATE visits SET state = 'open';\n",
    "clean": false,
    "bugs": [
      {
        "file": "migrations/031_visit_state.sql",
        "kind": "wrong",
        "tag": "update-without-where",
        "explain": "Второй UPDATE идёт без WHERE и переписывает вообще все строки, включая те, что первый только что пометил закрытыми.",
        "consequence": "После миграции все визиты за всю историю становятся открытыми. Откатить нечем: прошлое значение уже затёрто, а старая колонка в следующей миграции удаляется.",
        "line": 14
      }
    ],
    "decoys": [
      {
        "why": "колонка без NOT NULL и без индекса — вопросы к схеме, но данные не портит",
        "line": 4
      },
      {
        "why": "индекс строится до заполнения колонки — медленнее, чем после, но корректно",
        "line": 6
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "swift-float-money-001",
    "stack": "swift",
    "difficulty": 4,
    "title": "Итог по корзине услуг",
    "prompt": "Посчитай итог по выбранным услугам со скидкой и покажи в рублях",
    "tests": "Test Suite 'All tests' passed at 2026-08-02 12:15:09.673.\n\t Executed 9 tests, with 0 failures (0 unexpected) in 0.008 seconds",
    "diff": "--- a/Sources/Clinic/Cart.swift\n+++ b/Sources/Clinic/Cart.swift\n@@ -6,17 +6,31 @@ struct Service {\n     let priceKop: Int\n }\n \n struct Cart {\n \n     var services: [Service] = []\n     var discountPercent: Int = 0\n \n-    var totalKop: Int {\n-        services.reduce(0) { $0 + $1.priceKop }\n+    var total: Double {\n+        let sum = services.reduce(0.0) { $0 + Double($1.priceKop) / 100 }\n+        let discount = sum * Double(discountPercent) / 100\n+\n+        return sum - discount\n+    }\n+\n+    var formatted: String {\n+        String(format: \"%.2f ₽\", total)\n+    }\n+\n+    mutating func add(_ service: Service) {\n+        services.append(service)\n     }\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "Sources/Clinic/Cart.swift",
        "kind": "wrong",
        "tag": "float-money",
        "explain": "Копейки переводятся в рубли и складываются в Double. Погрешность двоичной дроби копится на каждой позиции, а итог был целым числом копеек до этой правки.",
        "consequence": "На корзине из десятка услуг со скидкой итог расходится с чеком на копейку. Касса не сходится в конце смены, и сверять руками будут не корзину, а платежи — искать станут не там, где сломано.",
        "line": 15
      }
    ],
    "decoys": [
      {
        "why": "Формат без локали и без разделителя разрядов — на больших суммах читается плохо, но точность не при чём",
        "line": 22
      },
      {
        "why": "Одну и ту же услугу можно добавить дважды — вопрос к продукту, не к деньгам",
        "line": 26
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "swift-force-unwrap-001",
    "stack": "swift",
    "difficulty": 1,
    "title": "Ссылка на карточку питомца",
    "prompt": "Открывай карточку питомца по ссылке из уведомления",
    "tests": "Test Suite 'All tests' passed at 2026-08-02 11:41:03.204.\n\t Executed 6 tests, with 0 failures (0 unexpected) in 0.019 seconds",
    "diff": "--- a/Sources/Clinic/DeepLink.swift\n+++ b/Sources/Clinic/DeepLink.swift\n@@ -7,15 +7,28 @@ enum DeepLink {\n \n     static let scheme = \"clinic\"\n \n-    static func card(for id: String) -> URL {\n-        URL(string: \"\\(scheme)://cards\")!\n+    static func card(for id: String) -> URL {\n+        let path = \"\\(scheme)://cards/\\(id)\"\n+\n+        return URL(string: path)!\n+    }\n+\n+    static func open(_ notification: [String: Any], with router: Router) {\n+        guard let id = notification[\"petId\"] as? String else {\n+            return\n+        }\n+\n+        router.open(card(for: id))\n     }\n \n     static func isCard(_ url: URL) -> Bool {\n         url.scheme == scheme && url.host == \"cards\"\n     }\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "Sources/Clinic/DeepLink.swift",
        "kind": "wrong",
        "tag": "force-unwrap",
        "explain": "Идентификатор приходит из уведомления и попадает в строку без экранирования. Пробел или кириллица — и URL получится nil, а восклицательный знак превращает это в падение приложения.",
        "consequence": "Одно кривое уведомление роняет приложение прямо на открытии — у всех, кому оно пришло. В отчётах о падениях это будет выглядеть как проблема роутера, а не бэкенда, который положил в пуш неожиданный идентификатор.",
        "line": 13
      }
    ],
    "decoys": [
      {
        "why": "Пуш без нужного поля молча игнорируется — стоило бы залогировать, но падения тут нет",
        "line": 17
      },
      {
        "why": "Сравнение схемы с учётом регистра — на практике схема всегда в нижнем",
        "line": 25
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "swift-index-out-of-range-001",
    "stack": "swift",
    "difficulty": 1,
    "title": "Ближайший приём на экране",
    "prompt": "Покажи на главном экране ближайший приём, если он есть",
    "tests": "Test Suite 'All tests' passed at 2026-08-02 12:31:55.402.\n\t Executed 5 tests, with 0 failures (0 unexpected) in 0.006 seconds",
    "diff": "--- a/Sources/Clinic/NextVisit.swift\n+++ b/Sources/Clinic/NextVisit.swift\n@@ -5,15 +5,28 @@ struct NextVisit {\n \n     let visits: [Visit]\n \n-    var title: String {\n-        \"Приёмов нет\"\n+    var upcoming: [Visit] {\n+        visits\n+            .filter { $0.at > .now }\n+            .sorted { $0.at < $1.at }\n+    }\n+\n+    var title: String {\n+        let next = upcoming[0]\n+\n+        return \"\\(next.petName) — \\(next.at.formatted(date: .omitted, time: .shortened))\"\n+    }\n+\n+    var hasUpcoming: Bool {\n+        !upcoming.isEmpty\n     }\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "Sources/Clinic/NextVisit.swift",
        "kind": "wrong",
        "tag": "index-out-of-range",
        "explain": "Обращение по индексу к массиву, который бывает пустым. Рядом есть hasUpcoming, но title его не спрашивает — здесь нужен first.",
        "consequence": "Как только приёмов на сегодня не осталось — а это каждый вечер, — главный экран падает при отрисовке. Тесты передают непустой список, потому что писались вместе с happy path.",
        "line": 15
      }
    ],
    "decoys": [
      {
        "why": "Момент «сейчас» берётся при каждом обращении — свойство пересчитывается чаще, чем нужно",
        "line": 10
      },
      {
        "why": "Проверка пересобирает и сортирует весь список ради одного вопроса — расточительно, но верно",
        "line": 21
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "swift-off-main-thread-001",
    "stack": "swift",
    "difficulty": 3,
    "title": "Загрузка фото питомца",
    "prompt": "Подгружай фото питомца в фоне и показывай, когда придёт",
    "tests": "Test Suite 'All tests' passed at 2026-08-02 12:03:44.117.\n\t Executed 5 tests, with 0 failures (0 unexpected) in 0.211 seconds",
    "diff": "--- a/Sources/Clinic/PetPhotoLoader.swift\n+++ b/Sources/Clinic/PetPhotoLoader.swift\n@@ -8,16 +8,30 @@ final class PetPhotoLoader: ObservableObject {\n \n     @Published var image: UIImage?\n     @Published var isLoading = false\n \n     private let session: URLSession\n \n-    func load(_ url: URL) {\n-        isLoading = true\n+    func load(_ url: URL) {\n+        isLoading = true\n+\n+        session.dataTask(with: url) { data, _, _ in\n+            guard let data, let loaded = UIImage(data: data) else {\n+                return\n+            }\n+\n+            self.image = loaded\n+\n+            DispatchQueue.main.async {\n+                self.isLoading = false\n+            }\n+        }\n+        .resume()\n     }\n \n     func reset() {\n         image = nil\n         isLoading = false\n     }\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "Sources/Clinic/PetPhotoLoader.swift",
        "kind": "wrong",
        "tag": "off-main-thread-ui",
        "explain": "Замыкание dataTask выполняется на фоновой очереди, а публикуемое свойство, на которое подписан экран, обновляется прямо из неё. Соседняя строка про isLoading переключение на главный поток делает — это про image забыли.",
        "consequence": "Интерфейс обновляется не из главного потока: обычно проносит, иногда картинка не появляется до следующего касания, а в сборке с проверками — падение с «Publishing changes from background threads». Воспроизводится через раз, поэтому чинить будут долго.",
        "line": 22
      }
    ],
    "decoys": [
      {
        "why": "Ошибка загрузки молча оставляет isLoading включённым — крутилка навсегда, но это другая беда",
        "line": 18
      },
      {
        "why": "Флаг ставится синхронно до запроса — здесь это как раз главный поток",
        "line": 15
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "swift-retain-cycle-001",
    "stack": "swift",
    "difficulty": 2,
    "title": "Обновление списка приёмов",
    "prompt": "Пусть список приёмов сам обновляется, когда приходит пуш об изменении расписания",
    "tests": "Test Suite 'All tests' passed at 2026-08-02 11:52:10.882.\n\t Executed 8 tests, with 0 failures (0 unexpected) in 0.044 seconds",
    "diff": "--- a/Sources/Clinic/VisitListViewModel.swift\n+++ b/Sources/Clinic/VisitListViewModel.swift\n@@ -9,16 +9,30 @@ final class VisitListViewModel: ObservableObject {\n \n     @Published private(set) var visits: [Visit] = []\n \n     private let api: API\n     private let center: NotificationCenter\n \n-    init(api: API, center: NotificationCenter = .default) {\n-        self.api = api\n-        self.center = center\n+    init(api: API, center: NotificationCenter = .default) {\n+        self.api = api\n+        self.center = center\n+\n+        center.addObserver(forName: .scheduleChanged, object: nil, queue: .main) { _ in\n+            self.reload()\n+        }\n+    }\n+\n+    func reload() {\n+        Task {\n+            visits = (try? await api.visits()) ?? visits\n+        }\n     }\n \n     deinit {\n         center.removeObserver(self)\n     }\n }\n",
    "clean": false,
    "bugs": [
      {
        "file": "Sources/Clinic/VisitListViewModel.swift",
        "kind": "wrong",
        "tag": "retain-cycle",
        "explain": "Замыкание наблюдателя сильно держит self, а сам наблюдатель живёт в центре уведомлений — модель уже не освободится. Нужен [weak self].",
        "consequence": "Экран закрыли, а модель осталась и продолжает ходить в сеть на каждый пуш. Через десяток открытий списка запрос уходит десять раз, deinit не вызывается никогда — и снять наблюдателя тоже уже некому.",
        "line": 20
      }
    ],
    "decoys": [
      {
        "why": "Ошибка запроса молча оставляет старые данные — пользователю бы показать, что обновить не удалось",
        "line": 26
      },
      {
        "why": "Снятие наблюдателя по объекту, а не по возвращённому токену — на блочной подписке это не работает, но проблема начинается раньше",
        "line": 31
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "yaml-cache-key-without-lock-001",
    "stack": "yaml",
    "difficulty": 2,
    "title": "Кэш зависимостей в пайплайне",
    "prompt": "Ускорь пайплайн, зависимости качаются по две минуты каждый раз",
    "tests": "✓ checks / test (pull_request)  Successful in 51s\n  Cache restored from key: deps-ubuntu-24.04\n  → npm ci: 6s (было 2m 11s)\n\nAll checks have passed",
    "diff": "--- a/.github/workflows/checks.yml\n+++ b/.github/workflows/checks.yml\n@@ -1,12 +1,30 @@\n name: checks\n \n on:\n   pull_request:\n \n jobs:\n   test:\n     runs-on: ubuntu-24.04\n     steps:\n       - uses: actions/checkout@v4\n       - uses: actions/setup-node@v4\n         with:\n           node-version: '22.11'\n+      - name: cache deps\n+        uses: actions/cache@v4\n+        with:\n+          path: ~/.npm\n+          key: deps-${{ runner.os }}\n+          restore-keys: |\n+            deps-\n+      - run: npm ci\n+      - run: npm test\n",
    "clean": false,
    "bugs": [
      {
        "file": ".github/workflows/checks.yml",
        "kind": "wrong",
        "tag": "cache-key-ignores-lockfile",
        "explain": "Ключ не зависит от содержимого package-lock.json, поэтому кэш не обновляется никогда: первый сохранённый архив продолжает восстанавливаться и после смены зависимостей. В ключ подмешивают hashFiles('**/package-lock.json').",
        "consequence": "Обновили библиотеку, влили, на CI всё зелёное — потому что там стоит старая версия из кэша недельной давности. Ломается это на проде, где зависимости ставятся честно.",
        "line": 18
      }
    ],
    "decoys": [
      {
        "why": "кэшируется каталог npm, а не node_modules — так и рекомендуется",
        "line": 17
      },
      {
        "why": "restore-keys как запасной вариант — нормальная практика, проблема не в нём",
        "line": 20
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "yaml-deploy-on-any-branch-001",
    "stack": "yaml",
    "difficulty": 3,
    "title": "Автовыкладка на сервер",
    "prompt": "Сделай автодеплой: когда изменения попадают в main, выкладываем на сервер",
    "tests": "✓ deploy / release (push)  Successful in 2m 04s\n  → vet-api:0.5.2 pushed to registry\n  → docker compose up -d on vet-prod\n\nDeployment succeeded",
    "diff": "--- a/.github/workflows/deploy.yml\n+++ b/.github/workflows/deploy.yml\n@@ -1,8 +1,30 @@\n name: deploy\n \n on:\n   push:\n+    tags:\n+      - 'v*'\n+  workflow_dispatch:\n \n+concurrency:\n+  group: deploy-prod\n+  cancel-in-progress: false\n+\n jobs:\n   release:\n     runs-on: ubuntu-24.04\n+    environment: production\n+    steps:\n+      - uses: actions/checkout@v4\n+      - name: build image\n+        run: docker build -t vet-api:${{ github.ref_name }} .\n+      - name: push\n+        run: docker push vet-api:${{ github.ref_name }}\n+      - name: up\n+        run: ssh deploy@vet-prod 'cd /srv/vet && docker compose up -d'\n",
    "clean": false,
    "bugs": [
      {
        "file": ".github/workflows/deploy.yml",
        "kind": "wrong",
        "tag": "deploy-trigger-mismatch",
        "explain": "Просили выкладывать то, что попало в main, а триггер стоит на теги. Пуш в main деплой не запускает вовсе — выкладка происходит, только когда кто-то вручную поставит тег. Работает не то, что заказывали.",
        "consequence": "Команда неделю уверена, что main автоматически едет в прод, и никто не проверяет сервер. Расхождение находят, когда срочный фикс «уже выкачен» два дня как, а на проде старая версия.",
        "line": 5
      }
    ],
    "decoys": [
      {
        "why": "одна выкладка за раз — это правильно, две параллельные хуже",
        "line": 10
      },
      {
        "why": "деплой через ssh и compose — грубовато, но это наш способ, см. deploy/",
        "line": 23
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "yaml-npm-install-not-ci-001",
    "stack": "yaml",
    "difficulty": 1,
    "title": "Прогон линтера на пуше",
    "prompt": "Добавь в CI шаг с линтером, чтобы стиль не разъезжался",
    "tests": "✓ lint / oxlint (push)  Successful in 22s\n  → 0 warnings, 0 errors\n\nAll checks have passed",
    "diff": "--- a/.github/workflows/lint.yml\n+++ b/.github/workflows/lint.yml\n@@ -1,6 +1,22 @@\n name: lint\n \n on:\n   push:\n \n jobs:\n   oxlint:\n     runs-on: ubuntu-24.04\n     steps:\n       - uses: actions/checkout@v4\n+      - uses: actions/setup-node@v4\n+        with:\n+          node-version: '22.11'\n+          cache: npm\n+      - name: deps\n+        run: npm install\n+      - name: lint\n+        run: npm run lint\n",
    "clean": false,
    "bugs": [
      {
        "file": ".github/workflows/lint.yml",
        "kind": "wrong",
        "tag": "ci-install-rewrites-lock",
        "explain": "npm install имеет право обновить версии в пределах диапазонов и переписать package-lock.json. На CI ставят npm ci — он ставит ровно то, что в лок-файле, и падает, если лок разошёлся с package.json.",
        "consequence": "Проверка идёт на других версиях, чем прод, — и однажды зелёный CI сопровождает сборку, которая на сервере не поднимается. Разбираться приходится сравнением деревьев зависимостей, а не кодом.",
        "line": 16
      }
    ],
    "decoys": [
      {
        "why": "кэш зависимостей от setup-node — нормально, ключ он считает сам по лок-файлу",
        "line": 14
      },
      {
        "why": "линтер на каждый пуш, а не только на PR — это дороже, но не ошибка",
        "line": 4
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "yaml-pr-target-checkout-001",
    "stack": "yaml",
    "difficulty": 5,
    "title": "Бот с покрытием в пул-реквестах",
    "prompt": "Пусть бот пишет в пул-реквест комментарий с покрытием тестов, в том числе на внешние PR",
    "tests": "✓ coverage / report (pull_request_target)  Successful in 1m 07s\n  → покрытие 74.2% (+0.8%)\n  → комментарий обновлён в PR #218\n\nAll checks have passed",
    "diff": "--- a/.github/workflows/coverage.yml\n+++ b/.github/workflows/coverage.yml\n@@ -1,10 +1,32 @@\n name: coverage\n \n on:\n-  pull_request:\n+  pull_request_target:\n \n permissions:\n   pull-requests: write\n \n jobs:\n   report:\n     runs-on: ubuntu-24.04\n     steps:\n+      - uses: actions/checkout@v4\n+        with:\n+          ref: ${{ github.event.pull_request.head.sha }}\n+      - uses: actions/setup-node@v4\n+        with:\n+          node-version: '22.11'\n+      - run: npm ci\n+      - run: npm run coverage\n+      - name: comment\n+        uses: actions/github-script@v7\n+        with:\n+          script: |\n+            const body = require('fs').readFileSync('coverage/summary.md', 'utf8')\n+            await github.rest.issues.createComment({ ...context.repo, issue_number: context.issue.number, body })\n",
    "clean": false,
    "bugs": [
      {
        "file": ".github/workflows/coverage.yml",
        "kind": "wrong",
        "tag": "untrusted-code-with-write-token",
        "explain": "pull_request_target выполняется в контексте базовой ветки — с правами на запись и доступом к секретам. Выкачивать сюда код из чужого пул-реквеста и запускать его npm ci означает отдать этот токен автору PR: любой хук установки выполнит что угодно. Либо checkout базовой ветки, либо обычный pull_request без прав на запись.",
        "consequence": "Первый же посторонний пул-реквест с постустановочным скриптом получает токен с правом записи в репозиторий. Понять, что произошло, можно только по логам аудита, и то если знать, что искать.",
        "line": 15
      }
    ],
    "decoys": [
      {
        "why": "право писать комментарии боту действительно нужно, само по себе оно не ошибка",
        "line": 7
      },
      {
        "why": "официальное действие с закреплённой мажорной версией — обычная практика",
        "line": 22
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "yaml-secret-through-base64-001",
    "stack": "yaml",
    "difficulty": 4,
    "title": "Ключ подписи в сборке приложения",
    "prompt": "Нужно подписывать сборку, положи ключ в секреты и достань его в пайплайне",
    "tests": "✓ release / sign (workflow_dispatch)  Successful in 1m 33s\n  → keystore восстановлен, размер 2.3KB\n  → apk подписан, verify: ok\n\nDeployment succeeded",
    "diff": "--- a/.github/workflows/release.yml\n+++ b/.github/workflows/release.yml\n@@ -1,10 +1,28 @@\n name: release\n \n on:\n   workflow_dispatch:\n \n jobs:\n   sign:\n     runs-on: ubuntu-24.04\n     steps:\n       - uses: actions/checkout@v4\n+      - name: restore keystore\n+        run: |\n+          echo \"${{ secrets.KEYSTORE_B64 }}\" | base64 -d > release.keystore\n+          echo \"восстановлен, размер $(stat -c%s release.keystore)\"\n+      - name: sign\n+        env:\n+          STORE_PASSWORD: ${{ secrets.STORE_PASSWORD }}\n+        run: ./gradlew assembleRelease\n+      - name: cleanup\n+        if: always()\n+        run: rm -f release.keystore\n",
    "clean": false,
    "bugs": [
      {
        "file": ".github/workflows/release.yml",
        "kind": "wrong",
        "tag": "secret-unmasked-in-log",
        "explain": "Раннер заменяет в логе только точное значение секрета. Подстановка ${{ secrets.X }} прямо в команду — это ещё и способ получить его в логе через любое преобразование: достаточно ошибки в пайпе, и base64 напечатает вход. Секреты передают через env, а не подставляют в текст команды.",
        "consequence": "Ключ подписи утекает в лог публичного пайплайна и живёт там, пока кто-нибудь не заметит. Отзыв ключа означает перевыпуск и новую подпись у всех, кто уже поставил приложение.",
        "line": 13
      }
    ],
    "decoys": [
      {
        "why": "а вот так — правильно: секрет уходит в env шага, а не в текст команды",
        "line": 17
      },
      {
        "why": "удаление ключа при любом исходе — как раз то, что нужно",
        "line": 20
      }
    ],
    "verified_by": "",
    "verified_at": ""
  },
  {
    "id": "yaml-tests-never-fail-001",
    "stack": "yaml",
    "difficulty": 2,
    "title": "Тесты на пул-реквестах",
    "prompt": "Пусть на каждый пул-реквест гоняются тесты, а результат виден в PR",
    "tests": "✓ checks / test (pull_request)  Successful in 48s\n✓ checks / build (pull_request)  Successful in 1m 12s\n\nAll checks have passed",
    "diff": "--- a/.github/workflows/checks.yml\n+++ b/.github/workflows/checks.yml\n@@ -1,10 +1,29 @@\n name: checks\n \n on:\n   pull_request:\n \n jobs:\n   test:\n     runs-on: ubuntu-24.04\n     steps:\n       - uses: actions/checkout@v4\n+      - uses: actions/setup-node@v4\n+        with:\n+          node-version: '22.11'\n+          cache: npm\n+      - run: npm ci\n+      - name: unit\n+        continue-on-error: true\n+        run: npm test\n+      - name: summary\n+        if: always()\n+        run: echo \"тесты отработали\" >> $GITHUB_STEP_SUMMARY\n+\n+  build:\n+    runs-on: ubuntu-24.04\n+    steps:\n+      - uses: actions/checkout@v4\n+      - run: npm ci && npm run build\n",
    "clean": false,
    "bugs": [
      {
        "file": ".github/workflows/checks.yml",
        "kind": "wrong",
        "tag": "ci-step-cannot-fail",
        "explain": "Шаг с continue-on-error не роняет джобу. Тесты гоняются, красные тесты честно печатаются в лог — и проверка всё равно зелёная. Галочка в пул-реквесте перестаёт что-либо значить.",
        "consequence": "Красные тесты едут в main месяцами, потому что никто не открывает лог зелёной проверки. Обнаруживается это в тот день, когда кто-то наконец запускает тесты локально — и видит, что их там сорок штук падает.",
        "line": 17
      }
    ],
    "decoys": [
      {
        "why": "шаг сводки должен выполняться и при падении — здесь always() как раз к месту",
        "line": 20
      },
      {
        "why": "сборка отдельной джобой без кэша — медленно, но не ошибка",
        "line": 27
      }
    ],
    "verified_by": "",
    "verified_at": ""
  }
]
