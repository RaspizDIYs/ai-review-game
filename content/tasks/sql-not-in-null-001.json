{
  "id": "sql-not-in-null-001",
  "stack": "sql",
  "difficulty": 4,
  "title": "Услуги без назначений",
  "prompt": "Найди услуги, которые ни разу не назначали за год",
  "tests": "PASS  tests/services.test.ts (5)\n  ✓ отдаёт неиспользуемые услуги\n  ✓ учитывает границу года\n\nTests  5 passed (5)",
  "diff": "--- a/src/unused_services.sql\n+++ b/src/unused_services.sql\n@@ -1,11 +1,27 @@\n -- Услуги, которые за год ни разу не назначали.\n -- По отчёту решают, что выкинуть из прайса.\n \n SELECT s.id, s.name, s.price\n FROM services s\n+WHERE s.clinic_id = :clinic\n+  AND s.archived_at IS NULL\n+  AND s.id NOT IN (\n+    SELECT p.service_id FROM prescriptions p\n+    WHERE p.created_at >= :year_start\n+      AND p.clinic_id = :clinic\n+  )\n ORDER BY s.name\n",
  "clean": false,
  "bugs": [
    {
      "file": "src/unused_services.sql",
      "match": "  AND s.id NOT IN (",
      "kind": "wrong",
      "tag": "not-in-with-null",
      "explain": "Если в prescriptions.service_id встретится хоть один NULL, NOT IN даёт неизвестность вместо истины и выборка становится пустой.",
      "consequence": "Отчёт «неиспользуемые услуги» показывает ноль строк, и это выглядит как хорошая новость, а не как поломка. Ломается от одной осиротевшей записи, которую занесли год назад."
    }
  ],
  "decoys": [
    {
      "match": "  SELECT p.service_id FROM prescriptions p",
      "why": "подзапрос без DISTINCT — планировщику всё равно, на результат не влияет"
    },
    {
      "match": "      AND p.clinic_id = :clinic",
      "why": "подзапрос ограничен той же клиникой — так и надо, иначе чужие назначения скрыли бы свои услуги"
    },
    {
      "match": "  AND s.archived_at IS NULL",
      "why": "архивные и так не в прайсе, фильтр лишний, но безобидный"
    }
  ],
  "verified_by": "",
  "verified_at": ""
}
