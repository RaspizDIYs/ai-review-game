{
  "id": "py-assert-true-001",
  "stack": "py",
  "difficulty": 1,
  "title": "Тест расчёта дозы",
  "prompt": "Покрой тестом расчёт дозы по весу",
  "tests": "==== test session starts ====\ncollected 3 items\n\ntests/test_dosage.py ...              [100%]\n\n==== 3 passed in 0.08s ====",
  "diff": "--- a/tests/test_dosage.py\n+++ b/tests/test_dosage.py\n@@ -1,14 +1,31 @@\n import pytest\n \n from dosage import calculate, MAX_SINGLE_DOSE\n \n \n+def test_dosage_for_small_pet():\n+    result = calculate(weight_kg=3.5, drug=\"amoxicillin\")\n+    assert result is not None\n+\n+\n+def test_dosage_for_large_pet():\n+    result = calculate(weight_kg=42.0, drug=\"amoxicillin\")\n+    assert result\n+\n+\n+def test_dosage_is_capped():\n+    result = calculate(weight_kg=120.0, drug=\"amoxicillin\")\n+    assert result <= MAX_SINGLE_DOSE\n+\n+\n+def test_unknown_drug_raises():\n+    with pytest.raises(KeyError):\n+        calculate(weight_kg=5.0, drug=\"нет-такого\")\n",
  "clean": false,
  "bugs": [
    {
      "file": "tests/test_dosage.py",
      "match": "    assert result is not None",
      "kind": "wrong",
      "tag": "assert-without-expectation",
      "explain": "Тест проверяет, что функция что-то вернула, но не проверяет, что именно. Любое число пройдёт.",
      "consequence": "Ошибка в формуле дозы не будет поймана вообще: тест зелёный при любом результате. Здесь это не абстрактный риск — по этой цифре препарат назначают животному."
    }
  ],
  "decoys": [
    {
      "match": "def test_dosage_for_large_pet():",
      "why": "второй тест по сути ровно такой же — но подлянку достаточно отметить один раз"
    },
    {
      "match": "    assert result <= MAX_SINGLE_DOSE",
      "why": "вот этот тест проверяет настоящее свойство — на него как раз претензий нет"
    },
    {
      "match": "    with pytest.raises(KeyError):",
      "why": "KeyError наружу вместо своего исключения — вопрос к коду, а не к тесту"
    }
  ],
  "verified_by": "",
  "verified_at": ""
}
