{
  "id": "py-generator-consumed-001",
  "stack": "py",
  "difficulty": 5,
  "title": "Отчёт по движению склада",
  "prompt": "Посчитай по движениям склада и сумму, и количество позиций",
  "tests": "==== test session starts ====\ncollected 5 items\n\ntests/test_stock_report.py .....      [100%]\n\n==== 5 passed in 0.21s ====",
  "diff": "--- a/stock_report.py\n+++ b/stock_report.py\n@@ -3,7 +3,14 @@\n def build_report(clinic_id):\n-    moves = list(fetch_moves(clinic_id))\n+    moves = (m for m in fetch_moves(clinic_id) if m.delta != 0)\n+\n+    total = sum(m.delta * m.price for m in moves)\n+    positions = len({m.sku for m in moves})\n+\n+    return Report(total=total, positions=positions)\n",
  "clean": false,
  "bugs": [
    {
      "file": "stock_report.py",
      "match": "    positions = len({m.sku for m in moves})",
      "kind": "wrong",
      "tag": "generator-consumed-twice",
      "explain": "moves теперь генератор, а не список. Первый проход его исчерпывает, второй получает пустоту.",
      "consequence": "Сумма считается верно, а число позиций всегда ноль. Ноль выглядит как «движений не было», а не как поломка, поэтому отчёт неделями считают правдой и по нему решают, что закупать."
    }
  ],
  "decoys": [
    {
      "match": "    moves = (m for m in fetch_moves(clinic_id) if m.delta != 0)",
      "why": "замена списка на генератор — как раз разумная оптимизация, ломает не она сама"
    },
    {
      "match": "    total = sum(m.delta * m.price for m in moves)",
      "why": "первый проход отрабатывает совершенно нормально — в этом и коварство"
    }
  ],
  "verified_by": "",
  "verified_at": ""
}
